---
phase: 27-client-simplification-cleanup
plan: 02
type: execute
wave: 2
depends_on: ["27-01"]
files_modified:
  - tests/test_route_authorization.py
autonomous: true
requirements: [CLNT-01, CLNT-02]

must_haves:
  truths:
    - "All 27-01 changes are verified through integration tests"
    - "Logout endpoint is tested and working"
    - "No client-side match detection from HTTP response remains"
    - "No localStorage token reads remain in production code"
  artifacts:
    - path: "tests/test_route_authorization.py"
      provides: "Tests verifying CLNT-01 and CLNT-02 compliance"
      contains: "test_logout"
  key_links:
    - from: "tests/test_route_authorization.py"
      to: "jellyswipe/__init__.py"
      via: "test client against routes"
      pattern: "auth/logout"
---

<objective>
Verify the Phase 27 client cleanup is complete through targeted integration tests. Confirm no localStorage token usage remains, match detection is SSE-only, and all routes follow new patterns.

Purpose: Ensure the cleanup is thorough and nothing was missed — no regressions in auth flow, route handling, or match delivery.
Output: Comprehensive test coverage for Phase 27 changes.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-client-simplification-cleanup/27-CONTEXT.md
@.planning/phases/27-client-simplification-cleanup/27-01-SUMMARY.md

<interfaces>
<!-- Key test fixtures from existing test file -->

From tests/test_route_authorization.py:
```python
class FakeProvider:
    # Provides server_access_tokens_for_delegate, authenticate_user_session, etc.
    def server_info(self) -> dict  # returns {name, machineIdentifier}

@pytest.fixture
def auth_client(client):
    # Creates authenticated session via /auth/jellyfin-use-server-identity
    # Returns (client, user_id) tuple
```

Server routes under test:
- POST /auth/logout — new endpoint from 27-01
- GET /me — extended with activeRoom from 27-01
- All room routes require /room/{code}/... pattern
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Integration verification tests for Phase 27 compliance</name>
  <files>tests/test_route_authorization.py</files>
  <behavior>
    - Test 1: Full auth lifecycle — delegate login → GET /me (authenticated) → POST /auth/logout → GET /me (401)
    - Test 2: Swipe returns {accepted: true} only with no match field in response body (CLNT-02 verification)
    - Test 3: SSE stream delivers match data with enriched fields (deep_link, rating, duration, year)
    - Test 4: Solo room via POST /room/solo works and go-solo route returns 404
    - Test 5: GET /me returns activeRoom=null before room creation, activeRoom=CODE after POST /room
  </behavior>
  <read_first>
    - tests/test_route_authorization.py (existing test patterns, fixtures, FakeProvider)
    - jellyswipe/__init__.py (current route handlers — especially GET /me, POST /auth/logout, POST /room, POST /room/solo, POST /room/{code}/swipe)
    - jellyswipe/templates/index.html (verify no localStorage, providerToken, identity headers remain)
  </read_first>
  <action>
    Add a `TestPhase27Compliance` class to tests/test_route_authorization.py with:

    1. **test_auth_lifecycle**: Login via delegate → GET /me returns 200 → POST /auth/logout returns 200 → GET /me returns 401. Proves full session lifecycle works.

    2. **test_swipe_no_match_in_response**: Create room, add deck, swipe → response body is exactly `{accepted: true}` with no `match`, `title`, `thumb`, or `solo` fields. Verifies CLNT-02 server-side (match data only in SSE, not HTTP response).

    3. **test_sse_match_has_enriched_fields**: Create two-player room with two users → both swipe right on same movie → poll /room/{code}/status → response.last_match contains `deep_link`, `rating`, `duration`, `year`, `title`, `thumb`, `movie_id`, `ts`. Verifies client receives complete match data via SSE.

    4. **test_solo_endpoint_not_go_solo**: POST /room/solo creates solo room (200) → POST /room/{code}/go-solo returns 404. Verifies D-18 (old route removed).

    5. **test_me_returns_active_room**: GET /me before room creation → activeRoom is null → POST /room → GET /me → activeRoom is the pairing code → quit room → GET /me → activeRoom is null again. Verifies room tracking.

    After adding tests, also run a codebase grep verification:
    ```bash
    grep -n "localStorage" jellyswipe/templates/index.html  # should return nothing
    grep -n "providerToken\|providerUserId\|providerIdentityHeaders" jellyswipe/templates/index.html  # should return nothing
    ```

    If any of these grep patterns match, note the failures — the test suite verifies server-side behavior but the grep verifies client-side cleanup.
  </action>
  <verify>
    <automated>cd /Users/andrew/Documents/code/kino-swipe/.codenomad/worktrees/EPIC-05 && python -m pytest tests/test_route_authorization.py::TestPhase27Compliance -xvs 2>&1 | tail -40</automated>
  </verify>
  <done>
    - 5 new tests in TestPhase27Compliance class all pass
    - Full test suite (130+ tests) passes
    - grep verification confirms zero localStorage/identity header references in client JS
    - Phase 27 CLNT-01 and CLNT-02 requirements verified through automated tests
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test→Server | Test client simulates browser requests; verifies auth boundaries |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-02-01 | N/A | Test code | accept | Tests are non-production code with no external surface |

</threat_model>

<verification>
1. `python -m pytest tests/test_route_authorization.py -x -q` passes all tests (130+)
2. `grep -c "localStorage" jellyswipe/templates/index.html` returns 0
3. `grep -c "providerToken" jellyswipe/templates/index.html` returns 0
4. TestPhase27Compliance tests cover: auth lifecycle, swipe response shape, SSE match data, solo endpoint, active room tracking
</verification>

<success_criteria>
1. All Phase 27 integration tests pass
2. No localStorage token references remain in client code
3. No identity header references remain in client code
4. Match detection is confirmed SSE-only (swipe response verified as {accepted: true})
5. Full test suite green (130+ tests)
</success_criteria>

<output>
After completion, create `.planning/phases/27-client-simplification-cleanup/27-02-SUMMARY.md`
</output>
