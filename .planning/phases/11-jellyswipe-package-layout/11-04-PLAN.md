---
phase: 11-jellyswipe-package-layout
plan: 04
type: execute
wave: 3
depends_on: [11-02, 11-03]
files_modified:
  - templates/index.html
  - data/index.html
  - Dockerfile
  - docker-compose.yml
  - README.md
autonomous: false
requirements:
  - PKG-01
  - PKG-02
must_haves:
  truths:
    - "All imports throughout the codebase use the new jellyswipe package structure"
    - "Gunicorn entry point is 'jellyswipe:app' (not 'app:app')"
    - "Dockerfile uses 'gunicorn -b 0.0.0.0:5005 jellyswipe:app'"
    - "Flask app starts correctly with gunicorn jellyswipe:app"
    - "Application responds to HTTP requests after refactoring"
  artifacts:
    - path: "templates/index.html"
      provides: "Main UI template (if it had any imports to update)"
    - path: "data/index.html"
      provides: "PWA-oriented template (if it had any imports to update)"
    - path: "Dockerfile"
      provides: "Container image with Gunicorn entry point jellyswipe:app"
      contains: "jellyswipe:app"
    - path: "docker-compose.yml"
      provides: "Docker Compose configuration (may reference app.py or jellyswipe)"
    - path: "README.md"
      provides: "Documentation (may reference app.py or jellyswipe)"
  key_links:
    - from: "Dockerfile"
      to: "jellyswipe/__init__.py"
      via: "Gunicorn WSGI entry point"
      pattern: "jellyswipe:app"
    - from: "docker-compose.yml"
      to: "Dockerfile"
      via: "Compose build reference"
      pattern: "build:"
    - from: "templates/index.html"
      to: "jellyswipe/__init__.py"
      via: "HTTP requests to Flask app"
      pattern: "POST /room/create"
---

<objective>
Update all imports throughout the codebase to use the new **jellyswipe** package structure and update the Gunicorn entry point to **jellyswipe:app**.

Purpose: Per D-07, D-10, D-11 — ensure all code imports from the new package and Gunicorn targets the correct module:attribute.
Output: All imports updated; Dockerfile uses `gunicorn jellyswipe:app`; application verified working.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-jellyswipe-package-layout/11-CONTEXT.md
@.planning/phases/11-jellyswipe-package-layout/11-02-SUMMARY.md
@.planning/phases/11-jellyswipe-package-layout/11-03-SUMMARY.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md

# Current import references (from earlier analysis)
@app.py:67-68 — Already updated in plan 11-02 (now imports from jellyswipe)

# Template files (checked earlier — no Python imports, only CSS imports and JS variables)
@templates/index.html — No media_provider imports, only CSS @import and JS mediaProvider variable
@data/index.html — No media_provider imports, only CSS @import

# Docker configuration
@Dockerfile — Current CMD: `["gunicorn", "-b", "0.0.0.0:5005", "app:app"]`
@docker-compose.yml — May reference app.py or build context
@README.md — May reference app.py in development/install instructions
</context>

<tasks>

<task type="auto">
  <name>Task 1: Search and update any remaining media_provider imports in the codebase</name>
  <files>templates/index.html, data/index.html, app.py</files>
  <action>
    Search the entire codebase for any remaining references to `from media_provider` or `import media_provider` that need to be updated to use the new `jellyswipe` package structure.

    **Known locations:**
    - `app.py` — Already updated in plan 11-02 (now imports from jellyswipe via the shim)
    - `templates/index.html` — Checked earlier, no Python imports (only CSS @import and JS variables)
    - `data/index.html` — Checked earlier, no Python imports (only CSS @import)

    **Search command:**
    ```bash
    grep -r "from media_provider" --include="*.py" --include="*.html" .
    grep -r "import media_provider" --include="*.py" --include="*.html" .
    ```

    If any imports are found, update them:
    - `from media_provider import X` → `from jellyswipe import X` (if X is exported by jellyswipe package)
    - `from media_provider.module import X` → `from jellyswipe.module import X` (flattened structure)

    **Note:** Since app.py is now a thin shim importing from jellyswipe, and templates don't have Python imports, there may be no imports left to update in this task. This is expected — the main import updates were done in plan 11-02.
  </action>
  <verify>No remaining "from media_provider" or "import media_provider" references in codebase (except in comments/documentation)</verify>
  <done>All media_provider imports updated to jellyswipe package structure (or verified none exist)</done>
</task>

<task type="auto">
  <name>Task 2: Update Dockerfile Gunicorn entry point to jellyswipe:app</name>
  <files>Dockerfile</files>
  <action>
    Update the Dockerfile to use the new Gunicorn entry point (per D-10, D-11).

    Find the current CMD line (likely):
    ```dockerfile
    CMD ["gunicorn", "-b", "0.0.0.0:5005", "app:app"]
    ```

    Change it to:
    ```dockerfile
    CMD ["gunicorn", "-b", "0.0.0.0:5005", "jellyswipe:app"]
    ```

    Per D-11, document the Gunicorn entry point **only in the Dockerfile** (not in README or other docs). The README documentation is a separate concern for Phase 12.

    **Note:** Keep all other Dockerfile configuration unchanged (uv sync, port exposure, working directory, etc.). Only change the Gunicorn entry point.
  </action>
  <verify>Dockerfile CMD uses "jellyswipe:app"</verify>
  <done>Dockerfile updated with jellyswipe:app Gunicorn entry point</done>
</task>

<task type="auto">
  <name>Task 3: Update docker-compose.yml if it references app.py</name>
  <files>docker-compose.yml</files>
  <action>
    Check if `docker-compose.yml` has any references to `app.py` that need to be updated to reference the new package structure.

    **Common patterns to look for:**
- `command: python app.py` → Should be removed or updated (we use Gunicorn now, not python app.py)
- `volumes: ./app.py:/app/app.py` → No longer needed since app.py is now a shim
- Build context references — Ensure the build context is still correct (likely `.` for repo root)

    **Read docker-compose.yml** and identify any references to `app.py`. Update them to be compatible with the new package structure.

    **Likely scenario:** docker-compose.yml may not need changes if it only references the Dockerfile (which was updated in task 2). However, if there are explicit volume mounts or commands referencing app.py, they should be removed or updated.

    **Note:** Per D-15, `data/` stays at repo root, so any volume mounts for `./data` should remain unchanged.
  </action>
  <verify>docker-compose.yml has no outdated references to app.py (volume mounts, commands, etc.)</verify>
  <done>docker-compose.yml updated if needed (or verified no changes needed)</done>
</task>

<task type="auto">
  <name>Task 4: Update README.md development instructions (optional cleanup)</name>
  <files>README.md</files>
  <action>
    Check if README.md has any references to `python app.py` or `app:app` in development/install instructions that should be updated to reference the new package structure.

    **Patterns to look for:**
    - `python app.py` → Should be `uv run` or `gunicorn jellyswipe:app` (per Phase 10 uv-first approach)
    - `app:app` in Gunicorn examples → Should be `jellyswipe:app`
    - References to running the app — Should use `uv run` or `gunicorn`

    **Note:** Per D-11, Gunicorn entry point documentation is in the Dockerfile only. However, if README has outdated examples showing `python app.py` or `app:app`, update them to be consistent with the new package structure.

    **Caution:** Don't do a full README rewrite for Phase 12 — that's DOC-01. Only update references that are clearly outdated due to the package structure change (e.g., `python app.py` → `uv run` or `gunicorn jellyswipe:app`).
  </action>
  <verify>README.md has no outdated references to app.py or app:app</verify>
  <done>README.md updated if needed (or verified no changes needed)</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: Verify application works with jellyswipe package</name>
  <files>(manual verification - no files modified)</files>
  <action>(checkpoint task - verification performed by human)</action>
  <verify>(manual verification - see how-to-verify below)</verify>
  <done>(manual verification - approved by human)</done>
  <what-built>
    - All imports updated to use jellyswipe package structure
    - Dockerfile Gunicorn entry point changed to jellyswipe:app
    - docker-compose.yml updated if it referenced app.py
    - README.md updated if it had outdated examples
  </what-built>
  <how-to-verify>
    **Manual verification steps:**

    1. **Import test:**
       ```bash
       python -c "from jellyswipe import app; print('Import successful')"
       ```
       Expected: "Import successful" (no import errors)

    2. **Gunicorn start test (local dev):**
       ```bash
       gunicorn -b 0.0.0.0:5005 jellyswipe:app
       ```
       Expected: Gunicorn starts without errors, logs show "Listening at: http://0.0.0.0:5005"

    3. **HTTP request test:**
       In a separate terminal:
       ```bash
       curl http://localhost:5005/
       ```
       Expected: HTML response (the index.html template rendered)

    4. **Verify templates and static files:**
       - Visit http://localhost:5005/ in a browser
       - Verify the page renders correctly (check for CSS, images)
       - Check browser DevTools Network tab — static assets (icons, manifest) should load from `/static/`

    5. **Verify env validation:**
       Stop Gunicorn and try again with missing env vars:
       ```bash
       unset PLEX_URL PLEX_TOKEN
       gunicorn -b 0.0.0.0:5005 jellyswipe:app
       ```
       Expected: Gunicorn fails to start with "Missing env vars" error (env validation works)

    6. **Restore env vars and verify again:**
       ```bash
       # Set your env vars back
       gunicorn -b 0.0.0.0:5005 jellyswipe:app
       ```
       Expected: Gunicorn starts successfully
  </how-to-verify>
  <resume-signal>Type "approved" if all verification steps pass, or describe any issues encountered.</resume-signal>
</task>

<task type="auto">
  <name>Task 6: Clean up old media_provider/ directory if it still exists</name>
  <files>media_provider/</files>
  <action>
    If the `media_provider/` directory still exists (it should have been emptied after plan 11-01), remove it:

    ```bash
    rm -rf media_provider/
    ```

    Verify that no code references `media_provider` anymore (already done in task 1).

    **Note:** Only do this if the directory exists and is empty. If it still has files, investigate why — they may have been missed during the migration.
  </action>
  <verify>media_provider/ directory removed (if it existed)</verify>
  <done>Old media_provider/ directory cleaned up</done>
</task>

<task type="auto">
  <name>Task 7: Remove or document app.py shim</name>
  <files>app.py</files>
  <action>
    Decide what to do with `app.py` now that it's a thin shim importing from jellyswipe.

    **Options:**
    1. **Keep it as a documented thin re-export** — Update the comment to explicitly state it's a shim for backward compatibility:
       ```python
       """
       Jelly Swipe — Flask application (legacy entry point).

       This file is a thin re-export for backward compatibility.
       The actual Flask app lives in the jellyswipe package.

       Per PKG-01, server code lives under jellyswipe/ and this file
       is maintained as a compatibility shim. New code should import
       from jellyswipe directly: from jellyswipe import app
       """

       from jellyswipe import app  # noqa: F401

       __all__ = ["app"]
       ```

    2. **Remove it** — Delete `app.py` since all code should import from jellyswipe now.

    **Decision:** Per the success criteria in ROADMAP, "No remaining production logic in a repo-root app.py monolith unless it is a documented thin re-export (prefer none)." The preference is to remove it, but keeping it as a documented thin re-export is acceptable if needed for backward compatibility.

    **Action:** Since the Dockerfile now uses `jellyswipe:app` and all imports should be updated, **remove app.py**:
    ```bash
    rm app.py
    ```

    If any downstream tools or documentation still reference `app.py`, they should be updated to reference `jellyswipe` instead.

    **Note:** This is optional per the plan — if keeping the shim is preferred for any reason, that's acceptable. The key requirement (PKG-01) is satisfied either way as long as the production logic is in jellyswipe/ and Gunicorn targets jellyswipe:app.
  </action>
  <verify>app.py removed (or documented as thin re-export if kept)</verify>
  <done>Legacy app.py file cleaned up</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Gunicorn WSGI entry | gunicorn loads jellyswipe:app |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-08 | T | Gunicorn entry point change | accept | No security impact — same Flask app, different import path |
| T-11-09 | I | Docker configuration | accept | Existing security model unchanged — only entry point updated |
</threat_model>

<verification>
1. Verify no remaining "from media_provider" or "import media_provider" references in codebase
2. Verify Dockerfile CMD uses "jellyswipe:app"
3. Verify docker-compose.yml has no outdated app.py references
4. Verify README.md has no outdated app.py or app:app examples
5. Smoke test: `python -c "from jellyswipe import app"` succeeds
6. Gunicorn test: `gunicorn -b 0.0.0.0:5005 jellyswipe:app` starts without errors
7. HTTP test: `curl http://localhost:5005/` returns HTML
8. Verify media_provider/ directory removed (if it existed)
9. Verify app.py removed or documented as thin re-export
</verification>

<success_criteria>
1. No remaining "from media_provider" or "import media_provider" references in code (except comments/docs)
2. Dockerfile CMD is `["gunicorn", "-b", "0.0.0.0:5005", "jellyswipe:app"]`
3. docker-compose.yml has no outdated app.py references
4. README.md has no outdated app.py or app:app examples
5. `from jellyswipe import app` succeeds without errors
6. `gunicorn -b 0.0.0.0:5005 jellyswipe:app` starts successfully
7. Flask app responds to HTTP requests (curl returns HTML)
8. Templates and static files load correctly in browser
9. Env validation still works (missing vars cause startup failure)
10. media_provider/ directory removed
11. app.py removed (or documented as thin re-export)
12. **PKG-01 satisfied:** Server code lives under jellyswipe/ package
13. **PKG-02 satisfied:** Gunicorn imports from jellyswipe package (jellyswipe:app)
</success_criteria>

<output>
After completion, create `.planning/phases/11-jellyswipe-package-layout/11-04-SUMMARY.md`
</output>
