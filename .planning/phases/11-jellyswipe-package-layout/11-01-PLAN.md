---
phase: 11-jellyswipe-package-layout
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - jellyswipe/__init__.py
  - jellyswipe/base.py
  - jellyswipe/plex_library.py
  - jellyswipe/jellyfin_library.py
  - jellyswipe/factory.py
autonomous: true
requirements:
  - PKG-01
must_haves:
  truths:
    - "jellyswipe/ package directory exists with __init__.py skeleton"
    - "media_provider modules are flattened into jellyswipe/ root"
    - "All provider modules (base.py, plex_library.py, jellyfin_library.py, factory.py) import cleanly"
  artifacts:
    - path: "jellyswipe/__init__.py"
      provides: "Package initialization skeleton"
      min_lines: 5
    - path: "jellyswipe/base.py"
      provides: "LibraryMediaProvider abstract base class"
      contains: "class LibraryMediaProvider"
    - path: "jellyswipe/plex_library.py"
      provides: "PlexLibraryProvider implementation"
      contains: "class PlexLibraryProvider"
    - path: "jellyswipe/jellyfin_library.py"
      provides: "JellyfinLibraryProvider implementation"
      contains: "class JellyfinLibraryProvider"
    - path: "jellyswipe/factory.py"
      provides: "Provider factory functions (get_provider, reset)"
      contains: "def get_provider"
  key_links:
    - from: "jellyswipe/factory.py"
      to: "jellyswipe/base.py"
      via: "import LibraryMediaProvider"
      pattern: "from .base import LibraryMediaProvider"
    - from: "jellyswipe/factory.py"
      to: "jellyswipe/plex_library.py"
      via: "import PlexLibraryProvider"
      pattern: "from .plex_library import PlexLibraryProvider"
    - from: "jellyswipe/factory.py"
      to: "jellyswipe/jellyfin_library.py"
      via: "import JellyfinLibraryProvider"
      pattern: "from .jellyfin_library import JellyfinLibraryProvider"
---

<objective>
Create **jellyswipe/** package structure and flatten **media_provider/** modules into **jellyswipe/** root.

Purpose: Establish the package foundation per D-01, D-02, D-03 — Flask app will be created in a later plan.
Output: jellyswipe/ package directory with __init__.py skeleton and flattened provider modules.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-jellyswipe-package-layout/11-CONTEXT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md

# Current structure (pre-change)
@media_provider/__init__.py
@media_provider/base.py
@media_provider/plex_library.py
@media_provider/jellyfin_library.py
@media_provider/factory.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create jellyswipe package directory and __init__.py skeleton</name>
  <files>jellyswipe/__init__.py</files>
  <action>
    Create directory `jellyswipe/` if it doesn't exist.
    Create `jellyswipe/__init__.py` with a minimal package skeleton that exports the Flask `app` object (to be implemented in a later plan). For now, include a placeholder comment indicating the Flask app will be created here.

    Per D-04, the final __init__.py will export **only the Flask `app` object**. Other symbols (get_provider, LibraryMediaProvider, etc.) will be accessed via module paths.

    Initial skeleton:
    ```python
    """Jelly Swipe — Flask application package."""

    # Flask app will be created here in a later plan
    # Per D-04, this module exports only the `app` object
    # Other symbols accessed via module paths: jellyswipe.get_provider, jellyswipe.PlexLibraryProvider, etc.
    ```
  </action>
  <verify>Directory exists and file contains package skeleton</verify>
  <done>jellyswipe/__init__.py created with package skeleton</done>
</task>

<task type="auto">
  <name>Task 2: Flatten media_provider modules into jellyswipe/ root</name>
  <files>jellyswipe/base.py, jellyswipe/plex_library.py, jellyswipe/jellyfin_library.py, jellyswipe/factory.py</files>
  <action>
    Move the following files from `media_provider/` to `jellyswipe/` root (per D-02):
    - `media_provider/base.py` → `jellyswipe/base.py`
    - `media_provider/plex_library.py` → `jellyswipe/plex_library.py`
    - `media_provider/jellyfin_library.py` → `jellyswipe/jellyfin_library.py`
    - `media_provider/factory.py` → `jellyswipe/factory.py`

    **IMPORTANT:** After moving, update all imports within these files to use **relative imports** (per D-08):
    - In `jellyswipe/factory.py`: Change `from media_provider.base import LibraryMediaProvider` → `from .base import LibraryMediaProvider`
    - In `jellyswipe/factory.py`: Change `from media_provider.jellyfin_library import JellyfinLibraryProvider` → `from .jellyfin_library import JellyfinLibraryProvider`
    - In `jellyswipe/factory.py`: Change `from media_provider.plex_library import PlexLibraryProvider` → `from .plex_library import PlexLibraryProvider`

    Do NOT delete `media_provider/` directory yet — it may still be referenced by `app.py` until updated in a later plan.
  </action>
  <verify>Files moved and imports updated to use relative imports</verify>
  <done>All provider modules moved to jellyswipe/ with relative imports</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| package import | Code loads from jellyswipe/ package |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | T | Package structure | accept | No security impact — pure code reorganization |
| T-11-02 | I | Import paths | accept | Import path changes don't affect security model |
</threat_model>

<verification>
1. Verify `jellyswipe/` directory exists with __init__.py
2. Verify all provider modules moved: `jellyswipe/base.py`, `jellyswipe/plex_library.py`, `jellyswipe/jellyfin_library.py`, `jellyswipe/factory.py`
3. Verify relative imports in `jellyswipe/factory.py` (grep "from \.")
4. Smoke test: `python -c "import jellyswipe.base; import jellyswipe.factory"` should succeed
</verification>

<success_criteria>
1. `jellyswipe/` package directory exists with `__init__.py` skeleton
2. All provider modules (base.py, plex_library.py, jellyfin_library.py, factory.py) exist in `jellyswipe/` root
3. All intra-package imports use relative imports (from .module)
4. Provider modules import cleanly: `python -c "import jellyswipe.base, jellyswipe.factory"` succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/11-jellyswipe-package-layout/11-01-SUMMARY.md`
</output>
