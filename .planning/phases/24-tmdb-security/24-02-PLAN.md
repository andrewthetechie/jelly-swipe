---
phase: 24-tmdb-security
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - docker-compose.yml
  - unraid_template/jelly-swipe.html
  - scripts/lint-unraid-template.py
autonomous: true
requirements:
  - TMDB-01
  - TMDB-02

must_haves:
  truths:
    - "README.md references TMDB_ACCESS_TOKEN exclusively (no TMDB_API_KEY)"
    - "docker-compose.yml uses TMDB_ACCESS_TOKEN env var"
    - "Unraid template defines TMDB_ACCESS_TOKEN variable"
    - "Lint script recognizes TMDB_ACCESS_TOKEN as valid env var"
  artifacts:
    - path: "README.md"
      provides: "Updated TMDB setup instructions with v4 token guidance"
      contains: "TMDB_ACCESS_TOKEN"
    - path: "docker-compose.yml"
      provides: "Docker env var with TMDB_ACCESS_TOKEN"
      contains: "TMDB_ACCESS_TOKEN"
    - path: "unraid_template/jelly-swipe.html"
      provides: "Unraid template with TMDB_ACCESS_TOKEN"
      contains: "TMDB_ACCESS_TOKEN"
    - path: "scripts/lint-unraid-template.py"
      provides: "Updated recognized env vars list"
      contains: "TMDB_ACCESS_TOKEN"
  key_links:
    - from: "unraid_template/jelly-swipe.html"
      to: "scripts/lint-unraid-template.py"
      via: "RECOGNIZED_VARS must include TMDB_ACCESS_TOKEN for lint to pass"
      pattern: "TMDB_ACCESS_TOKEN"
---

<objective>
Update all documentation, configuration files, and deployment templates to reference TMDB_ACCESS_TOKEN instead of TMDB_API_KEY.

Purpose: Operators need consistent documentation and configuration that matches the new v4 Bearer token authentication. If docs still say TMDB_API_KEY, operators will be confused and the app will fail to start.

Output: Updated README.md, docker-compose.yml, Unraid template, and lint script — all referencing TMDB_ACCESS_TOKEN with clear v4 token instructions.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/24-tmdb-security/24-CONTEXT.md

<interfaces>
<!-- Files being modified — executor must read each before editing. -->

From README.md — all TMDB_API_KEY occurrences (lines 63, 76, 86, 93, 96, 100, 102, 157, 172, 184, 241):
- Line 63: `| TMDB_API_KEY | Always | TMDB API key (trailers / cast). |`
- Line 76: `TMDB_API_KEY=your-tmdb-v3-key`
- Line 86: `TMDB_API_KEY=your-tmdb-v3-key`
- Line 93: `- **TMDB API key** — required at startup`
- Lines 96-102: TMDB API instructions section
- Line 157: `TMDB_API_KEY=your_copied_tmdb_key_here`
- Line 172: `-e TMDB_API_KEY=your_copied_tmdb_key_here \`
- Line 184: `- **TMDB_API_KEY** — TMDB API key for trailers and cast information`
- Line 241: TMDB attribution (keep as-is, this is a legal notice)

From docker-compose.yml (line 15):
```yaml
- TMDB_API_KEY=your-tmdb-v3-key
```

From unraid_template/jelly-swipe.html:
- Line 51: `<Name>TMDB_API_KEY</Name>`
- Line 71: `<Config Name="TMDB_API_KEY" Target="TMDB_API_KEY" .../>`

From scripts/lint-unraid-template.py (line 17):
```python
RECOGNIZED_VARS = {
    "JELLYFIN_URL",
    "JELLYFIN_API_KEY",
    "JELLYFIN_USERNAME",
    "JELLYFIN_PASSWORD",
    "TMDB_API_KEY",    # ← change to TMDB_ACCESS_TOKEN
    "FLASK_SECRET",
    "DB_PATH",
    "JELLYFIN_DEVICE_ID",
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update documentation and config files to TMDB_ACCESS_TOKEN</name>
  <files>README.md, docker-compose.yml, unraid_template/jelly-swipe.html, scripts/lint-unraid-template.py</files>
  <read_first>
    - README.md (all sections mentioning TMDB — env var table, setup instructions, docker examples, TMDB API instructions)
    - docker-compose.yml (env var at line 15)
    - unraid_template/jelly-swipe.html (Variable at line 51, Config at line 71)
    - scripts/lint-unraid-template.py (RECOGNIZED_VARS at line 17)
  </read_first>
  <action>
    Per D-12: Update ALL references to TMDB_API_KEY. Per D-13: Update TMDB API instructions to explain v4 token.

    **1. README.md — Replace all TMDB_API_KEY references:**

    a) Line 63 env var table: Change `TMDB_API_KEY` to `TMDB_ACCESS_TOKEN`, description to "TMDB Read Access Token (v4 Bearer token for trailers/cast)."

    b) Lines 76 and 86 (example env files): Change `TMDB_API_KEY=your-tmdb-v3-key` to `TMDB_ACCESS_TOKEN=your-tmdb-read-access-token`

    c) Line 93: Change `TMDB API key` to `TMDB Read Access Token`

    d) Lines 96-102 (TMDB API instructions section): Replace with instructions for finding the Read Access Token in TMDB account settings:
       - Go to https://www.themoviedb.org/settings/api
       - Log in or create a free TMDB account
       - Under "API" section, find "Read Access Token" (this is the v4 Bearer token)
       - Copy the token value (not the API key)
       - Set as `TMDB_ACCESS_TOKEN` environment variable

    e) Line 157 (docker-compose example): Change `TMDB_API_KEY=your_copied_tmdb_key_here` to `TMDB_ACCESS_TOKEN=your_copied_tmdb_token_here`

    f) Line 172 (docker run example): Change `-e TMDB_API_KEY=your_copied_tmdb_key_here \` to `-e TMDB_ACCESS_TOKEN=your_copied_tmdb_token_here \`

    g) Line 184: Change `TMDB_API_KEY — TMDB API key for trailers and cast information` to `TMDB_ACCESS_TOKEN — TMDB Read Access Token for trailers and cast information`

    h) Line 241: TMDB attribution text ("This product uses the TMDB API but is not endorsed or certified by TMDB.") — keep as-is, this is a legal notice unrelated to the env var.

    **2. docker-compose.yml line 15:**
    Change `- TMDB_API_KEY=your-tmdb-v3-key` to `- TMDB_ACCESS_TOKEN=your-tmdb-read-access-token`

    **3. unraid_template/jelly-swipe.html:**
    a) Lines 50-54 Variable block: Change `<Name>TMDB_API_KEY</Name>` to `<Name>TMDB_ACCESS_TOKEN</Name>`
    b) Line 71 Config element: Change `Name="TMDB_API_KEY" Target="TMDB_API_KEY"` to `Name="TMDB_ACCESS_TOKEN" Target="TMDB_ACCESS_TOKEN"` and update Description from "TMDB API key for Trailers to work" to "TMDB Read Access Token for trailers and cast"

    **4. scripts/lint-unraid-template.py line 17:**
    Change `"TMDB_API_KEY",` to `"TMDB_ACCESS_TOKEN",` in the RECOGNIZED_VARS set.

    **5. Verify the lint script passes:**
    ```bash
    python scripts/lint-unraid-template.py unraid_template/jelly-swipe.html
    ```
    Must exit 0 with "validation passed" message.
  </action>
  <acceptance_criteria>
    - `grep -c "TMDB_API_KEY" README.md` returns 0
    - `grep -c "TMDB_API_KEY" docker-compose.yml` returns 0
    - `grep -c "TMDB_API_KEY" unraid_template/jelly-swipe.html` returns 0
    - `grep -c "TMDB_API_KEY" scripts/lint-unraid-template.py` returns 0
    - `grep -c "TMDB_ACCESS_TOKEN" README.md` returns at least 5
    - `grep -c "TMDB_ACCESS_TOKEN" docker-compose.yml` returns 1
    - `grep -c "TMDB_ACCESS_TOKEN" unraid_template/jelly-swipe.html` returns 2
    - `grep -c "TMDB_ACCESS_TOKEN" scripts/lint-unraid-template.py` returns 1
    - `python scripts/lint-unraid-template.py unraid_template/jelly-swipe.html` exits 0
    - README.md contains "Read Access Token" (not just "API key")
  </acceptance_criteria>
  <verify>
    <automated>python scripts/lint-unraid-template.py unraid_template/jelly-swipe.html && grep -c "TMDB_API_KEY" README.md docker-compose.yml unraid_template/jelly-swipe.html scripts/lint-unraid-template.py | grep -v ":0$" && echo "FAIL: TMDB_API_KEY still present" || echo "PASS: No TMDB_API_KEY references remain"</automated>
  </verify>
  <done>
    - All 4 files reference TMDB_ACCESS_TOKEN exclusively
    - Zero TMDB_API_KEY references in modified files
    - README TMDB instructions explain how to find Read Access Token
    - Unraid lint script passes with updated template
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Config → App | Environment variable TMDB_ACCESS_TOKEN containing secret |
| Docs → Operator | Documentation instructs operators to configure credentials securely |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-04 | Info disclosure | README example env vars | accept | Example values are clearly placeholders (your-tmdb-read-access-token). Operators understand to replace with real values. |
| T-24-05 | Info disclosure | docker-compose.yml example | accept | Same as T-24-04 — placeholder values only. |
</threat_model>

<verification>
1. `grep -rn "TMDB_API_KEY" README.md docker-compose.yml unraid_template/jelly-swipe.html scripts/lint-unraid-template.py` — zero results
2. `python scripts/lint-unraid-template.py unraid_template/jelly-swipe.html` — exits 0
3. `grep -c "TMDB_ACCESS_TOKEN" README.md` — at least 5 occurrences
4. Visual check that README TMDB instructions mention "Read Access Token" and link to TMDB API settings
</verification>

<success_criteria>
- Zero TMDB_API_KEY references in all 4 modified files
- TMDB_ACCESS_TOKEN referenced consistently across all deployment configs
- Unraid template lint passes
- README explains v4 token acquisition clearly
</success_criteria>

<output>
After completion, create `.planning/phases/24-tmdb-security/24-02-SUMMARY.md`
</output>
