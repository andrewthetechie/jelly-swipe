---
phase: 15-database-module-tests
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [tests/conftest.py, tests/test_db.py]
autonomous: true
requirements: [DB-01, DB-02, DB-03]
user_setup: []

must_haves:
  truths:
    - "Database tests use tmp_path fixture for isolated SQLite databases"
    - "Tests verify schema initialization creates all 3 tables"
    - "Tests verify migration logic adds correct columns"
    - "Tests verify CRUD operations for rooms, swipes, matches tables"
    - "Database state does not leak between tests (each test starts fresh)"
    - "pytest discovers and runs all database tests successfully"
  artifacts:
    - path: "tests/conftest.py"
      provides: "Database fixtures for isolated testing"
      contains: "db_path fixture, db_connection fixture"
    - path: "tests/test_db.py"
      provides: "Comprehensive database module tests"
      contains: "Tests for get_db, init_db, schema, migrations, CRUD"
  key_links:
    - from: "tests/test_db.py"
      to: "tests/conftest.py"
      via: "pytest fixture system (db_connection, db_path)"
      pattern: "def test_.*\\(db_connection\\)"
    - from: "tests/conftest.py"
      to: "jellyswipe.db"
      via: "monkeypatch DB_PATH global"
      pattern: "monkeypatch.setattr\\(jellyswipe.db, 'DB_PATH',"
    - from: "tests/test_db.py"
      to: "jellyswipe.db"
      via: "direct module import and function calls"
      pattern: "jellyswipe.db.(get_db|init_db)"
---

<objective>
Create database fixtures and comprehensive tests for jellyswipe/db.py module using isolated SQLite databases.

Purpose: Ensure database operations (schema, migrations, CRUD) work correctly and are isolated per test, improving reliability when making changes to the database module.
Output: Database fixtures in conftest.py and test_db.py with comprehensive test coverage.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-database-module-tests/15-CONTEXT.md
@.planning/research/ARCHITECTURE.md
@jellyswipe/db.py
@tests/conftest.py

<!-- Key interfaces from existing codebase -->

From jellyswipe/db.py:
```python
# Global variable that must be patched before calling get_db/init_db
DB_PATH = None

def get_db():
    """Get a database connection."""
    conn = sqlite3.connect(DB_PATH)
    conn.row_factory = sqlite3.Row
    return conn

def init_db():
    """Initialize the database schema and run migrations."""
    # Creates rooms, swipes, matches tables
    # Runs ALTER TABLE migrations for status, user_id, solo_mode, last_match_data
    # Cleans up orphan swipes
```

From tests/conftest.py (Phase 14):
```python
@pytest.fixture(scope="session", autouse=True)
def setup_test_environment():
    # Patches load_dotenv() and Flask() to enable framework-agnostic imports
    # Already sets test environment variables
    ...

@pytest.fixture
def mock_env_vars(monkeypatch):
    # Provides test environment variables for individual tests
    ...
```
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create database fixtures in conftest.py</name>
  <files>tests/conftest.py</files>
  <action>
    Add two new fixtures to tests/conftest.py to support isolated database testing per D-01, D-02, D-03, D-06, and D-43 from CONTEXT.md:

    1. **db_path fixture** (function-scoped):
       - Use pytest's tmp_path fixture to create a temporary database file
       - Yield the absolute path string of the database file
       - No cleanup needed - tmp_path handles automatic deletion
       - Follows Pattern 1 from ARCHITECTURE.md research

    2. **db_connection fixture** (function-scoped):
       - Depends on db_path fixture
       - Use monkeypatch to set jellyswipe.db.DB_PATH to the temp db path (per D-03)
       - Call jellyswipe.db.init_db() to initialize schema (per D-06)
       - Call jellyswipe.db.get_db() to get connection and yield it to test
       - Close connection after test (in finally block or after yield)
       - monkeypatch automatically restores original DB_PATH value

    Both fixtures should be function-scoped to ensure complete test isolation (per D-02). Add fixtures at the bottom of conftest.py after existing fixtures.

    Do NOT modify existing fixtures (setup_test_environment, mock_env_vars).
  </action>
  <verify>
    <automated>uv run pytest --fixtures | grep -E "^(db_path|db_connection)"</automated>
  </verify>
  <done>
    db_path fixture creates temporary database file per test
    db_connection fixture patches DB_PATH, calls init_db, yields connection
    Both fixtures are function-scoped for isolation
    No modifications to existing Phase 14 fixtures
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create test_db.py with database module tests</name>
  <files>tests/test_db.py</files>
  <behavior>
    - Test 1: get_db() returns connection with row_factory configured
    - Test 2: init_db() is idempotent (can be called multiple times safely)
    - Test 3: All 3 tables exist after init_db (rooms, swipes, matches)
    - Test 4: Rooms table has all columns (pairing_code, movie_data, ready, current_genre, solo_mode, last_match_data)
    - Test 5: Swipes table has all columns (room_code, movie_id, user_id, direction)
    - Test 6: Matches table has all columns (room_code, movie_id, title, thumb, status, user_id) with UNIQUE constraint
    - Test 7: Migration adds status column to matches table
    - Test 8: Migration adds user_id column to matches table
    - Test 9: Migration adds user_id column to swipes table
    - Test 10: Migration adds solo_mode column to rooms table
    - Test 11: Migration adds last_match_data column to rooms table
    - Test 12: Can INSERT and SELECT from rooms table
    - Test 13: Can INSERT and SELECT from swipes table
    - Test 14: Can INSERT and SELECT from matches table
    - Test 15: UNIQUE constraint prevents duplicate matches (same room_code, movie_id, user_id)
    - Test 16: Orphaned swipes are deleted (cleanup query works)
    - Test 17: No state leakage between tests (each test gets fresh empty DB)
  </behavior>
  <action>
    Create tests/test_db.py with comprehensive tests for jellyswipe/db.py module:

    **Import required modules:**
    ```python
    import pytest
    import sqlite3
    import jellyswipe.db
    ```

    **Test structure:**
    1. Test get_db connection behavior (row_factory, connection object)
    2. Test init_db idempotency (call twice, no errors)
    3. Test schema initialization (PRAGMA table_info for all 3 tables)
    4. Test migrations (verify columns added if not present)
    5. Test CRUD operations for each table (INSERT, SELECT with assertions)
    6. Test UNIQUE constraint on matches table (expect sqlite3.IntegrityError)
    7. Test orphaned swipes cleanup (insert orphan swipe, verify deleted after init_db)
    8. Test isolation (no data from previous tests in fresh DB)

    Use the db_connection fixture from Task 1 for all tests. Tests should:
    - Use db_connection fixture (gets initialized DB with fresh schema)
    - Use db_connection.execute() for queries
    - Use db_connection.fetchone() or fetchall() to verify results
    - Assert on column presence, row counts, and data values
    - Test error cases with pytest.raises(sqlite3.IntegrityError)

    Follow conventions from Phase 14: function-scoped tests, descriptive names, clear assertions.
  </action>
  <verify>
    <automated>uv run pytest tests/test_db.py -v</automated>
  </verify>
  <done>
    All 17 tests pass (or reasonable subset if combining similar tests)
    Tests cover get_db, init_db, schema, migrations, CRUD per DB-02
    Tests use tmp_path via db_connection fixture per DB-01
    No state leakage between tests per DB-03 (verified by isolation test)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test → jellyswipe.db | Test code directly imports and calls database module functions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01 | Tampering | DB_PATH global variable | mitigate | Monkeypatch in fixture ensures test isolation, original value restored after test |
| T-15-02 | Information Disclosure | Test database files | accept | tmp_path automatically deletes temp files after test completion; no sensitive data in tests |
| T-15-03 | Spoofing | Mock database connections | N/A | Not applicable - tests use real SQLite with real schema, not mocked connections |
| T-15-04 | Repudiation | Test execution evidence | accept | pytest output provides test execution record; no audit trail needed for unit tests |
| T-15-05 | Denial of Service | Test database resource exhaustion | accept | Function-scoped fixtures ensure resources cleaned up after each test; tmp_path prevents disk space issues |
| T-15-06 | Elevation of Privilege | Database file permissions | accept | Tests run in isolated tmp_path directories; no privilege escalation risk in unit test context |

</threat_model>

<verification>
Phase-level verification checks:

1. **pytest discovers database tests**: `uv run pytest --collect-only tests/test_db.py` shows all test functions
2. **All tests pass**: `uv run pytest tests/test_db.py -v` shows no failures
3. **No state leakage**: Run tests twice in same session - results identical; isolation test confirms fresh DB each time
4. **Coverage check**: `uv run pytest --cov=jellyswipe.db --cov-report=term-missing tests/test_db.py` shows db.py covered
5. **No regressions**: Run all tests (`uv run pytest tests/`) - Phase 14 tests still pass

If any verification step fails, diagnose and fix before considering phase complete.
</verification>

<success_criteria>
Phase 15 is complete when:

1. **DB-01 satisfied**: Database tests use tmp_path fixture for isolated SQLite databases
   - db_path fixture uses tmp_path
   - db_connection fixture yields fresh DB per test
   - Temporary database files are automatically cleaned up

2. **DB-02 satisfied**: Database tests cover schema initialization, migrations, and CRUD operations for db.py
   - Tests verify get_db() returns connection with row_factory
   - Tests verify init_db() is idempotent and creates all 3 tables
   - Tests verify all migrations add correct columns
   - Tests verify INSERT/SELECT for rooms, swipes, matches tables
   - Tests verify UNIQUE constraint and orphan cleanup

3. **DB-03 satisfied**: Database tests ensure no state leakage between tests
   - Function-scoped fixtures ensure fresh DB per test
   - Isolation test confirms empty tables at test start
   - Re-running tests produces identical results

4. **pytest integration**: Tests are discoverable, run successfully, and produce clear output
</success_criteria>

<output>
After completion, create `.planning/phases/15-database-module-tests/15-01-SUMMARY.md`
</output>
