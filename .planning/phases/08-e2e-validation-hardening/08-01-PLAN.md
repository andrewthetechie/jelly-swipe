---
phase: "08"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/08-e2e-validation-hardening/08-E2E.md
autonomous: true
requirements: []
---

# Plan 01 — Jellyfin-forward operator E2E narrative (`08-E2E.md`)

<objective>
Create `.planning/phases/08-e2e-validation-hardening/08-E2E.md` documenting **Jellyfin** login → room create → deck swipe → trailer + cast lookup using **operator-run** steps and optional **Flask `test_client()`** snippets, with **dual-session** coverage notes for **JUSR-01** spirit and **cross-links** to `07-VERIFICATION.md`, native `03-` / `04-` / `05-` `*-VERIFICATION.md`, and each `01-`–`05-`*`-VALIDATION.md` section anchors per `08-CONTEXT.md` D-03, D-08, D-12.
</objective>

<threat_model>
| ID | Threat | Severity | Mitigation |
|----|--------|----------|------------|
| T-08-01 | Secrets pasted into E2E markdown | high | Never paste real `JELLYFIN_API_KEY`, Jellyfin access tokens, or `Authorization:` headers; use placeholders like `***REDACTED***` and describe env var **names** only |
| T-08-02 | Misleading PASS without dated operator attestation | medium | Every operator-only flow row includes **Date**, **Environment** (shape), and **Result** (PASS/PARTIAL/FAIL) |
</threat_model>

<tasks>

<task id="08-01-01" type="execute">
<read_first>
- .planning/phases/08-e2e-validation-hardening/08-CONTEXT.md
- .planning/phases/07-verification-closure-jellyfin-parity/07-VERIFICATION.md
- .planning/phases/03-jellyfin-authentication-http-client/03-VERIFICATION.md
- .planning/phases/04-jellyfin-library-media/04-VERIFICATION.md
- .planning/phases/05-user-parity-packaging/05-VERIFICATION.md
- .planning/ROADMAP.md
- app.py
</read_first>
<action>
1. Add new file `.planning/phases/08-e2e-validation-hardening/08-E2E.md` with YAML frontmatter: `title`, `phase: 8`, `status: draft` until operator fills dates, `owner: operator`.
2. Sections (H2 order fixed for deep links): `## Preconditions` (env var **names** for `MEDIA_PROVIDER=jellyfin`, `JELLYFIN_URL`, auth choice, `TMDB_API_KEY`, `FLASK_SECRET` — no values), `## Jellyfin login and session`, `## Room create and deck load`, `## Swipe / match flow`, `## Trailer and cast`, `## Dual browser / identity isolation (JUSR-01 spirit)`, `## Traceability links`.
3. In `## Traceability links`, include Markdown links to: `.planning/phases/07-verification-closure-jellyfin-parity/07-VERIFICATION.md`, `.planning/phases/03-jellyfin-authentication-http-client/03-VERIFICATION.md`, `.planning/phases/04-jellyfin-library-media/04-VERIFICATION.md`, `.planning/phases/05-user-parity-packaging/05-VERIFICATION.md`, and **each** of `.planning/phases/01-configuration-startup/01-VALIDATION.md`, `.planning/phases/02-media-provider-abstraction/02-VALIDATION.md`, `.planning/phases/05-user-parity-packaging/05-VALIDATION.md`, plus **planned** paths `.planning/phases/03-jellyfin-authentication-http-client/03-VALIDATION.md` and `.planning/phases/04-jellyfin-library-media/04-VALIDATION.md` with a one-line note “created in Phase 8 plan 02”.
4. Under `## Trailer and cast`, cite concrete route names from `app.py` (e.g. `/get-trailer/<movie_id>` and `/cast/<movie_id>`) and instruct operator to use a `movie_id` taken from an active Jellyfin-backed deck JSON field named `id` as emitted by the app (cite `04-VERIFICATION.md` for field parity evidence).
5. Include one **copy-paste** `python -c` or `test_client` snippet for a **non-secret** startup/import check mirroring Phase 3 style (`env -i PATH="$PATH" HOME="$HOME" MEDIA_PROVIDER=jellyfin ...`) that does **not** print tokens.
</action>
<acceptance_criteria>
- `test -f .planning/phases/08-e2e-validation-hardening/08-E2E.md` exits 0.
- `grep -n "## Preconditions" .planning/phases/08-e2e-validation-hardening/08-E2E.md` exits 0.
- `grep -n "## Traceability links" .planning/phases/08-e2e-validation-hardening/08-E2E.md` exits 0.
- `grep -n "07-VERIFICATION.md" .planning/phases/08-e2e-validation-hardening/08-E2E.md` exits 0.
- `grep -nE "JELLYFIN_API_KEY=[^*]|PLEX_TOKEN=|Authorization: Bearer [A-Za-z0-9_-]{8,}" .planning/phases/08-e2e-validation-hardening/08-E2E.md` exits 1.
</acceptance_criteria>
</task>

</tasks>

<verification>
1. Re-read `08-E2E.md` for operator clarity (each subsection has Date/Environment/Result placeholders or filled values).
2. Re-run the secret-pattern `grep` from acceptance (must exit 1).
</verification>

<success_criteria>
- Milestone roadmap success criterion **#1** has a durable home in `08-E2E.md` with explicit Jellyfin-forward steps and links out to Phase 7 + native verification artifacts.
</success_criteria>
