---
phase: 34-sse-route-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - uv.lock
autonomous: true
requirements:
  - FAPI-03
must_haves:
  truths:
    - "sse-starlette is listed as a runtime dependency in pyproject.toml"
    - "uv.lock is regenerated and consistent with pyproject.toml"
    - "uv sync completes without errors"
  artifacts:
    - path: "pyproject.toml"
      provides: "sse-starlette>=3.4.1 entry in [project.dependencies]"
      contains: "sse-starlette>=3.4.1"
    - path: "uv.lock"
      provides: "resolved sse-starlette package entry"
      contains: "sse-starlette"
  key_links:
    - from: "pyproject.toml"
      to: "uv.lock"
      via: "uv sync"
      pattern: "sse-starlette"
---

<objective>
Add `sse-starlette>=3.4.1` to pyproject.toml runtime dependencies and regenerate uv.lock so that the SSE implementation in Plan 02 has the package available to import.

Purpose: The `EventSourceResponse` class from `sse_starlette.sse` replaces the manual `StreamingResponse` SSE pattern (D-03). The package must be installed before any code changes reference it.
Output: Updated pyproject.toml and regenerated uv.lock.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add sse-starlette to pyproject.toml and regenerate lockfile</name>
  <files>pyproject.toml, uv.lock</files>
  <read_first>
    - pyproject.toml — read the full [project.dependencies] block to see current order before inserting
  </read_first>
  <action>
    Edit pyproject.toml: in the `[project.dependencies]` list, add `"sse-starlette>=3.4.1"` in alphabetical order — after `"requests>=2.33.1"` and before `"uvicorn[standard]>=0.46.0"`.

    The resulting dependencies block must be:

    ```toml
    dependencies = [
        "fastapi>=0.136.1",
        "itsdangerous>=2.2.0",
        "jinja2>=3.1.6",
        "python-dotenv>=1.2.2",
        "python-multipart>=0.0.18",
        "requests>=2.33.1",
        "sse-starlette>=3.4.1",
        "uvicorn[standard]>=0.46.0",
    ]
    ```

    Then run `uv sync` from the project root to regenerate uv.lock. Do NOT run uv add — edit the file directly and sync.
  </action>
  <verify>
    <automated>grep 'sse-starlette' /Users/aherrington/Documents/code/jelly-swipe/pyproject.toml && grep 'sse-starlette' /Users/aherrington/Documents/code/jelly-swipe/uv.lock && uv sync --project /Users/aherrington/Documents/code/jelly-swipe 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - pyproject.toml contains the line `"sse-starlette>=3.4.1",`
    - uv.lock contains at least one line matching `sse-starlette`
    - `uv sync` exits 0 with no error output
    - sse-starlette entry appears between `requests` and `uvicorn` lines in pyproject.toml (alphabetical order preserved)
  </acceptance_criteria>
  <done>pyproject.toml declares sse-starlette>=3.4.1 and uv.lock is consistent; `from sse_starlette.sse import EventSourceResponse` will succeed in the venv.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| supply chain | Third-party package sse-starlette pulled from PyPI |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-01-01 | Tampering | sse-starlette package (supply chain) | accept | uv pins exact hashes in uv.lock; version constraint >=3.4.1 limits exposure to known good releases; no sensitive data touched in this plan |
</threat_model>

<verification>
1. `grep '"sse-starlette>=3.4.1"' pyproject.toml` returns a match
2. `grep 'sse-starlette' uv.lock` returns at least one line
3. `uv sync` exits 0
4. `python -c "from sse_starlette.sse import EventSourceResponse"` exits 0
</verification>

<success_criteria>
sse-starlette is a resolved runtime dependency: it appears in pyproject.toml, is pinned in uv.lock, and can be imported in the active venv.
</success_criteria>

<output>
After completion, create `.planning/phases/34-sse-route-migration/34-01-SUMMARY.md`
</output>
