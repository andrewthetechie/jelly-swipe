---
phase: "05"
plan: "02"
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - templates/index.html
  - README.md
  - requirements.txt
autonomous: true
requirements:
  - JUSR-03
  - JUSR-04
---

# Plan 02 — Frontend contract + packaging

<objective>
Implement provider-aware frontend auth/header behavior and document the header contract for Jellyfin mode while keeping Docker/build dependency chain healthy.
</objective>

<threat_model>
| ID | Threat | Severity | Mitigation |
|----|--------|----------|------------|
| T-01 | Frontend keeps Plex-only assumptions in Jellyfin mode | medium | Provider-aware login flow + provider identity headers |
| T-02 | Dependency drift breaks build | medium | Keep requirements explicit and validate docker build |
</threat_model>

<tasks>
<task id="05-02-01" type="execute">
<read_first>
- templates/index.html
- app.py
</read_first>
<action>
Update frontend auth and request headers by provider: Jellyfin login prompt path, provider token/id local storage keys, provider identity headers for match/delete/undo/swipe/watchlist requests, and provider-specific login CTA text.
</action>
<acceptance_criteria>
- `templates/index.html` contains `mediaProvider` variable and `providerIdentityHeaders(`
- `templates/index.html` uses providerIdentityHeaders for `/matches`, `/undo`, `/matches/delete`, `/room/swipe`
</acceptance_criteria>
</task>

<task id="05-02-02" type="execute">
<read_first>
- README.md
- requirements.txt
- .github/workflows/docker-image.yml
</read_first>
<action>
Document Jellyfin user identity/header contract and login flow in README, keep requirements complete, and verify packaging path (`pip install -r requirements.txt`, `docker build .`) remains valid.
</action>
<acceptance_criteria>
- README includes provider header/session note for Jellyfin mode
- `python -m py_compile app.py media_provider/*.py` exits 0
- `docker build .` can be executed without missing dependency errors
</acceptance_criteria>
</task>
</tasks>

<verification>
1. Browser in Jellyfin mode can log in and reach main menu.
2. Requests in devtools include provider identity headers.
3. `docker build .` passes.
</verification>
