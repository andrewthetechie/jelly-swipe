---
phase: "05"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - app.py
  - media_provider/jellyfin_library.py
autonomous: true
requirements:
  - JUSR-01
  - JUSR-02
---

# Plan 01 — Server-side Jellyfin user identity and list parity

<objective>
Implement provider-aware user identity resolution and Jellyfin list-add parity so per-user rows in `matches/swipes` are scoped by Jellyfin user id in Jellyfin mode, and `/watchlist/add` can add to user favorites with valid user token.
</objective>

<threat_model>
| ID | Threat | Severity | Mitigation |
|----|--------|----------|------------|
| T-01 | User token leakage in errors/logs | high | Return generic auth errors; never echo token |
| T-02 | Cross-user data bleed in room/history | high | Resolve provider user id per request; require identity for mutable endpoints |
</threat_model>

<tasks>
<task id="05-01-01" type="execute">
<read_first>
- app.py
- media_provider/jellyfin_library.py
- .planning/phases/05-user-parity-packaging/05-CONTEXT.md
</read_first>
<action>
Add Jellyfin user-session helpers in `JellyfinLibraryProvider`: user login exchange, resolve user id from user token, and add favorite endpoint helper. Add provider-aware auth routes and identity extraction in `app.py` (`/auth/provider`, `/auth/jellyfin-login`, `_provider_user_id_from_request`).
</action>
<acceptance_criteria>
- `app.py` contains `/auth/jellyfin-login` route
- `media_provider/jellyfin_library.py` contains `add_to_user_favorites(` and `resolve_user_id_from_token(`
- `python -m py_compile app.py media_provider/jellyfin_library.py` exits 0
</acceptance_criteria>
</task>

<task id="05-01-02" type="execute">
<read_first>
- app.py
</read_first>
<action>
Update `/watchlist/add`, `/matches`, `/matches/delete`, `/undo`, and jellyfin-mode `/room/swipe` identity path to use provider user id semantics; keep `plex_id` DB column while storing Jellyfin user IDs in Jellyfin mode.
</action>
<acceptance_criteria>
- `rg "_provider_user_id_from_request" app.py` finds usage in matches/delete/undo
- `rg "MEDIA_PROVIDER == \"jellyfin\"" app.py` includes watchlist and swipe branches
</acceptance_criteria>
</task>
</tasks>

<verification>
1. `python -m py_compile app.py media_provider/jellyfin_library.py`
2. Jellyfin mode: `/watchlist/add` with user token succeeds; missing token returns 401.
3. Two-user manual run shows separate per-user rows using shared `plex_id` column semantics.
</verification>
