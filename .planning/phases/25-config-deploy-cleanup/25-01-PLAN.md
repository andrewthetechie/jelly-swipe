---
phase: 25-config-deploy-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - jellyswipe/static/manifest.json
  - data/manifest.json
  - data/index.html
  - requirements.txt
autonomous: true
requirements:
  - CFG-01
  - CFG-02
  - CFG-03
  - CFG-04

must_haves:
  truths:
    - "Both manifest.json files describe Jellyfin only — no 'Plex or' text"
    - "data/index.html no longer exists on disk"
    - "requirements.txt no longer exists on disk"
    - "Unraid template remains Plex-free (already clean, verified)"
  artifacts:
    - path: "jellyswipe/static/manifest.json"
      provides: "PWA manifest with Jellyfin-only description"
      contains: "Jellyfin library"
    - path: "data/manifest.json"
      provides: "Duplicate PWA manifest with Jellyfin-only description"
      contains: "Jellyfin library"
  key_links:
    - from: "jellyswipe/static/manifest.json"
      to: "description field"
      via: "text replacement"
      pattern: "Tinder-style movie matching for your Jellyfin library"
---

<objective>
Update manifest descriptions to remove "Plex or" and delete two dead files (data/index.html, requirements.txt).

Purpose: Per D-01, D-02, and D-04 — remove all Plex references from config/deploy artifacts and delete deprecated files. CFG-03 (Unraid template) is pre-completed and requires no changes.
Output: Clean manifests, two files deleted, all CFG requirements satisfied.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/25-config-deploy-cleanup/25-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update manifest descriptions to Jellyfin-only (CFG-01)</name>
  <files>jellyswipe/static/manifest.json, data/manifest.json</files>
  <action>
Per D-01: In both `jellyswipe/static/manifest.json` and `data/manifest.json`, replace the `description` field value from:
  `"Tinder-style movie matching for your Plex or Jellyfin library."`
to:
  `"Tinder-style movie matching for your Jellyfin library."`

Both files are identical — same edit in both. This is a simple text replacement: drop "Plex or " from the sentence.

Do NOT modify any other field in either manifest.
  </action>
  <verify>
    <automated>rg -i 'plex' jellyswipe/static/manifest.json data/manifest.json; echo "Exit: $?"</automated>
    Expected: zero matches (exit code 1 from rg). Both files should contain "Jellyfin library" without "Plex".
  </verify>
  <done>Both manifest.json files contain description "Tinder-style movie matching for your Jellyfin library." with zero Plex references.</done>
</task>

<task type="auto">
  <name>Task 2: Delete dead data/index.html and deprecated requirements.txt (CFG-02, CFG-04)</name>
  <files>data/index.html, requirements.txt</files>
  <action>
Per D-02: Delete `data/index.html` entirely. It is a 1032-line never-fetched PWA shell containing stale Plex references. Nothing references or serves this file — verified: `rg 'data/index.html'` in jellyswipe/, README.md, Dockerfile returns no matches.

Per D-04: Delete `requirements.txt` entirely. The file header says "Deprecated for local dev: use uv". Docker uses `pyproject.toml` + `uv.lock`. The file contains the `plexapi` dependency. Verified: no references to `requirements.txt` in README.md, Dockerfile, or CONTRIBUTING.md.

Per CFG-03: The Unraid template (`unraid_template/jelly-swipe.html`) is already clean — verified: `rg -i 'plex' unraid_template/jelly-swipe.html` returns zero matches. No changes needed; note this in commit message.

Execute:
```bash
rm data/index.html
rm requirements.txt
```

Then verify both files are gone and no broken references remain:
```bash
test ! -f data/index.html && echo "data/index.html deleted" || echo "ERROR: still exists"
test ! -f requirements.txt && echo "requirements.txt deleted" || echo "ERROR: still exists"
rg -i 'requirements\.txt' README.md Dockerfile CONTRIBUTING.md .github/ 2>/dev/null || echo "No broken references"
```
  </action>
  <verify>
    <automated>test ! -f data/index.html && test ! -f requirements.txt && echo "Both deleted OK" || echo "ERROR: file still exists"</automated>
    Expected: "Both deleted OK"
  </verify>
  <done>data/index.html and requirements.txt no longer exist on disk. No broken references in documentation or build files. Unraid template confirmed clean (no changes needed).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | This phase only deletes dead files and edits manifest descriptions. No trust boundaries crossed — no user input, no API calls, no auth changes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-01 | I | manifest.json description | accept | Description is a static string with no user-controlled input — no information disclosure risk |
| T-25-02 | T | File deletion | accept | Deleting dead files with no dependents — no tampering surface created |

Note: Pure deletion/text-edit phase with minimal security surface. No code execution paths modified.
</threat_model>

<verification>
1. `rg -i 'plex' jellyswipe/static/manifest.json data/manifest.json` — zero matches
2. `test ! -f data/index.html` — file deleted
3. `test ! -f requirements.txt` — file deleted
4. `rg -i 'plex' unraid_template/jelly-swipe.html` — zero matches (pre-cleaned, unchanged)
5. `git status` — shows 2 modified files (manifests) + 2 deleted files
</verification>

<success_criteria>
1. Both manifest.json files have description "Tinder-style movie matching for your Jellyfin library." — no Plex text
2. data/index.html does not exist on disk
3. requirements.txt does not exist on disk
4. Unraid template unchanged and Plex-free (pre-verified)
5. All 4 CFG requirements (CFG-01, CFG-02, CFG-03, CFG-04) satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/25-config-deploy-cleanup/25-01-SUMMARY.md`
</output>
