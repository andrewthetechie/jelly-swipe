---
phase: 39-room-swipe-match-and-sse-persistence-conversion
plan: 02
type: execute
wave: 2
depends_on:
  - 39-01
files_modified:
  - jellyswipe/services/room_lifecycle.py
  - jellyswipe/routers/rooms.py
  - tests/test_room_lifecycle.py
  - tests/test_routes_room.py
  - tests/test_route_authorization.py
autonomous: true
requirements:
  - MVC-02
  - MVC-04
  - PAR-02
must_haves:
  truths:
    - "D-02/D-03/D-04: room create, solo create, join, quit, deck, genre, status, and match-history flows all use one room-lifecycle service, solo mode stays a flag on the shared room path, and room teardown remains immediate."
    - "D-08/D-15: room status and match-history controllers keep the same response shapes while reading their state from persisted room/match data instead of inline route SQL."
    - "MVC-04: non-swipe room routes become controller-only handlers that delegate persistence to the service/UoW seam."
  artifacts:
    - path: "jellyswipe/services/room_lifecycle.py"
      provides: "Room lifecycle orchestration service"
      contains: "class RoomLifecycleService"
    - path: "jellyswipe/routers/rooms.py"
      provides: "Controller-only room lifecycle endpoints"
      contains: "await room_lifecycle_service.create_room"
    - path: "tests/test_room_lifecycle.py"
      provides: "Service-level room lifecycle parity tests"
      contains: "async def test_"
    - path: "tests/test_routes_room.py"
      provides: "Room route parity coverage after service migration"
      contains: "test_room_create"
    - path: "tests/test_route_authorization.py"
      provides: "Real-auth room lifecycle and deck parity coverage"
      contains: "TestDeckCursorTracking"
  key_links:
    - from: "jellyswipe/routers/rooms.py"
      to: "jellyswipe/services/room_lifecycle.py"
      via: "room handlers await the room lifecycle service"
      pattern: "room_lifecycle_service\\."
    - from: "jellyswipe/services/room_lifecycle.py"
      to: "jellyswipe/db_uow.py"
      via: "room lifecycle orchestration uses `uow.rooms`, `uow.swipes`, and `uow.matches`"
      pattern: "uow\\.(rooms|swipes|matches)\\."
---

<objective>
Move non-swipe room lifecycle behavior behind an async room-lifecycle service while preserving all current endpoint shapes, session keys, and teardown semantics.

Purpose: This plan finishes the low-risk room CRUD/controller migration so the later swipe and SSE plans can focus on lock and stream semantics instead of still carrying direct room SQL.
Output:
- `RoomLifecycleService` owns create/join/quit/deck/genre/status/history orchestration
- non-swipe room endpoints use `DBUoW` plus the service instead of `get_db_closing()`
- service-level and route-level tests prove multiplayer and solo room behavior stayed compatible
</objective>

<execution_context>
@/Users/aherrington/.codex/get-shit-done/workflows/execute-plan.md
@/Users/aherrington/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/39-room-swipe-match-and-sse-persistence-conversion/39-CONTEXT.md
@.planning/phases/39-room-swipe-match-and-sse-persistence-conversion/39-RESEARCH.md
@.planning/phases/39-room-swipe-match-and-sse-persistence-conversion/39-PATTERNS.md
@.planning/phases/39-room-swipe-match-and-sse-persistence-conversion/39-01-PLAN.md
@jellyswipe/routers/rooms.py
@jellyswipe/auth.py
@jellyswipe/db_uow.py
@jellyswipe/repositories/rooms.py
@jellyswipe/repositories/matches.py
@tests/test_routes_room.py
@tests/test_route_authorization.py

<interfaces>
From `jellyswipe/auth.py` today:
```python
async def resolve_active_room(session_dict: dict, uow: DatabaseUnitOfWork) -> str | None: ...
```

From the repository foundation expected after plan 39-01:
```python
class RoomRepository:
    async def create(...): ...
    async def fetch_status(pairing_code: str) -> RoomStatusSnapshot | None: ...

class MatchRepository:
    async def list_active_for_user(pairing_code: str, user_id: str) -> list[MatchRecord]: ...
    async def list_history_for_user(user_id: str) -> list[MatchRecord]: ...
```

Target service surface for this plan:
```python
class RoomLifecycleService:
    async def create_room(..., solo_mode: bool = False) -> dict: ...
    async def join_room(code: str, ..., user_id: str) -> dict: ...
    async def quit_room(code: str, session_dict: dict, user_id: str, uow: DatabaseUnitOfWork) -> dict: ...
    async def get_deck(code: str, user_id: str, page: int, uow: DatabaseUnitOfWork) -> list[dict]: ...
    async def set_genre(code: str, genre: str, ..., uow: DatabaseUnitOfWork) -> list[dict]: ...
    async def get_status(code: str, uow: DatabaseUnitOfWork) -> dict: ...
    async def get_matches(active_room: str | None, user_id: str, view: str | None, uow: DatabaseUnitOfWork) -> list[dict]: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement the room lifecycle service on top of the repository seam</name>
  <files>jellyswipe/services/room_lifecycle.py, tests/test_room_lifecycle.py</files>
  <read_first>
    - .planning/phases/39-room-swipe-match-and-sse-persistence-conversion/39-CONTEXT.md
    - .planning/phases/39-room-swipe-match-and-sse-persistence-conversion/39-RESEARCH.md
    - jellyswipe/room_types.py
    - jellyswipe/repositories/rooms.py
    - jellyswipe/repositories/swipes.py
    - jellyswipe/repositories/matches.py
    - jellyswipe/auth.py
    - jellyswipe/routers/rooms.py
    - tests/conftest.py
  </read_first>
  <behavior>
    - Test 1: create and solo-create persist one room, initialize the creator cursor at `0`, and set `session_dict["active_room"]` plus `session_dict["solo_mode"]` with `False` or `True` respectively.
    - Test 2: join marks `ready=1`, adds the joining user's cursor at `0`, and keeps the exact `{"status": "success"}` visible contract when the room exists.
    - Test 3: quit performs immediate hard cleanup by deleting the room, deleting room swipes, archiving active matches to `room_code='HISTORY'`, and clearing local room session state.
    - Test 4: deck, genre, status, and match-history helpers preserve the current cursor, genre-reset, and `last_match` semantics.
  </behavior>
  <action>
Create `jellyswipe/services/room_lifecycle.py` with `class RoomLifecycleService` per D-03, D-04, D-08, and D-14.

Implement these async methods with the exact semantics below:
- `create_room(session_dict: dict, user_id: str, provider, uow: DatabaseUnitOfWork) -> dict`
  - generate a 4-digit pairing code using `secrets.randbelow(9000) + 1000`
  - retry at most 10 times
  - call `provider.fetch_deck()` once per successful room creation
  - persist `ready=False`, `current_genre="All"`, `solo_mode=False`, `deck_position_json=json.dumps({user_id: 0})`
  - set `session_dict["active_room"]` and `session_dict["solo_mode"] = False`
  - return `{"pairing_code": pairing_code}`
- `create_solo_room(...) -> dict`
  - same path as `create_room`, but persist `ready=True` and `solo_mode=True`, and set `session_dict["solo_mode"] = True` per D-03
- `join_room(code: str, session_dict: dict, user_id: str, uow: DatabaseUnitOfWork) -> dict | None`
  - if room exists, set `ready=True`, merge `{user_id: 0}` into the stored deck-position map, set `session_dict["active_room"]=code`, `session_dict["solo_mode"]=False`, and return `{"status": "success"}`
  - if missing, return `None`
- `quit_room(code: str, session_dict: dict, user_id: str, uow: DatabaseUnitOfWork) -> dict`
  - call the room/match/swipe repositories so teardown deletes the room immediately, deletes room swipes, archives active matches, and clears `active_room` plus `solo_mode` from the session per D-04
  - always return `{"status": "session_ended"}`
- `get_deck(code: str, user_id: str, page: int, uow: DatabaseUnitOfWork) -> list[dict]`
  - keep `page_size = 20`
  - return `[]` when the room row is missing
- `set_genre(code: str, genre: str, provider, uow: DatabaseUnitOfWork) -> list[dict]`
  - fetch the replacement deck with `provider.fetch_deck(genre)`
  - reset stored deck positions to `json.dumps({})` per the established deck-reset behavior
  - return the new deck list
- `get_status(code: str, uow: DatabaseUnitOfWork) -> dict`
  - keep the exact visible payload keys `ready`, `genre`, `solo`, `last_match`
  - return `{"ready": False}` when the room is missing
- `get_matches(active_room: str | None, user_id: str, view: str | None, uow: DatabaseUnitOfWork) -> list[dict]`
  - use history rows when `view == "history"` and active-room rows otherwise

Create `tests/test_room_lifecycle.py` against the shared Alembic/runtime fixtures. Prefer real `DatabaseUnitOfWork` instances plus `FakeProvider` over route calls so D-16 gets lower-level service coverage.
  </action>
  <verify>
    <automated>./.venv/bin/pytest tests/test_room_lifecycle.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `jellyswipe/services/room_lifecycle.py` contains `class RoomLifecycleService`
    - `jellyswipe/services/room_lifecycle.py` contains `async def create_room`
    - `jellyswipe/services/room_lifecycle.py` contains `async def create_solo_room`
    - `jellyswipe/services/room_lifecycle.py` contains `async def join_room`
    - `jellyswipe/services/room_lifecycle.py` contains `async def quit_room`
    - `jellyswipe/services/room_lifecycle.py` contains `page_size = 20`
    - `tests/test_room_lifecycle.py` contains `session_ended`
    - `tests/test_room_lifecycle.py` contains `HISTORY`
    - `./.venv/bin/pytest tests/test_room_lifecycle.py -q` exits 0
  </acceptance_criteria>
  <done>The room lifecycle service owns create/join/quit/deck/genre/status/history orchestration and its lower-level parity tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migrate non-swipe room routes to controller-only service delegation</name>
  <files>jellyswipe/routers/rooms.py, tests/test_routes_room.py, tests/test_route_authorization.py</files>
  <read_first>
    - jellyswipe/routers/rooms.py
    - jellyswipe/services/room_lifecycle.py
    - jellyswipe/dependencies.py
    - jellyswipe/auth.py
    - tests/test_routes_room.py
    - tests/test_route_authorization.py
    - .planning/phases/39-room-swipe-match-and-sse-persistence-conversion/39-CONTEXT.md
  </read_first>
  <behavior>
    - Test 1: `/room`, `/room/solo`, `/room/{code}/join`, and `/room/{code}/quit` keep the same JSON payloads and session behavior under both dependency-overridden and real-auth clients.
    - Test 2: `/room/{code}/deck`, `/room/{code}/genre`, `/room/{code}/status`, and `/matches` keep pagination, genre reset, status, and history parity.
    - Test 3: deleted or missing rooms still cause `/me` to clear `activeRoom` immediately through the existing `resolve_active_room()` contract.
  </behavior>
  <action>
Update `jellyswipe/routers/rooms.py` so the non-swipe room endpoints become async controller functions that delegate to `RoomLifecycleService` and `DBUoW`, satisfying MVC-04 and D-15.

Apply these concrete changes:
- inject `uow: DBUoW` into `/room`, `/room/solo`, `/room/{code}/join`, `/matches`, `/room/{code}/quit`, `/room/{code}/deck`, `/room/{code}/genre`, and `/room/{code}/status`
- instantiate one module-level `room_lifecycle_service = RoomLifecycleService()` or equivalent reusable service object in the router module
- replace the inline `get_db_closing()` blocks in those endpoints with awaited service calls
- keep endpoint paths, response bodies, status codes, and error strings unchanged:
  - `{"pairing_code": "..."}`
  - `{"status": "success"}`
  - `{"status": "session_ended"}`
  - `{"ready": False}`
  - 404 `{"error": "Invalid Code"}`
  - 400 `{"error": "Genre required"}` and page-validation error behavior
- do not change the existing swipe endpoint or SSE endpoint in this plan

Update `tests/test_routes_room.py` and `tests/test_route_authorization.py` so they continue covering the same visible behavior but now rely on the async controller/service seam. Add or keep real-auth assertions for:
- solo-room `activeRoom` parity via `GET /me`
- genre reset restoring the creator deck cursor to the first movie
- quit archiving active matches to `room_code='HISTORY'`
- stale active-room cleanup after the underlying room is deleted
  </action>
  <verify>
    <automated>./.venv/bin/pytest tests/test_room_lifecycle.py tests/test_routes_room.py tests/test_route_authorization.py -q -k "room or deck or solo or active_room"</automated>
  </verify>
  <acceptance_criteria>
    - `jellyswipe/routers/rooms.py` contains `room_lifecycle_service`
    - `jellyswipe/routers/rooms.py` contains `await room_lifecycle_service.create_room`
    - `jellyswipe/routers/rooms.py` contains `await room_lifecycle_service.create_solo_room`
    - `jellyswipe/routers/rooms.py` contains `await room_lifecycle_service.join_room`
    - `jellyswipe/routers/rooms.py` contains `await room_lifecycle_service.quit_room`
    - `jellyswipe/routers/rooms.py` contains `await room_lifecycle_service.get_deck`
    - `jellyswipe/routers/rooms.py` contains `await room_lifecycle_service.set_genre`
    - `jellyswipe/routers/rooms.py` contains `await room_lifecycle_service.get_status`
    - `tests/test_route_authorization.py` contains `activeRoom`
    - `./.venv/bin/pytest tests/test_room_lifecycle.py tests/test_routes_room.py tests/test_route_authorization.py -q -k "room or deck or solo or active_room"` exits 0
  </acceptance_criteria>
  <done>All non-swipe room lifecycle controllers delegate through `RoomLifecycleService` and preserve multiplayer/solo room parity under both test-client modes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser session -> room lifecycle service | Session-backed room identifiers and room actions are user-controlled inputs |
| provider -> room lifecycle service | Provider deck data becomes persisted room deck state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-04 | S | room join/quit/create endpoints | mitigate | Continue requiring `AuthUser` from `require_auth`; service accepts explicit `user_id` and never trusts body-supplied identity. |
| T-39-05 | T | genre/deck persistence | mitigate | Only persist provider-returned deck payloads and service-generated deck-position JSON; do not accept client deck payloads. |
| T-39-06 | D | pairing-code generation | accept | Ten-attempt collision retry remains bounded and consistent with current behavior; 4-digit code space is a product constraint, not expanded in this phase. |
</threat_model>

<verification>
Run the room lifecycle service tests plus the existing room/authorization route suites focused on create, join, quit, deck, genre, and `/me` active-room behavior.
</verification>

<success_criteria>
Non-swipe room routes no longer own persistence logic, and room lifecycle behavior for solo and multiplayer flows remains unchanged.
</success_criteria>

<output>
After completion, create `.planning/phases/39-room-swipe-match-and-sse-persistence-conversion/39-02-SUMMARY.md`
</output>
