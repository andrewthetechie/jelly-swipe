---
phase: 39-room-swipe-match-and-sse-persistence-conversion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - jellyswipe/room_types.py
  - jellyswipe/repositories/rooms.py
  - jellyswipe/repositories/swipes.py
  - jellyswipe/repositories/matches.py
  - jellyswipe/db_uow.py
  - tests/test_repositories.py
autonomous: true
requirements:
  - MVC-02
  - MVC-03
must_haves:
  truths:
    - "D-13: room, swipe, and match persistence each have their own async repository surface instead of route-level SQL blocks."
    - "D-16: lower-level repository tests lock cursor, history, archive, and session-aware swipe lookup behavior before controller migration starts."
    - "Later room and swipe services can obtain all persistence collaborators from `DatabaseUnitOfWork` without inventing new DB seams mid-phase."
  artifacts:
    - path: "jellyswipe/room_types.py"
      provides: "Shared typed contracts for room, match, and stream snapshots"
      contains: "class StreamSnapshot"
    - path: "jellyswipe/repositories/rooms.py"
      provides: "Async room persistence queries and mutations"
      contains: "class RoomRepository"
    - path: "jellyswipe/repositories/swipes.py"
      provides: "Async swipe persistence helpers"
      contains: "class SwipeRepository"
    - path: "jellyswipe/repositories/matches.py"
      provides: "Async match history and archive helpers"
      contains: "class MatchRepository"
    - path: "jellyswipe/db_uow.py"
      provides: "Room-domain repositories registered on the request-scoped UoW"
      contains: "self.rooms = RoomRepository(session)"
    - path: "tests/test_repositories.py"
      provides: "Repository-level parity coverage for room, swipe, and match persistence"
      contains: "async def test_"
  key_links:
    - from: "jellyswipe/db_uow.py"
      to: "jellyswipe/repositories/rooms.py"
      via: "DatabaseUnitOfWork exposes `rooms`"
      pattern: "self\\.rooms = RoomRepository\\(session\\)"
    - from: "jellyswipe/db_uow.py"
      to: "jellyswipe/repositories/swipes.py"
      via: "DatabaseUnitOfWork exposes `swipes`"
      pattern: "self\\.swipes = SwipeRepository\\(session\\)"
    - from: "jellyswipe/db_uow.py"
      to: "jellyswipe/repositories/matches.py"
      via: "DatabaseUnitOfWork exposes `matches`"
      pattern: "self\\.matches = MatchRepository\\(session\\)"
---

<objective>
Create the repository and type-contract foundation for the Phase 39 room domain migration.

Purpose: Room lifecycle, swipe/match orchestration, and SSE polling all need stable async repository seams before route SQL is removed.
Output:
- Shared room-domain contract types in `jellyswipe/room_types.py`
- Dedicated `rooms`, `swipes`, and `matches` repositories wired into `DatabaseUnitOfWork`
- Repository-focused parity tests that prove the later service plans can rely on these seams
</objective>

<execution_context>
@/Users/aherrington/.codex/get-shit-done/workflows/execute-plan.md
@/Users/aherrington/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/39-room-swipe-match-and-sse-persistence-conversion/39-CONTEXT.md
@.planning/phases/39-room-swipe-match-and-sse-persistence-conversion/39-RESEARCH.md
@.planning/phases/39-room-swipe-match-and-sse-persistence-conversion/39-PATTERNS.md
@.planning/phases/37-async-database-infrastructure/37-01-SUMMARY.md
@.planning/phases/38-auth-persistence-conversion/38-01-SUMMARY.md
@jellyswipe/db_uow.py
@jellyswipe/models/room.py
@jellyswipe/models/swipe.py
@jellyswipe/models/match.py
@jellyswipe/routers/rooms.py
@tests/conftest.py

<interfaces>
From `jellyswipe/db_uow.py` today:
```python
class DatabaseUnitOfWork:
    auth_sessions: AuthSessionRepository
    swipes: SwipeRepository
    async def run_sync(self, fn, /, *args, **kwargs): ...
```

From `jellyswipe/routers/rooms.py` today:
```python
def _get_cursor(conn: Connection, code, user_id): ...
def _set_cursor(conn: Connection, code, user_id, position): ...
def _resolve_movie_meta(movie_data_json, movie_id): ...
```

Repository surface required after this plan:
```python
class RoomRepository:
    async def pairing_code_exists(self, pairing_code: str) -> bool: ...
    async def create(..., deck_position_json: str) -> None: ...
    async def get_room(self, pairing_code: str) -> RoomRecord | None: ...
    async def fetch_status(self, pairing_code: str) -> RoomStatusSnapshot | None: ...

class SwipeRepository:
    async def find_other_right_swipe(...) -> SwipeCounterparty | None: ...
    async def delete_room_swipes(self, pairing_code: str) -> int: ...
    async def delete_by_room_movie_session(...) -> int: ...

class MatchRepository:
    async def list_active_for_user(...) -> list[MatchRecord]: ...
    async def list_history_for_user(user_id: str) -> list[MatchRecord]: ...
    async def archive_active_for_room(pairing_code: str) -> int: ...
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define shared room-domain contracts for repositories and services</name>
  <files>jellyswipe/room_types.py</files>
  <read_first>
    - .planning/phases/39-room-swipe-match-and-sse-persistence-conversion/39-CONTEXT.md
    - .planning/phases/39-room-swipe-match-and-sse-persistence-conversion/39-RESEARCH.md
    - jellyswipe/models/room.py
    - jellyswipe/models/swipe.py
    - jellyswipe/models/match.py
    - jellyswipe/auth_types.py
  </read_first>
  <action>
Create `jellyswipe/room_types.py` as the shared non-ORM contract module for this phase, per D-13 and to support later Plans 39-02 through 39-04 without signature guessing.

Export `@dataclass(slots=True)` records for exactly these cross-layer payloads:
- `RoomRecord(pairing_code: str, movie_data_json: str, ready: bool, current_genre: str, solo_mode: bool, last_match_data_json: str | None, deck_position_json: str | None, deck_order_json: str | None)`
- `RoomStatusSnapshot(ready: bool, genre: str, solo: bool, last_match: dict | None)`
- `StreamSnapshot(ready: bool, genre: str, solo: bool, last_match: dict | None, last_match_ts: str | float | int | None)`
- `MatchRecord(room_code: str, movie_id: str, title: str, thumb: str, status: str, user_id: str, deep_link: str | None, rating: str | None, duration: str | None, year: str | None)`
- `SwipeCounterparty(user_id: str, session_id: str | None)`

Keep JSON-bearing columns as raw `*_json` strings in the record layer per the carried Phase 36 schema decision; do not normalize JSON fields inside the type definitions.
  </action>
  <verify>
    <automated>python -m compileall jellyswipe/room_types.py</automated>
  </verify>
  <acceptance_criteria>
    - `jellyswipe/room_types.py` contains `class RoomRecord`
    - `jellyswipe/room_types.py` contains `class RoomStatusSnapshot`
    - `jellyswipe/room_types.py` contains `class StreamSnapshot`
    - `jellyswipe/room_types.py` contains `class MatchRecord`
    - `jellyswipe/room_types.py` contains `class SwipeCounterparty`
    - `python -m compileall jellyswipe/room_types.py` exits 0
  </acceptance_criteria>
  <done>Shared room-domain contracts exist and later plans can import them without depending on route helpers or ORM entities.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement room-domain repositories and register them on the UoW</name>
  <files>jellyswipe/repositories/rooms.py, jellyswipe/repositories/swipes.py, jellyswipe/repositories/matches.py, jellyswipe/db_uow.py, tests/test_repositories.py</files>
  <read_first>
    - jellyswipe/db_uow.py
    - jellyswipe/room_types.py
    - jellyswipe/models/room.py
    - jellyswipe/models/swipe.py
    - jellyswipe/models/match.py
    - jellyswipe/routers/rooms.py
    - tests/conftest.py
    - .planning/phases/39-room-swipe-match-and-sse-persistence-conversion/39-PATTERNS.md
  </read_first>
  <behavior>
    - Test 1: room repository create/fetch/status methods round-trip `ready`, `solo_mode`, `current_genre`, `deck_position`, and `last_match_data` without route helpers.
    - Test 2: match repository active/history/archive helpers preserve `room_code='HISTORY'` archive semantics and enriched metadata fields.
    - Test 3: swipe repository lookup/delete helpers keep session-aware participant matching behavior needed for D-01 and D-06.
  </behavior>
  <action>
Implement the dedicated repositories required by D-13, and wire them into `DatabaseUnitOfWork`.

Create `jellyswipe/repositories/rooms.py` with `class RoomRepository(session: AsyncSession)` and at least these async methods:
- `pairing_code_exists(pairing_code: str) -> bool`
- `create(pairing_code: str, movie_data_json: str, ready: bool, current_genre: str, solo_mode: bool, deck_position_json: str) -> None`
- `get_room(pairing_code: str) -> RoomRecord | None`
- `set_ready(pairing_code: str, ready: bool) -> int`
- `set_deck_position(pairing_code: str, deck_position_json: str) -> int`
- `set_genre_and_deck(pairing_code: str, genre: str, movie_data_json: str, deck_position_json: str) -> int`
- `fetch_status(pairing_code: str) -> RoomStatusSnapshot | None`
- `fetch_movie_data(pairing_code: str) -> str | None`
- `delete(pairing_code: str) -> int`

Create `jellyswipe/repositories/swipes.py` with `class SwipeRepository(session: AsyncSession)` and these async methods:
- `find_other_right_swipe(pairing_code: str, movie_id: str, user_id: str, session_id: str | None) -> SwipeCounterparty | None`
- `delete_room_swipes(pairing_code: str) -> int`
- `delete_by_room_movie_session(pairing_code: str, movie_id: str, session_id: str | None) -> int`

Create `jellyswipe/repositories/matches.py` with `class MatchRepository(session: AsyncSession)` and these async methods:
- `list_active_for_user(pairing_code: str, user_id: str) -> list[MatchRecord]`
- `list_history_for_user(user_id: str) -> list[MatchRecord]`
- `archive_active_for_room(pairing_code: str) -> int`
- `delete_for_user(movie_id: str, user_id: str) -> int`
- `delete_active_for_room_movie_user(pairing_code: str, movie_id: str, user_id: str) -> int`
- `latest_active_for_room(pairing_code: str) -> MatchRecord | None`

Update `jellyswipe/db_uow.py` so `DatabaseUnitOfWork.__init__` assigns `self.rooms`, `self.swipes`, and `self.matches` from the new repository classes, while keeping `auth_sessions` and `run_sync()` intact.

Create `tests/test_repositories.py` using the shared Alembic/runtime bootstrap from `tests/conftest.py`. Cover at least:
- room create + status snapshot parsing with `last_match_data`
- match history vs active-room queries and archive-to-`HISTORY` behavior
- swipe counterparty lookup that ignores the caller's own `session_id` when it exists and falls back to `user_id` inequality when it does not
  </action>
  <verify>
    <automated>./.venv/bin/pytest tests/test_repositories.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `jellyswipe/repositories/rooms.py` contains `class RoomRepository`
    - `jellyswipe/repositories/swipes.py` contains `class SwipeRepository`
    - `jellyswipe/repositories/matches.py` contains `class MatchRepository`
    - `jellyswipe/db_uow.py` contains `self.rooms = RoomRepository(session)`
    - `jellyswipe/db_uow.py` contains `self.matches = MatchRepository(session)`
    - `jellyswipe/db_uow.py` contains `self.swipes = SwipeRepository(session)`
    - `tests/test_repositories.py` contains `archive_active_for_room`
    - `tests/test_repositories.py` contains `find_other_right_swipe`
    - `./.venv/bin/pytest tests/test_repositories.py -q` exits 0
  </acceptance_criteria>
  <done>The room domain has dedicated repositories, they are reachable through `DatabaseUnitOfWork`, and repository tests prove the later service migrations have a stable persistence seam.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| route/service -> repository | Untrusted request-derived identifiers become DB query parameters |
| repository -> SQLite models | JSON/text persistence fields cross from Python values into stored rows |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-01 | T | room/match repository methods | mitigate | Use SQLAlchemy parameterized statements/ORM filters only; no string-built SQL in new repositories. |
| T-39-02 | I | `room_types.py` JSON-bearing fields | accept | JSON blobs stay as existing persisted text per Phase 36 decision; later services parse only required fields and route tests remain the contract guard. |
| T-39-03 | E | `DatabaseUnitOfWork` repository registry | mitigate | Keep one request-scoped UoW owner so repositories cannot independently commit elevated state outside dependency-managed transactions. |
</threat_model>

<verification>
Run repository tests and confirm `DatabaseUnitOfWork` exposes the new repositories before starting plan 39-02.
</verification>

<success_criteria>
Lower-level room-domain persistence seams exist, are typed, and are verified independently from controller logic.
</success_criteria>

<output>
After completion, create `.planning/phases/39-room-swipe-match-and-sse-persistence-conversion/39-01-SUMMARY.md`
</output>
