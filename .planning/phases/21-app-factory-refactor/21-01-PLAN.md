---
phase: 21-app-factory-refactor
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [jellyswipe/__init__.py]
autonomous: true
requirements: [FACTORY-01]
user_setup: []

must_haves:
  truths:
    - "Application can be created via create_app(test_config=None) factory function"
    - "Global app instance exists for backwards compatibility with Dockerfile/Gunicorn"
    - "Factory accepts test_config parameter for test configuration overrides"
    - "Application runs with existing configuration (no breaking changes)"
    - "Database initialization happens inside factory, not at module import"
    - "Tests can create fresh app instances with test configuration"
  artifacts:
    - path: "jellyswipe/__init__.py"
      provides: "Flask app factory and global app instance"
      exports: ["create_app", "app"]
      min_lines: 515
  key_links:
    - from: "create_app(test_config=None)"
      to: "Flask app instance"
      via: "Factory function creates and configures app"
      pattern: "def create_app.*return app"
    - from: "app = create_app()"
      to: "Dockerfile CMD 'jellyswipe:app'"
      via: "Module-level global app for backwards compatibility"
      pattern: "^app = create_app\\(\\)"
    - from: "test_config parameter"
      to: "app.config updates"
      via: "Factory merges test_config with default configuration"
      pattern: "if test_config.*app.config"
    - from: "init_db() call"
      to: "create_app() factory"
      via: "Database initialization moved from module-level to factory"
      pattern: "def create_app.*init_db\\(\\)"
---

<objective>
Refactor `jellyswipe/__init__.py` from side-effecting module body to an explicit `create_app(test_config=None)` factory function, enabling test isolation while maintaining full backwards compatibility.

Purpose: Allow route tests to create fresh Flask app instances with test-specific configuration (in-memory databases, test secrets), preserving all existing functionality and import patterns.

Output: Refactored `jellyswipe/__init__.py` with factory pattern, global app instance, and backwards-compatible imports.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-app-factory-refactor/21-CONTEXT.md
@.planning/REQUIREMENTS.md
@jellyswipe/__init__.py
@tests/conftest.py
@tests/test_route_authorization.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create create_app() factory function</name>
  <files>jellyswipe/__init__.py</files>
  <action>
    Create `create_app(test_config=None)` factory function in `jellyswipe/__init__.py` with the following structure:

    1. Keep module-level imports unchanged (lines 1-14: pathlib, dotenv, Flask, ProxyFix, stdlib)
    2. Keep module-level load_dotenv() call (lines 2-8) for environment loading
    3. Keep module-level DB_PATH, CLIENT_ID, and _APP_ROOT variables (lines 16-21)
    4. Keep module-level environment variable validation (lines 23-41) - per D-05, this must run at module import time
    5. Define `create_app(test_config=None)` function after environment validation:
       - Create Flask app instance with template_folder and static_folder (move lines 43-45 into factory)
       - Configure ProxyFix middleware (move line 46 into factory)
       - Set app.secret_key from FLASK_SECRET env var (move line 47 into factory)
       - Store JELLYFIN_URL and TMDB_API_KEY in app.config for future use (add new lines)
       - Import JellyfinLibraryProvider and create provider singleton (move lines 52-69 into factory)
       - Import get_db and init_db from db module (move lines 71-72 into factory)
       - Set DB_PATH in db module (move lines 75-76 into factory)
       - Call init_db() to initialize database (move line 515 into factory)
       - Register all routes on the app instance (all @app.route decorators remain in place)
       - Return the configured app instance

    6. When test_config is not None, merge it with app.config to override specific keys:
       - Use `app.config.update(test_config)` to apply test configuration
       - If test_config contains 'DB_PATH', update `jellyswipe.db.DB_PATH` before calling init_db()

    7. Move all route decorator functions (lines 79-514) to be defined after create_app() function
       - Change decorators from `@app.route(...)` to use the app parameter passed to create_app()
       - Store routes in a separate function or define them inline within create_app()
       - Recommended: Define routes inline within create_app() to capture the app closure

    Key design decisions (per CONTEXT.md D-01 through D-16):
    - Factory signature must be exactly `create_app(test_config=None)` (D-01)
    - Environment validation remains at module import time for early production failure (D-05)
    - Database initialization happens inside factory, not at module import (D-10)
    - Provider singleton pattern unchanged (D-13)
    - All route definitions remain in jellyswipe/__init__.py (D-15)

    Avoid:
    - Moving environment variable validation into factory (per D-05)
    - Removing module-level global variables (DB_PATH, CLIENT_ID, etc.)
    - Changing route handler logic or behavior
    - Moving routes to separate files (per D-15)
  </action>
  <verify>
    <automated>python -c "from jellyswipe import create_app; app = create_app(); assert app is not None; print('Factory creates app successfully')"</automated>
  </verify>
  <done>create_app() function exists, accepts test_config parameter, returns configured Flask app instance</done>
</task>

<task type="auto">
  <name>Task 2: Create global app instance for backwards compatibility</name>
  <files>jellyswipe/__init__.py</files>
  <action>
    Add module-level global app instance at the bottom of `jellyswipe/__init__.py`:

    1. After create_app() function and all route definitions are complete, add:
       ```python
       # Create global app instance for backwards compatibility
       # Dockerfile CMD uses: gunicorn jellyswipe:app
       app = create_app()
       ```

    2. Ensure this happens at module import time (not inside any function)

    3. Verify that `jellyswipe.app` is importable and is a Flask app instance

    Key design decisions (per CONTEXT.md D-07 through D-09):
    - Global `app` instance must exist at module level (D-07)
    - Created by calling `app = create_app()` at module level (D-08)
    - All existing imports continue to work without changes (D-09)

    Avoid:
    - Making global `app` conditional (e.g., `if not os.getenv('TESTING'):`)
    - Moving global app creation into a function or lazy initialization
    - Removing or renaming the global `app` variable
  </action>
  <verify>
    <automated>python -c "from jellyswipe import app; assert hasattr(app, 'test_client'); print('Global app instance importable')"</automated>
  </verify>
  <done>Global app instance exists at module level, is importable as jellyswipe.app, is a Flask app instance</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → API | Untrusted HTTP requests cross route handlers |
| test_config → factory | Test code can override app configuration |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-01 | T | test_config parameter in create_app() | accept | Test code is trusted in test environment; no production exposure |
| T-21-02 | I | FLASK_SECRET from environment | accept | Secret required at module import time (D-05); no changes to secret handling |
| T-21-03 | I | DB_PATH configuration | accept | Configuration merge controlled by factory; test isolation is intentional behavior |
| T-21-04 | S | Flask app configuration | accept | No new security surface; refactor is structural only |
</threat_model>

<verification>
## Phase Verification Checklist

- [ ] `create_app(test_config=None)` function exists in jellyswipe/__init__.py
- [ ] Factory returns configured Flask app instance
- [ ] Global `app` instance exists at module level (created by `app = create_app()`)
- [ ] `from jellyswipe import app` works (backwards compatibility)
- [ ] `from jellyswipe import create_app; app = create_app()` works
- [ ] Environment variable validation still runs at module import time
- [ ] Database initialization (init_db()) happens inside factory, not at module import
- [ ] All routes are registered on factory-returned app instance
- [ ] Existing route tests (test_route_authorization.py) still pass
- [ ] Application runs without breaking changes (verified by importing and basic route check)
</verification>

<success_criteria>
1. Factory pattern implemented: `create_app(test_config=None)` function creates and configures Flask app
2. Backwards compatibility maintained: Global `app` instance exists, all imports work unchanged
3. Test isolation enabled: Factory accepts test_config parameter to override DB_PATH and other settings
4. No breaking changes: All existing functionality works, route tests pass, application runs normally
5. Database initialization moved: init_db() called inside factory, not at module import
6. Configuration merge works: test_config properly overrides app.config for tests
</success_criteria>

<output>
After completion, create `.planning/phases/21-app-factory-refactor/21-01-SUMMARY.md`
</output>
