---
phase: 37-async-database-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - uv.lock
  - jellyswipe/db_runtime.py
  - jellyswipe/db_uow.py
  - jellyswipe/dependencies.py
  - jellyswipe/routers/rooms.py
  - tests/test_dependencies.py
autonomous: true
requirements:
  - ADB-01
  - ADB-02
  - ADB-04

must_haves:
  truths:
    - "D-04/D-05: the runtime has a dedicated async module that resolves `DATABASE_URL` first and builds a process-wide `AsyncEngine` plus `async_sessionmaker`"
    - "D-06/D-07: FastAPI exposes a unit-of-work dependency object, not a raw `AsyncSession`, and it commits on success, rolls back on error, and always closes the session"
    - "D-08/D-14: the new dependency surface is ready for bootstrap/startup wiring and the old `DBConn` dependency no longer remains the active DI seam"
    - "D-13: legacy synchronous SQL can still run only through a controlled bridge while the domain migration is incomplete"
  artifacts:
    - path: "jellyswipe/db_runtime.py"
      provides: "Async engine/sessionmaker runtime with SQLite URL resolution"
      contains: "create_async_engine"
    - path: "jellyswipe/db_uow.py"
      provides: "Typed unit-of-work and repository-backed maintenance bridge"
      contains: "class DatabaseUnitOfWork"
    - path: "jellyswipe/dependencies.py"
      provides: "FastAPI DB dependency alias for the async unit of work"
      contains: "DBUoW"
  key_links:
    - from: "jellyswipe/dependencies.py"
      to: "jellyswipe/db_uow.py"
      via: "yield dependency returns `DatabaseUnitOfWork` with automatic commit/rollback"
      pattern: "yield DatabaseUnitOfWork|await session.commit|await session.rollback"
    - from: "jellyswipe/routers/rooms.py"
      to: "jellyswipe/db_uow.py"
      via: "legacy swipe transaction executes through the async UoW bridge"
      pattern: "DBUoW|await uow.run_sync|BEGIN IMMEDIATE"
---

<objective>
Create the async database runtime foundation for Phase 37: add the SQLite async driver, define the dedicated runtime/UoW modules, and replace the old request-scoped DB dependency with an async unit-of-work seam.

Purpose: Later persistence phases cannot migrate auth, room, swipe, match, and SSE code safely until the project has one authoritative async engine/session boundary that FastAPI and tests can share.

Output:
- `aiosqlite` added as the runtime SQLite async driver
- `jellyswipe/db_runtime.py` with async engine/sessionmaker lifecycle helpers
- `jellyswipe/db_uow.py` with the typed unit-of-work boundary and maintenance repositories
- `dependencies.py` and the existing swipe DI callsite moved off `DBConn`
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/37-async-database-infrastructure/37-CONTEXT.md
@.planning/phases/37-async-database-infrastructure/37-RESEARCH.md
@.planning/phases/37-async-database-infrastructure/37-PATTERNS.md
@.planning/phases/36-alembic-baseline-and-sqlalchemy-models/36-03-SUMMARY.md
@pyproject.toml
@jellyswipe/migrations.py
@jellyswipe/dependencies.py
@jellyswipe/routers/rooms.py
@tests/test_dependencies.py

<interfaces>
From `jellyswipe/migrations.py`:
```python
def build_sqlite_url(db_path: str) -> str: ...
def get_database_url(db_path: str | None = None) -> str: ...
def upgrade_to_head(database_url: str | None = None) -> None: ...
```

From `jellyswipe/dependencies.py`:
```python
@dataclass
class AuthUser:
    jf_token: str
    user_id: str

def require_auth(request: Request) -> AuthUser: ...
def get_db_dep(): ...
DBConn = Annotated[sqlite3.Connection, Depends(get_db_dep)]
```

From `jellyswipe/routers/rooms.py`:
```python
async def swipe(
    code: str,
    request: Request,
    conn: DBConn,
    user: AuthUser = Depends(require_auth),
): ...
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the async runtime and unit-of-work contracts</name>
  <files>pyproject.toml, uv.lock, jellyswipe/db_runtime.py, jellyswipe/db_uow.py</files>
  <read_first>
    - pyproject.toml — preserve the existing dependency layout and add the SQLite async driver in the main runtime block
    - .planning/phases/37-async-database-infrastructure/37-CONTEXT.md — implement D-04, D-05, D-06, D-07, and D-08 exactly
    - .planning/phases/37-async-database-infrastructure/37-RESEARCH.md — use the recommended `create_async_engine`, `async_sessionmaker`, and SQLite event-hook patterns
    - jellyswipe/migrations.py — reuse the current `DATABASE_URL`/`DB_PATH` precedence instead of inventing a second resolver
    - jellyswipe/db.py — preserve the current SQLite PRAGMA expectations when moving the runtime path async
  </read_first>
  <action>
Update `pyproject.toml` to add `aiosqlite>=0.22.1` in `[project.dependencies]` next to `sqlalchemy`, then regenerate `uv.lock`.

Create `jellyswipe/db_runtime.py` with one authoritative async runtime surface:

- `build_async_sqlite_url(db_path: str) -> str` returning `sqlite+aiosqlite:///...`
- `get_runtime_database_url(db_path: str | None = None) -> str` that uses this precedence:
  1. explicit `db_path`
  2. `os.environ["DATABASE_URL"]`
  3. `os.environ["DB_PATH"]` converted to `sqlite+aiosqlite:///...`
  4. the current repo default DB file under `data/jellyswipe.db`
- module globals for the process runtime URL, `AsyncEngine`, and `async_sessionmaker`
- `initialize_runtime(database_url: str | None = None) -> None` that creates the engine once with `create_async_engine(...)`, `expire_on_commit=False`, and SQLite connect hooks on `engine.sync_engine` so each connection applies `foreign_keys=ON` and `synchronous=NORMAL`
- `dispose_runtime() -> None` that disposes the engine and clears the cached engine/sessionmaker globals
- `session_factory()` / `get_sessionmaker()` helper that raises a clear error if initialization has not happened yet

Create `jellyswipe/db_uow.py` with:

- `AuthSessionRepository` exposing `delete_expired(cutoff_iso: str) -> int`
- `SwipeRepository` exposing `delete_orphans() -> int`
- `class DatabaseUnitOfWork` wrapping one `AsyncSession`
  - `auth_sessions` and `swipes` repository properties backed by that session
  - `async def run_sync(self, fn, /, *args, **kwargs)` implemented through `AsyncSession.run_sync(...)` so one legacy sync transaction body can still execute on the managed connection

Do not put Alembic commands, FastAPI imports, or route logic into either module.
  </action>
  <verify>
    <automated>uv run python -c "from jellyswipe.db_runtime import build_async_sqlite_url, get_runtime_database_url; print(build_async_sqlite_url('/tmp/jellyswipe-test.db')); print(get_runtime_database_url('/tmp/jellyswipe-test.db'))"</automated>
  </verify>
  <acceptance_criteria>
    - `pyproject.toml` contains `aiosqlite>=0.22.1`
    - `jellyswipe/db_runtime.py` contains `create_async_engine` and `async_sessionmaker`
    - `jellyswipe/db_runtime.py` contains `def get_runtime_database_url` and checks `DATABASE_URL` before `DB_PATH`
    - `jellyswipe/db_uow.py` contains `class DatabaseUnitOfWork`, `class AuthSessionRepository`, and `class SwipeRepository`
    - `uv run python -c "from jellyswipe.db_runtime import build_async_sqlite_url; print(build_async_sqlite_url('/tmp/a.db'))"` prints a `sqlite+aiosqlite:///` URL
  </acceptance_criteria>
  <done>The repo has a dedicated async runtime module and typed UoW module that later plans can wire into startup and tests</done>
</task>

<task type="auto">
  <name>Task 2: Replace the sync DB dependency surface with the async UoW seam</name>
  <files>jellyswipe/dependencies.py, jellyswipe/routers/rooms.py, tests/test_dependencies.py</files>
  <read_first>
    - jellyswipe/dependencies.py — remove the `get_db_dep` / `DBConn` surface without disturbing auth/provider/rate-limit helpers
    - jellyswipe/routers/rooms.py — preserve the existing `BEGIN IMMEDIATE` transaction semantics in the swipe handler
    - jellyswipe/db_uow.py — use the new UoW API rather than reaching for raw `AsyncSession`
    - tests/test_dependencies.py — rewrite the dependency assertions around the new async generator and commit/rollback lifecycle
    - .planning/phases/37-async-database-infrastructure/37-CONTEXT.md — implement D-06, D-07, D-13, and D-14 without leaving the old dependency alias active
  </read_first>
  <action>
Rewrite `jellyswipe/dependencies.py` so the DB seam becomes:

- `async def get_db_uow()` that:
  - opens one `AsyncSession` from `jellyswipe.db_runtime.get_sessionmaker()`
  - yields `DatabaseUnitOfWork(session)`
  - `await session.commit()` if the downstream code returns successfully
  - `await session.rollback()` if an exception escapes
  - `await session.close()` in `finally`
- `DBUoW = Annotated[DatabaseUnitOfWork, Depends(get_db_uow, scope="function")]`
- `__all__` exports `get_db_uow` and `DBUoW`

Delete `get_db_dep` and `DBConn`. Do not keep a second request-scoped sync DB dependency.

Update `jellyswipe/routers/rooms.py` so the only DI-managed DB callsite (`swipe`) takes `uow: DBUoW` instead of `conn: DBConn`. Preserve the existing SQL and `BEGIN IMMEDIATE` behavior by extracting the transaction body into a sync helper and running it through `await uow.run_sync(...)`. The helper must still:

- set `isolation_level = None`
- execute `BEGIN IMMEDIATE`
- `COMMIT` on success
- `ROLLBACK` on failure
- keep `request.session['session_id']` as the swipe participant identity

Rewrite `tests/test_dependencies.py` to:

- use `@pytest.mark.anyio` for async dependency tests
- assert the yielded object is `DatabaseUnitOfWork`, not `sqlite3.Connection`
- prove the dependency commits on success, rolls back on error, and closes its session
- remove assertions referencing `get_db_dep` or `DBConn`
  </action>
  <verify>
    <automated>uv run pytest tests/test_dependencies.py -q --no-cov</automated>
  </verify>
  <acceptance_criteria>
    - `jellyswipe/dependencies.py` contains `async def get_db_uow` and `DBUoW = Annotated[DatabaseUnitOfWork, Depends(get_db_uow, scope="function")]`
    - `rg -n "get_db_dep|DBConn" jellyswipe/dependencies.py tests/test_dependencies.py` returns 0 matches
    - `jellyswipe/routers/rooms.py` contains `uow: DBUoW` and `await uow.run_sync(`
    - `jellyswipe/routers/rooms.py` still contains `BEGIN IMMEDIATE`
    - `uv run pytest tests/test_dependencies.py -q --no-cov` exits 0
  </acceptance_criteria>
  <done>FastAPI now exposes the async unit-of-work boundary and the old sync request-scoped DB dependency is gone</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| request handler -> DB dependency | untrusted HTTP input reaches the per-request database transaction boundary |
| async runtime globals -> concurrent requests | engine/sessionmaker state is shared process-wide while sessions must remain per-request |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-01 | Elevation of Privilege | `jellyswipe/dependencies.py` | mitigate | yield a fresh `AsyncSession` per dependency call and never cache `AsyncSession` globally |
| T-37-02 | Tampering | `jellyswipe/routers/rooms.py` swipe transaction | mitigate | preserve `BEGIN IMMEDIATE` by executing the legacy SQL body through `DatabaseUnitOfWork.run_sync(...)` instead of rewriting the transaction semantics in this phase |
| T-37-03 | Denial of Service | `get_db_uow()` lifecycle | mitigate | rollback on exceptions and always close the session in `finally` so broken requests do not leak open transactions |
</threat_model>

<verification>
After both tasks complete:

```bash
uv run python -c "from jellyswipe.db_runtime import build_async_sqlite_url; print(build_async_sqlite_url('/tmp/jellyswipe-test.db'))"
rg -n "get_db_dep|DBConn" jellyswipe/dependencies.py tests/test_dependencies.py
uv run pytest tests/test_dependencies.py -q --no-cov
```
</verification>

<success_criteria>
- the runtime exposes a reusable async engine/sessionmaker path built from `DATABASE_URL`
- FastAPI yields a typed unit-of-work object with automatic commit/rollback/close behavior
- the swipe route no longer depends on the old sync `DBConn` alias
</success_criteria>

<output>
After completion, create `.planning/phases/37-async-database-infrastructure/37-01-SUMMARY.md`
</output>
