---
phase: 37-async-database-infrastructure
plan: 02
type: execute
wave: 2
depends_on:
  - 37-01
files_modified:
  - jellyswipe/bootstrap.py
  - jellyswipe/__init__.py
  - jellyswipe/db.py
  - Dockerfile
  - tests/test_db.py
  - tests/test_infrastructure.py
autonomous: true
requirements:
  - MIG-04
  - ADB-01
  - ADB-04

must_haves:
  truths:
    - "D-01/D-02/D-03: normal startup runs `alembic upgrade head` before Uvicorn starts, and migration failure prevents the server from accepting requests"
    - "D-04/D-08: bootstrap initializes the async runtime against the `DATABASE_URL` target, and app shutdown disposes it cleanly"
    - "D-13/D-15/D-16: Phase 36 runtime maintenance moves onto the async path while sync compatibility stays confined to `jellyswipe/db.py` for still-unconverted callsites"
  artifacts:
    - path: "jellyswipe/bootstrap.py"
      provides: "Pre-Uvicorn migration and runtime bootstrap wrapper"
      contains: "upgrade_to_head"
    - path: "jellyswipe/__init__.py"
      provides: "Thin app factory with no schema/bootstrap work in lifespan startup"
      contains: "await dispose_runtime()"
    - path: "jellyswipe/db.py"
      provides: "Legacy sync shim that delegates runtime maintenance to the async path"
      contains: "asyncio.run"
  key_links:
    - from: "jellyswipe/bootstrap.py"
      to: "jellyswipe/migrations.py"
      via: "bootstrap executes Alembic against the resolved database URL before starting Uvicorn"
      pattern: "upgrade_to_head|get_database_url"
    - from: "jellyswipe/__init__.py"
      to: "jellyswipe/db_runtime.py"
      via: "app shutdown disposes the initialized async engine"
      pattern: "dispose_runtime"
---

<objective>
Move database startup responsibility out of the FastAPI app process and into an explicit bootstrap wrapper that runs migrations first, initializes the async runtime, and then starts Uvicorn.

Purpose: Phase 37 is not complete until the running app stops depending on in-process schema/bootstrap work and instead assumes the database is already migrated and ready before request handling begins.

Output:
- `jellyswipe/bootstrap.py` entrypoint for migration-first startup
- `__init__.py` lifespan reduced to app concerns and async runtime disposal
- `db.py` compatibility shims that delegate Phase 36 maintenance onto the async path
- infrastructure and DB smoke tests updated for the new startup contract
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/37-async-database-infrastructure/37-CONTEXT.md
@.planning/phases/37-async-database-infrastructure/37-RESEARCH.md
@.planning/phases/37-async-database-infrastructure/37-PATTERNS.md
@.planning/phases/37-async-database-infrastructure/37-01-PLAN.md
@jellyswipe/__init__.py
@jellyswipe/db.py
@jellyswipe/migrations.py
@Dockerfile
@tests/test_db.py
@tests/test_infrastructure.py

<interfaces>
From `jellyswipe/db_runtime.py` (created in 37-01):
```python
def get_runtime_database_url(db_path: str | None = None) -> str: ...
async def initialize_runtime(database_url: str | None = None) -> None: ...
async def dispose_runtime() -> None: ...
```

From `jellyswipe/migrations.py`:
```python
def get_database_url(db_path: str | None = None) -> str: ...
def upgrade_to_head(database_url: str | None = None) -> None: ...
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Introduce the migration-first bootstrap runner and thin app lifespan</name>
  <files>jellyswipe/bootstrap.py, jellyswipe/__init__.py, Dockerfile, tests/test_infrastructure.py</files>
  <read_first>
    - jellyswipe/__init__.py — remove startup DB work without disturbing middleware, router wiring, or lazy `app` export
    - jellyswipe/migrations.py — reuse the existing `upgrade_to_head(...)` helper and `DATABASE_URL` resolution order
    - jellyswipe/db_runtime.py — initialize and later dispose the async runtime created in 37-01
    - Dockerfile — keep the two-stage build and port `5005`, only swap the final startup command
    - tests/test_infrastructure.py — extend the existing raw-file startup assertions instead of creating a second smoke test file
    - .planning/phases/37-async-database-infrastructure/37-CONTEXT.md — implement D-01, D-02, D-03, D-04, and D-08 exactly
  </read_first>
  <action>
Create `jellyswipe/bootstrap.py` as the first explicit Python startup wrapper. Its `main()` path must:

- resolve the sync migration URL through `jellyswipe.migrations.get_database_url()`
- call `upgrade_to_head(...)`
- fail fast by re-raising or exiting non-zero if migration fails
- resolve/init the async runtime with `await initialize_runtime(...)` before importing or serving the FastAPI app
- start Uvicorn only after migration and runtime initialization succeed

Use the existing deployment values directly:

- host `0.0.0.0`
- port `5005`
- module target `jellyswipe:app`

Update `jellyswipe/__init__.py` so the app no longer calls `prepare_runtime_database()` or patches `jellyswipe.db.DB_PATH` during lifespan startup. The lifespan should only log startup, perform app teardown work, clear provider singletons, and `await dispose_runtime()` on shutdown.

If `create_app(test_config=...)` receives `DATABASE_URL`, pass that override into the runtime module rather than treating `DB_PATH` as the primary runtime setting. Keep `DB_PATH` test support only for legacy sync helpers that still need a filesystem path during the transition.

Update `Dockerfile` to invoke the bootstrap wrapper with Python instead of direct Uvicorn:

- final `CMD` becomes `["/app/.venv/bin/python", "-m", "jellyswipe.bootstrap"]`

Extend `tests/test_infrastructure.py` so it asserts:

- `aiosqlite` is declared in `pyproject.toml`
- the Docker `CMD` references `python -m jellyswipe.bootstrap`
- direct `uvicorn` launch is no longer the final container entrypoint
  </action>
  <verify>
    <automated>uv run pytest tests/test_infrastructure.py -q --no-cov</automated>
  </verify>
  <acceptance_criteria>
    - `jellyswipe/bootstrap.py` contains `upgrade_to_head(` and `uvicorn.run(`
    - `jellyswipe/bootstrap.py` imports `initialize_runtime`
    - `rg -n "prepare_runtime_database\\(" jellyswipe/__init__.py` returns 0 matches
    - `Dockerfile` contains `python", "-m", "jellyswipe.bootstrap"`
    - `tests/test_infrastructure.py` asserts `aiosqlite` and `jellyswipe.bootstrap`
    - `uv run pytest tests/test_infrastructure.py -q --no-cov` exits 0
  </acceptance_criteria>
  <done>The server now boots through a migration-first wrapper and the FastAPI app no longer owns schema/bootstrap startup work</done>
</task>

<task type="auto">
  <name>Task 2: Move runtime maintenance onto the async path while keeping legacy sync helpers bounded</name>
  <files>jellyswipe/db.py, tests/test_db.py</files>
  <read_first>
    - jellyswipe/db.py — keep the sync connection helpers only as temporary compatibility for unconverted routes and auth code
    - jellyswipe/db_runtime.py — use the authoritative async runtime URL/engine instead of inventing a second setup path
    - jellyswipe/db_uow.py — reuse the repository methods from 37-01 for orphan-swipe and expired-session cleanup
    - tests/test_db.py — update the current runtime smoke assertions instead of leaving them pinned to the pre-async startup shape
    - .planning/phases/36-alembic-baseline-and-sqlalchemy-models/36-03-SUMMARY.md — preserve the Phase 36 runtime expectations while moving the maintenance execution path async
  </read_first>
  <action>
Refactor `jellyswipe/db.py` into a bounded compatibility shim:

- keep `DB_PATH`, `configure_sqlite_connection()`, `get_db()`, and `get_db_closing()` for still-sync route and auth code that later phases will convert
- change `cleanup_orphan_swipes()` and `cleanup_expired_auth_sessions()` to delegate to the async runtime/UoW path using `asyncio.run(...)`
- change `prepare_runtime_database()` into a compatibility wrapper that executes the async maintenance path, not direct sync SQL
- keep the SQLite WAL/foreign-key expectations centralized; do not duplicate runtime setup logic in both `db.py` and `db_runtime.py`

Update `tests/test_db.py` to reflect the new public contract:

- keep the migration baseline assertions intact
- replace the old “runtime helper exists” smoke check with assertions that the bounded sync exports still exist while maintenance helpers now delegate through the async runtime path
- add a regression check that `prepare_runtime_database()` can run after `upgrade_to_head(...)` on a temp DB without dropping the required tables
  </action>
  <verify>
    <automated>uv run pytest tests/test_db.py -q --no-cov</automated>
  </verify>
  <acceptance_criteria>
    - `jellyswipe/db.py` contains `asyncio.run(`
    - `jellyswipe/db.py` still contains `def get_db(` and `def get_db_closing(`
    - `jellyswipe/db.py` no longer contains direct `DELETE FROM swipes` or `DELETE FROM auth_sessions` SQL in the maintenance functions
    - `tests/test_db.py` still verifies `upgrade_to_head(...)` and now includes a `prepare_runtime_database()` compatibility assertion
    - `uv run pytest tests/test_db.py -q --no-cov` exits 0
  </acceptance_criteria>
  <done>Phase 36 maintenance work now runs through the async path while the remaining sync helpers stay narrowly confined for later migration phases</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator environment -> bootstrap wrapper | deployment configuration decides which database receives migrations and runtime initialization |
| bootstrap wrapper -> ASGI server | the server must not start until the database is migrated and runtime initialization succeeds |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-04 | Tampering | `jellyswipe/bootstrap.py` DB target selection | mitigate | resolve the target through one `DATABASE_URL`-first helper path and reuse it for both Alembic and runtime initialization |
| T-37-05 | Denial of Service | startup path | mitigate | keep migration execution in the bootstrap wrapper and fail fast before Uvicorn serves requests when `upgrade_to_head(...)` errors |
| T-37-06 | Denial of Service | async runtime lifecycle | mitigate | initialize the engine explicitly before server start and `await dispose_runtime()` on shutdown so stale engines do not leak across process lifetime |
</threat_model>

<verification>
After both tasks complete:

```bash
rg -n "prepare_runtime_database\\(" jellyswipe/__init__.py
uv run pytest tests/test_infrastructure.py tests/test_db.py -q --no-cov
```
</verification>

<success_criteria>
- the only normal container/app entrypoint is the migration-first bootstrap wrapper
- app lifespan assumes the schema is already ready and only disposes the async runtime on shutdown
- runtime maintenance no longer has a second direct-sync source of truth
</success_criteria>

<output>
After completion, create `.planning/phases/37-async-database-infrastructure/37-02-SUMMARY.md`
</output>
