---
phase: 38-auth-persistence-conversion
plan: 02
type: execute
wave: 2
depends_on:
  - 38-01
files_modified:
  - jellyswipe/auth.py
  - jellyswipe/routers/auth.py
  - tests/test_route_authorization.py
autonomous: true
requirements:
  - MVC-01
  - PAR-01
must_haves:
  truths:
    - "D-04/D-09: auth routes and `require_auth` call the auth service only, not the repository directly, while continuing to return the same lightweight `AuthUser`-style object at the dependency boundary."
    - "D-07/D-08/D-10/D-12: a stale persisted session on `/me` or other protected auth paths still fails with the exact `401 {\"detail\": \"Authentication required\"}` contract and aggressively clears local session cookie/state before the request ends."
    - "D-16: logout still returns `{'status': 'logged_out'}` and leaves the client unauthenticated even when vault cleanup is best-effort behind the scenes, while route tests stay focused on visible auth parity rather than deep cleanup internals."
  artifacts:
    - path: "jellyswipe/auth.py"
      provides: "Auth-route service helpers, including `/me` active-room compatibility behind the auth/UoW seam"
      contains: "async def resolve_active_room"
    - path: "jellyswipe/routers/auth.py"
      provides: "Auth router wired to `DBUoW` and the async auth service"
      contains: "uow: DBUoW"
    - path: "tests/test_route_authorization.py"
      provides: "Visible auth-route parity coverage for login, stale-session clearing, `/me`, and logout"
      contains: "Authentication required"
  key_links:
    - from: "jellyswipe/routers/auth.py"
      to: "jellyswipe/auth.py"
      via: "login, delegate auth, `/me`, and logout all go through async auth-service helpers"
      pattern: "await create_session|await destroy_session|await resolve_active_room"
    - from: "jellyswipe/routers/auth.py"
      to: "jellyswipe/dependencies.py"
      via: "protected auth routes still authorize via `Depends(require_auth)`"
      pattern: "Depends\\(require_auth\\)"
    - from: "tests/test_route_authorization.py"
      to: "jellyswipe/routers/auth.py"
      via: "route tests assert the unchanged 401/logout contract and cookie clearing behavior"
      pattern: "logged_out|Authentication required|cookies\\.get\\(\"session\"\\)"
---

<objective>
Wire the auth router onto the new async auth service and close the route-level parity cases that users can see: login, delegate auth, stale-session clearing, `/me`, and logout.

Purpose: Phase 38 is only done when the real auth endpoints use the Phase 37 UoW path while keeping the exact external auth contract and the aggressive stale-session behavior locked in the context.

Locked decisions covered here: D-04, D-07, D-08, D-09, D-10, D-12, D-16.

Output:
- `jellyswipe/auth.py` exposes the thin auth-service helper needed to keep `/me` room compatibility behind the auth/UoW seam without broad room migration
- `jellyswipe/routers/auth.py` awaits the auth service through `DBUoW` for login, delegate auth, and logout
- `tests/test_route_authorization.py` proves the unchanged route contract plus stale-session clearing and logout cookie clearing
</objective>

<execution_context>
@/Users/aherrington/.codex/get-shit-done/workflows/execute-plan.md
@/Users/aherrington/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/38-auth-persistence-conversion/38-CONTEXT.md
@.planning/phases/38-auth-persistence-conversion/38-RESEARCH.md
@.planning/phases/38-auth-persistence-conversion/38-VALIDATION.md
@.planning/phases/38-auth-persistence-conversion/38-PATTERNS.md
@.planning/phases/37-async-database-infrastructure/37-03-SUMMARY.md
@jellyswipe/auth.py
@jellyswipe/dependencies.py
@jellyswipe/routers/auth.py
@tests/test_route_authorization.py

<interfaces>
From `jellyswipe/dependencies.py` after Plan 01:
```python
@dataclass
class AuthUser:
    jf_token: str
    user_id: str

async def require_auth(request: Request, uow: DBUoW) -> AuthUser: ...
DBUoW = Annotated[DatabaseUnitOfWork, Depends(get_db_uow, scope="function")]
```

From `jellyswipe/auth.py` after Plan 01:
```python
async def create_session(jf_token: str, jf_user_id: str, session_dict: dict, uow: DatabaseUnitOfWork) -> str: ...
async def get_current_token(session_dict: dict, uow: DatabaseUnitOfWork) -> AuthRecord | None: ...
async def destroy_session(session_dict: dict, uow: DatabaseUnitOfWork) -> None: ...
def clear_session_state(session_dict: dict) -> None: ...

async def resolve_active_room(session_dict: dict, uow: DatabaseUnitOfWork) -> str | None: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rewire the auth routes to the async auth service/UoW seam</name>
  <files>jellyswipe/auth.py, jellyswipe/routers/auth.py</files>
  <read_first>
    - jellyswipe/routers/auth.py
    - jellyswipe/auth.py
    - jellyswipe/dependencies.py
    - .planning/phases/38-auth-persistence-conversion/38-CONTEXT.md
    - .planning/phases/38-auth-persistence-conversion/38-RESEARCH.md
    - tests/test_route_authorization.py
  </read_first>
  <behavior>
    - Test 1: `POST /auth/jellyfin-use-server-identity` and `POST /auth/jellyfin-login` still return `{"userId": ...}` and create persisted auth sessions through `DBUoW`.
    - Test 2: `POST /auth/logout` still returns `{"status": "logged_out"}` while delegating delete semantics to the auth service.
    - Test 3: `GET /me` still relies on `Depends(require_auth)` for authorization, but any `active_room` compatibility check now runs behind the auth service/UoW seam instead of direct route-level DB access.
  </behavior>
  <action>
Update `jellyswipe/routers/auth.py` so the router speaks only to the async auth service for auth-session persistence per D-04:

- import `DBUoW` from `jellyswipe.dependencies`
- convert `jellyfin_use_server_identity` to `async def jellyfin_use_server_identity(request: Request, uow: DBUoW)`
- keep `jellyfin_login` async and add `uow: DBUoW`
- convert `logout` to `async def logout(request: Request, uow: DBUoW, user: AuthUser = Depends(require_auth))`
- convert `get_me` to `async def get_me(request: Request, uow: DBUoW, user: AuthUser = Depends(require_auth))`
- in the delegate and login routes, call `await create_session(token, uid, request.session, uow)` or `await create_session(out["token"], out["user_id"], request.session, uow)`; do not touch the repository directly
- in `logout`, call `await destroy_session(request.session, uow)` and keep the exact visible response `{"status": "logged_out"}`
- extend `jellyswipe/auth.py` with `async def resolve_active_room(session_dict: dict, uow: DatabaseUnitOfWork) -> str | None` so `/me` can keep its current room-existence compatibility behavior without route-level DB access:
  - read `active_room` from `session_dict`; return `None` immediately when absent
  - use `await uow.run_sync(...)` with one focused compatibility query against `rooms` on the managed connection to answer whether the current `active_room` still exists
  - when the room no longer exists, remove both `active_room` and `solo_mode` from `session_dict` and return `None`
  - do not introduce a new room repository or broader room persistence migration in this phase
- in `get_me`, replace the direct `get_db_closing` import/SQL block with `active_room = await resolve_active_room(request.session, uow)`
- keep `get_me` and `auth_provider` response bodies unchanged
- preserve the current auth error responses for delegate/login failures and keep `Depends(require_auth)` on protected auth endpoints

This task is the route-layer implementation surface for D-04, D-07, D-08, D-09, D-10, and D-12.
  </action>
  <verify>
    <automated>./.venv/bin/pytest tests/test_route_authorization.py -q -k "login or delegate or logout or get_me"</automated>
  </verify>
  <acceptance_criteria>
    - `jellyswipe/routers/auth.py` contains `uow: DBUoW`
    - `jellyswipe/routers/auth.py` contains `await create_session`, `await destroy_session`, and `await resolve_active_room`
    - `jellyswipe/routers/auth.py` still contains `Depends(require_auth)`
    - `jellyswipe/routers/auth.py` does not contain `get_db_closing`
    - `jellyswipe/auth.py` contains `async def resolve_active_room`
    - `jellyswipe/routers/auth.py` does not import `AuthSessionRepository` or `AsyncSession`
    - `./.venv/bin/pytest tests/test_route_authorization.py -q -k "login or delegate or logout or get_me"` exits 0
  </acceptance_criteria>
  <done>The real auth routes now use the async auth service/UoW seam without changing their user-visible JSON contracts, and `/me` no longer performs direct route-level DB access.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Lock in route-level stale-session and logout parity</name>
  <files>tests/test_route_authorization.py</files>
  <read_first>
    - tests/test_route_authorization.py
    - jellyswipe/routers/auth.py
    - jellyswipe/dependencies.py
    - jellyswipe/auth.py
    - tests/conftest.py
    - .planning/phases/38-auth-persistence-conversion/38-CONTEXT.md
    - .planning/phases/38-auth-persistence-conversion/38-VALIDATION.md
  </read_first>
  <behavior>
    - Test 1: a stale `session_id` cookie with no persisted row on `/me` returns `401 {"detail": "Authentication required"}` and clears the client session cookie/state.
    - Test 2: logout clears the client session cookie/state and leaves `/me` unauthorized on the next request.
    - Test 3: existing login/delegate/logout `/me` parity tests keep passing through the async DB path without widening route-level scope into room/swipe/match persistence changes.
  </behavior>
  <action>
Extend `tests/test_route_authorization.py` with visible-parity auth regressions only; do not broaden this file into room/swipe/match/SSE persistence migration work.

Add or update route-level auth tests as follows:

- add a stale-session regression that:
  - uses `set_session_cookie(client_real_auth, {"session_id": "stale-session", "active_room": "ROOM1", "solo_mode": True}, os.environ["FLASK_SECRET"])`
  - does not seed a matching `auth_sessions` row
  - calls `GET /me`
  - asserts `response.status_code == 401`
  - asserts `response.json() == {"detail": "Authentication required"}`
  - asserts `client_real_auth.cookies.get("session") is None` after the response so the stale local session state was aggressively cleared
- strengthen the logout coverage so after a successful `POST /auth/logout`, the test asserts both:
  - `client_real_auth.cookies.get("session") is None`
  - a follow-up `GET /me` returns `401 {"detail": "Authentication required"}`
- keep the login/delegate route tests focused on visible behavior: `{"userId": ...}`, vault row creation, and protected-route success after auth
- do not duplicate the best-effort delete failure injection here; Plan 01 owns the service-level `auth_session_delete_failed` branch, while this route file owns the visible behavior that must still be true after that service logic is wired in
- keep all existing non-auth room/swipe/match/SSE tests unchanged unless an auth-contract assertion must be updated to reflect the new stale-session clearing behavior
- keep this file lighter than the repository/service tests so D-16 remains true: visible route parity belongs here, deeper cleanup semantics stay in Plan 01
  </action>
  <verify>
    <automated>./.venv/bin/pytest tests/test_route_authorization.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_route_authorization.py` contains a stale-session case with `client_real_auth.cookies.get("session") is None`
    - `tests/test_route_authorization.py` contains `{"detail": "Authentication required"}` assertions for stale-session and post-logout flows
    - `tests/test_route_authorization.py` keeps `{"status": "logged_out"}` for logout
    - `tests/test_route_authorization.py` does not introduce new room/swipe/match/SSE persistence helpers beyond auth-related setup
    - `./.venv/bin/pytest tests/test_route_authorization.py -q` exits 0
  </acceptance_criteria>
  <done>The route-level auth flow now proves stale-session clearing, logout cookie clearing, and unchanged login/logout `/me` response contracts through the async auth persistence path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser cookie jar -> auth routes | untrusted client cookie state drives login-required behavior here |
| auth router -> auth dependency/service | route handlers trust the dependency/service seam to authorize and mutate local session state correctly |
| `/me` route -> auth service/UoW compatibility helper | auth route delegates room-state compatibility checks to the auth service so route code does not perform direct DB access |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-05 | Spoofing | `/me`, `/auth/logout`, protected auth routes | mitigate | keep `Depends(require_auth)` as the only auth gate and preserve the exact 401 contract on missing persisted sessions |
| T-38-06 | Tampering | stale cookie/session state | mitigate | route tests must prove stale `session_id` misses clear the client session cookie/state, not just raise 401 |
| T-38-07 | Repudiation | logout success despite best-effort delete failure | mitigate | route output remains `{"status": "logged_out"}` while Plan 01 logs `auth_session_delete_failed`; route tests confirm visible behavior stays correct |
| T-38-08 | Elevation of Privilege | auth route accidentally reaching room persistence migration scope | mitigate | keep `/me` room-existence compatibility behind the auth service/UoW seam and avoid introducing room/swipe/match/SSE repository work in this phase |
</threat_model>

<verification>
After both tasks complete:

```bash
./.venv/bin/pytest tests/test_auth.py tests/test_dependencies.py tests/test_route_authorization.py -q
rg -n "uow: DBUoW|await create_session|await destroy_session|await resolve_active_room" jellyswipe/routers/auth.py
! rg -n "get_db_closing" jellyswipe/routers/auth.py
rg -n "cookies\\.get\\(\"session\"\\) is None|Authentication required|logged_out" tests/test_route_authorization.py
```
</verification>

<success_criteria>
- the real auth endpoints use the async auth service/UoW seam
- login and delegate auth still return `{"userId": ...}` and create persisted auth sessions
- stale persisted sessions clear local session state aggressively and still surface `401 {"detail": "Authentication required"}`
- logout still returns `{"status": "logged_out"}` and leaves the client unauthenticated on the next request
</success_criteria>

<output>
After completion, create `.planning/phases/38-auth-persistence-conversion/38-02-SUMMARY.md`
</output>
