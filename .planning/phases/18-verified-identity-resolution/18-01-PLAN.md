---
phase: 18
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - jellyswipe/__init__.py
  - jellyswipe/jellyfin_library.py
autonomous: true
requirements:
  - SEC-01
  - SEC-02
user_setup: []
must_haves:
  truths:
    - "_provider_user_id_from_request() resolves identity from delegated identity first, then validated token path"
    - "Client-supplied identity headers are not accepted as identity aliases"
    - "Token-to-user-id cache exists with 5-minute TTL and hash-based keying"
    - "Token parsing remains compatible via extract_media_browser_token()"
  artifacts:
    - path: "jellyswipe/__init__.py"
      provides: "Hardened identity resolution and alias rejection policy"
      min_lines: 300
    - path: "jellyswipe/jellyfin_library.py"
      provides: "Token extraction and user-id resolution compatibility baseline"
      min_lines: 300
  key_links:
    - from: "jellyswipe/__init__.py"
      to: "jellyswipe/jellyfin_library.py"
      via: "_jellyfin_user_token_from_request() and resolve_user_id_from_token()"
      pattern: "validated token -> Jellyfin user id"
---

<objective>
Implement Phase 18 identity hardening by enforcing trusted identity-source precedence and blocking spoofable alias headers while preserving valid Jellyfin token compatibility.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/18-verified-identity-resolution/18-CONTEXT.md
@.planning/phases/18-verified-identity-resolution/18-RESEARCH.md
@.planning/phases/18-verified-identity-resolution/18-VALIDATION.md
@.planning/phases/18-verified-identity-resolution/18-PATTERNS.md
@jellyswipe/__init__.py
@jellyswipe/jellyfin_library.py
</context>

<tasks>

<task id="18-01-01" type="execute">
<read_first>
- jellyswipe/__init__.py
- jellyswipe/jellyfin_library.py
- .planning/phases/18-verified-identity-resolution/18-CONTEXT.md
- .planning/phases/18-verified-identity-resolution/18-RESEARCH.md
</read_first>
<action>
Refactor identity helper logic in `jellyswipe/__init__.py` to enforce trusted source order and explicit spoof-header handling:
1. Keep delegated identity (`session["jf_delegate_server_identity"]`) as first source.
2. Detect any of `X-Provider-User-Id`, `X-Jellyfin-User-Id`, `X-Emby-UserId`; classify this as unauthorized alias input (do not treat header value as user id).
3. Preserve tolerant token parsing by continuing to call `extract_media_browser_token()` through `_jellyfin_user_token_from_request()`.
4. Resolve token identity only via `resolve_user_id_from_token()`.
5. Add in-memory token-hash cache in `jellyswipe/__init__.py` with 300-second TTL and expiry checks.
</action>
<verify>
<automated>python -m py_compile jellyswipe/__init__.py jellyswipe/jellyfin_library.py</automated>
<automated>rg "_provider_user_id_from_request|_jellyfin_user_token_from_request|resolve_user_id_from_token" jellyswipe/__init__.py jellyswipe/jellyfin_library.py</automated>
</verify>
<acceptance_criteria>
- `jellyswipe/__init__.py` contains delegated identity branch before token resolution branch in `_provider_user_id_from_request()`.
- `jellyswipe/__init__.py` no longer returns values from `X-Provider-User-Id`, `X-Jellyfin-User-Id`, or `X-Emby-UserId` as identity.
- `jellyswipe/__init__.py` contains cache TTL constant/value of 300 seconds for token->user resolution.
- `jellyswipe/__init__.py` stores cache keys as token hash (not raw token values).
- `python -m py_compile jellyswipe/__init__.py jellyswipe/jellyfin_library.py` exits 0.
</acceptance_criteria>
</task>

<task id="18-01-02" type="execute">
<read_first>
- jellyswipe/__init__.py
- .planning/phases/18-verified-identity-resolution/18-CONTEXT.md
- .planning/phases/18-verified-identity-resolution/18-VALIDATION.md
</read_first>
<action>
Apply compatibility and security consistency pass:
1. Ensure spoof-header detection path leads to unauthorized classification usable by downstream routes in Phase 19.
2. Keep existing parser compatibility (`Token="..."`) intact; do not tighten to strict parser mode.
3. Add succinct inline comments explaining why alias headers are blocked and why token hash cache is short-lived.
4. Run static checks to confirm no alias-based identity return paths remain.
</action>
<verify>
<automated>rg "X-Provider-User-Id|X-Jellyfin-User-Id|X-Emby-UserId" jellyswipe/__init__.py</automated>
<automated>rg "extract_media_browser_token" jellyswipe/jellyfin_library.py</automated>
</verify>
<acceptance_criteria>
- Alias header names may exist only for rejection checks; no code path returns these header values as identity.
- `jellyswipe/jellyfin_library.py` still contains tolerant `extract_media_browser_token()` implementation.
- `jellyswipe/__init__.py` includes comments documenting security intent for alias rejection and cache TTL.
</acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> API | All request headers/body are untrusted inputs |
| API -> Jellyfin | Server-side trusted identity verification via Jellyfin API |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-01 | S | Identity alias headers | mitigate | Reject spoofable alias headers as unauthorized input |
| T-18-02 | T | Identity resolver fallback order | mitigate | Enforce delegate-first then validated-token order only |
| T-18-03 | I | Token leakage via cache keying | mitigate | Use hash-based cache keys, no raw token keys/logging |
| T-18-04 | D | Excessive token resolution calls | mitigate | Add short-lived TTL cache for token->user mappings |
</threat_model>

<verification>
After completion:
1. `python -m py_compile jellyswipe/__init__.py jellyswipe/jellyfin_library.py` passes.
2. Identity helper no longer trusts alias headers as identity values.
3. Token parsing compatibility remains unchanged.
4. Cache behavior exists with 300-second TTL and hash-based keying.
</verification>

<success_criteria>
1. SEC-01 satisfied: only delegated identity and validated token paths establish identity.
2. SEC-02 satisfied: spoofable alias headers are rejected as identity sources.
3. Resolver changes are localized and maintain compatibility with existing token header format.
4. Output is ready for Phase 19 route-level enforcement work without reworking helper semantics.
</success_criteria>

<output>
After completion, create `.planning/phases/18-verified-identity-resolution/18-01-SUMMARY.md`.
</output>
