---
phase: 33-router-extraction-and-endpoint-parity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - jellyswipe/config.py
  - jellyswipe/routers/__init__.py
  - jellyswipe/routers/auth.py
  - jellyswipe/routers/static.py
  - jellyswipe/routers/media.py
  - jellyswipe/routers/proxy.py
autonomous: true
requirements:
  - ARCH-01
  - FAPI-02

must_haves:
  truths:
    - "config.py is the single source of truth for TMDB_AUTH_HEADERS, JELLYFIN_URL, _token_user_id_cache, env var constants, and SSRF validation"
    - "routers/auth.py handles all 6 auth routes with identical URL paths, methods, and response shapes"
    - "routers/static.py handles all 4 static file routes"
    - "routers/media.py handles trailer, cast, genres, and watchlist routes"
    - "routers/proxy.py handles the image proxy route with rate limiting"
    - "All routes use Depends(require_auth), Depends(check_rate_limit), and get_provider() from dependencies.py"
  artifacts:
    - path: "jellyswipe/config.py"
      provides: "Shared module-level globals, env var constants, SSRF validation"
      contains: "TMDB_AUTH_HEADERS"
    - path: "jellyswipe/routers/__init__.py"
      provides: "Package init for routers directory"
    - path: "jellyswipe/routers/auth.py"
      provides: "6 auth routes: provider, server-identity, login, logout, me, jellyfin/server-info"
      contains: "auth_router"
    - path: "jellyswipe/routers/static.py"
      provides: "4 static routes: index, manifest, sw.js, favicon"
      contains: "static_router"
    - path: "jellyswipe/routers/media.py"
      provides: "4 media routes: trailer, cast, genres, watchlist/add"
      contains: "media_router"
    - path: "jellyswipe/routers/proxy.py"
      provides: "1 proxy route with rate limiting and regex validation"
      contains: "proxy_router"
  key_links:
    - from: "jellyswipe/routers/auth.py"
      to: "jellyswipe/dependencies.py"
      via: "from jellyswipe.dependencies import require_auth, get_provider"
      pattern: "from jellyswipe.dependencies import"
    - from: "jellyswipe/routers/auth.py"
      to: "jellyswipe/auth.py"
      via: "from jellyswipe.auth import create_session, destroy_session"
      pattern: "from jellyswipe.auth import"
    - from: "jellyswipe/routers/media.py"
      to: "jellyswipe/config.py"
      via: "from jellyswipe.config import TMDB_AUTH_HEADERS"
      pattern: "from jellyswipe.config import"
    - from: "jellyswipe/routers/proxy.py"
      to: "jellyswipe/config.py"
      via: "from jellyswipe.config import JELLYFIN_URL"
      pattern: "from jellyswipe.config import"
---

<objective>
Create config.py as the shared state module and extract four simpler domain routers (auth, static, media, proxy) from the monolith.

Purpose: Establish the config.py foundation (D-01 through D-05) and extract 4 of 5 router modules with correct DI wiring. This unblocks Plan 02 which extracts the complex rooms router and wires everything into the app factory.

Output: 6 new files (config.py, routers/__init__.py, routers/auth.py, routers/static.py, routers/media.py, routers/proxy.py)
</objective>

<execution_context>
@/Users/aherrington/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/aherrington/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-router-extraction-and-endpoint-parity/33-CONTEXT.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From jellyswipe/dependencies.py:
```python
@dataclass
class AuthUser:
    jf_token: str
    user_id: str

def require_auth(request: Request) -> AuthUser
def get_db_dep() -> Generator[sqlite3.Connection, None, None]
DBConn = Annotated[sqlite3.Connection, Depends(get_db_dep)]
def check_rate_limit(request: Request) -> None
def destroy_session_dep(request: Request) -> None
def get_provider() -> JellyfinLibraryProvider
```

From jellyswipe/auth.py:
```python
def create_session(jf_token: str, jf_user_id: str, session_dict: dict) -> str
def get_current_token(session_dict: dict) -> Optional[Tuple[str, str]]
def destroy_session(session_dict: dict) -> None
```

From jellyswipe/__init__.py (current monolith — extraction source):
```python
_RATE_LIMITS = {'get-trailer': 200, 'cast': 200, 'watchlist/add': 300, 'proxy': 200}
_JELLYFIN_URL: str = os.getenv("JELLYFIN_URL", "").rstrip("/")
TMDB_AUTH_HEADERS = {"Authorization": f"Bearer {os.getenv('TMDB_ACCESS_TOKEN')}"}
_token_user_id_cache: Dict[str, Tuple[str, float]] = {}
TOKEN_USER_ID_CACHE_TTL_SECONDS = 300
IDENTITY_ALIAS_HEADERS = ("X-Provider-User-Id", "X-Jellyfin-User-Id", "X-Emby-UserId")
CLIENT_ID = 'JellySwipe-AndrewTheTechie-2026'
_provider_singleton: Optional[JellyfinLibraryProvider] = None
```

From jellyswipe/ssrf_validator.py:
```python
def validate_jellyfin_url(url: str) -> None
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create config.py and routers/__init__.py</name>
  <files>jellyswipe/config.py, jellyswipe/routers/__init__.py</files>
  <read_first>
  - jellyswipe/__init__.py (lines 1-86 — all module-level globals, env var validation, constants)
  - jellyswipe/ssrf_validator.py (validate_jellyfin_url import)
  - jellyswipe/dependencies.py (existing pattern reference)
  </read_first>
  <action>
Per D-01: Create `jellyswipe/config.py` as the single source of truth for all shared runtime constants. Move the following from `__init__.py` module-level into `config.py`:

1. All `os.getenv()` reads for: `TMDB_ACCESS_TOKEN`, `FLASK_SECRET`, `JELLYFIN_URL`, `JELLYFIN_API_KEY`, `JELLYFIN_USERNAME`, `JELLYFIN_PASSWORD`
2. Missing env var validation block (lines 50-67 of `__init__.py`) — raise `RuntimeError` with missing list
3. `validate_jellyfin_url(os.getenv("JELLYFIN_URL"))` call per D-04
4. `_JELLYFIN_URL: str = os.getenv("JELLYFIN_URL", "").rstrip("/")` (the validated URL)
5. `TMDB_AUTH_HEADERS = {"Authorization": f"Bearer {os.getenv('TMDB_ACCESS_TOKEN')}"}`
6. `_token_user_id_cache: Dict[str, Tuple[str, float]] = {}` per D-02
7. `TOKEN_USER_ID_CACHE_TTL_SECONDS = 300`
8. `IDENTITY_ALIAS_HEADERS = ("X-Provider-User-Id", "X-Jellyfin-User-Id", "X-Emby-UserId")`
9. `CLIENT_ID = 'JellySwipe-AndrewTheTechie-2026'`
10. `_provider_singleton: Optional[JellyfinLibraryProvider] = None`

Per D-03: Config globals are initialized at import time — same pattern as current `__init__.py`. Per D-04: `validate_jellyfin_url` and `JELLYFIN_URL` both live in `config.py`. Per D-05: Routers import from `jellyswipe.config`.

Add `load_dotenv()` call at top of config.py (move from `__init__.py` — config is imported first so env vars are loaded). Import `from pathlib import Path` and call `load_dotenv(Path(__file__).resolve().parent.parent / ".env")`.

Import typing: `from typing import Dict, Optional, Tuple`. Import `JellyfinLibraryProvider` with a lazy pattern — use a string in the type hint or `TYPE_CHECKING` to avoid circular imports.

Create `jellyswipe/routers/__init__.py` as an empty file with a docstring: `"""Domain routers for Jelly Swipe FastAPI application."""`
  </action>
  <verify>
    <automated>python -c "from jellyswipe.config import TMDB_AUTH_HEADERS, JELLYFIN_URL, _token_user_id_cache, CLIENT_ID; print('config OK')" && test -f jellyswipe/routers/__init__.py</automated>
  </verify>
  <done>
    - jellyswipe/config.py exists with TMDB_AUTH_HEADERS, JELLYFIN_URL, _token_user_id_cache, CLIENT_ID, TOKEN_USER_ID_CACHE_TTL_SECONDS, IDENTITY_ALIAS_HEADERS, _provider_singleton all as module-level variables
    - jellyswipe/routers/__init__.py exists as empty package init
    - config.py contains env var validation (RuntimeError on missing)
    - config.py contains validate_jellyfin_url() call
    - config.py contains load_dotenv() call
  </done>
</task>

<task type="auto">
  <name>Task 2: Extract auth, static, media, and proxy routers</name>
  <files>jellyswipe/routers/auth.py, jellyswipe/routers/static.py, jellyswipe/routers/media.py, jellyswipe/routers/proxy.py</files>
  <read_first>
  - jellyswipe/__init__.py (FULL FILE — extraction source for all route handlers)
  - jellyswipe/dependencies.py (require_auth, DBConn, check_rate_limit, get_provider, AuthUser)
  - jellyswipe/auth.py (create_session, destroy_session, get_current_token)
  - jellyswipe/config.py (created in Task 1 — TMDB_AUTH_HEADERS, JELLYFIN_URL, _token_user_id_cache, _provider_singleton)
  - jellyswipe/http_client.py (make_http_request import)
  - jellyswipe/rate_limiter.py (rate_limiter import)
  </read_first>
  <action>
Create four router modules, each using `APIRouter()` with NO prefix per D-14. Each route handler is a verbatim copy of the corresponding handler from `__init__.py` with these mechanical substitutions:
- `_require_login(request)` → declare `user: AuthUser = Depends(require_auth)` parameter and use `user.jf_token` / `user.user_id` instead of `request.state.jf_token` / `request.state.user_id`
- `get_db_closing()` → `DBConn` dependency parameter where needed
- `_check_rate_limit('endpoint', request)` → `_: None = Depends(check_rate_limit)` parameter (the dependency auto-infers the endpoint key from path)
- `get_provider()` → `get_provider()` from `jellyswipe.dependencies` (same function, works as-is)
- `TMDB_AUTH_HEADERS` → import from `jellyswipe.config`
- `JELLYFIN_URL` → import from `jellyswipe.config`
- `_APP_ROOT` → use `os.path.dirname(os.path.abspath(__file__))` adjusted to point to jellyswipe package root, OR import a constant from config

**routers/auth.py** (per D-06, D-09, D-10):
- `APIRouter()` — no prefix
- 6 routes extracted from `__init__.py`:
  - `GET /auth/provider` (line 474-477) — no auth required, returns provider dict
  - `POST /auth/jellyfin-use-server-identity` (line 479-488) — uses `get_provider()`, calls `create_session()`
  - `POST /auth/jellyfin-login` (line 490-505) — async, reads JSON body, calls `create_session()`
  - `POST /auth/logout` (line 507-511) — uses `_require_login(request)` → `Depends(require_auth)`, calls `destroy_session(request.session)`
  - `GET /me` (line 513-531) — uses `_require_login(request)` → `Depends(require_auth)`, reads `request.session['active_room']`, queries DB
  - `GET /jellyfin/server-info` (line 533-539) — uses `get_provider().server_info()`
- Imports: `from fastapi import APIRouter, Request, Depends, HTTPException`, `from fastapi.responses import XSSSafeJSONResponse`, `from jellyswipe.dependencies import require_auth, AuthUser, get_provider, check_rate_limit`, `from jellyswipe.auth import create_session, destroy_session`, `from jellyswipe.config import TMDB_AUTH_HEADERS`, `from jellyswipe.db import get_db_closing`
- Export: `auth_router = APIRouter()`
- Each route decorated with `@auth_router.get(...)` / `@auth_router.post(...)` — same path strings as `@app.get(...)` in monolith

**routers/static.py** (per D-06):
- `APIRouter()` — no prefix
- 4 routes:
  - `GET /` (line 373-375) — `templates.TemplateResponse('index.html', ...)` — NOTE: needs `Jinja2Templates` instance. Create it in this router using `_APP_ROOT` from config or compute from `__file__`
  - `GET /manifest.json` (line 898-903) — `FileResponse`
  - `GET /sw.js` (line 905-910) — `FileResponse`
  - `GET /favicon.ico` (line 912-917) — `FileResponse`
- Import `from starlette.templating import Jinja2Templates`, `from fastapi.responses import FileResponse`
- Compute `_APP_ROOT = os.path.dirname(os.path.dirname(os.path.abspath(__file__)))` for static file paths (goes up from routers/ to jellyswipe/)
- Export: `static_router = APIRouter()`

**routers/media.py** (per D-06, D-07):
- `APIRouter()` — no prefix
- 4 routes:
  - `GET /get-trailer/{movie_id}` (line 377-414) — rate limited, uses TMDB API
  - `GET /cast/{movie_id}` (line 416-458) — rate limited, uses TMDB API
  - `GET /genres` (line 786-791) — calls `get_provider().list_genres()`
  - `POST /watchlist/add` (line 460-472) — auth required, rate limited, calls `get_provider().add_to_user_favorites()`
- Import `TMDB_AUTH_HEADERS` from `jellyswipe.config`, `make_http_request` from `jellyswipe.http_client`
- `make_error_response` and `log_exception` helpers: define as module-level functions in each router (copy the pattern from `__init__.py` lines 260-283). Each router gets its own `_logger = logging.getLogger(__name__)`.
- Export: `media_router = APIRouter()`

**routers/proxy.py** (per D-06):
- `APIRouter()` — no prefix
- 1 route:
  - `GET /proxy` (line 875-896) — rate limited, validates path regex, fetches image
- Import `JELLYFIN_URL` from `jellyswipe.config`
- Copy the regex pattern exactly: `re.match(r"^jellyfin/(?:[0-9a-fA-F]{32}|[0-9a-fA-F-]{36})/Primary$", path)`
- Import `requests` for `requests.exceptions.RequestException`
- Export: `proxy_router = APIRouter()`

For ALL routers: use `XSSSafeJSONResponse` for error responses (import from `jellyswipe` or define locally — the class is in `__init__.py` so import it from the package or from a shared location). Since `__init__.py` still exports it, routers can `from jellyswipe import XSSSafeJSONResponse`. However, to avoid circular imports, import it locally or move it to a shared module. The safest approach: keep XSSSafeJSONResponse in `__init__.py` and have routers import it with `from fastapi.responses import JSONResponse` and override render locally, OR import from `jellyswipe` (which works since routers are in a subpackage). Use `from jellyswipe import XSSSafeJSONResponse` — this works because `jellyswipe/__init__.py` exports it and routers are inside the package.

For `make_error_response` and `log_exception` helper functions: each router defines these as private module-level functions, copied from `__init__.py` lines 260-283. Each uses its own `_logger = logging.getLogger(__name__)`.

DO NOT modify `__init__.py` in this task — Plan 02 handles the app factory refactor.
  </action>
  <verify>
    <automated>python -c "
from jellyswipe.routers.auth import auth_router
from jellyswipe.routers.static import static_router
from jellyswipe.routers.media import media_router
from jellyswipe.routers.proxy import proxy_router
# Check route counts
auth_routes = [r for r in auth_router.routes]
static_routes = [r for r in static_router.routes]
media_routes = [r for r in media_router.routes]
proxy_routes = [r for r in proxy_router.routes]
assert len(auth_routes) == 6, f'auth: expected 6 routes, got {len(auth_routes)}'
assert len(static_routes) == 4, f'static: expected 4 routes, got {len(static_routes)}'
assert len(media_routes) == 4, f'media: expected 4 routes, got {len(media_routes)}'
assert len(proxy_routes) == 1, f'proxy: expected 1 route, got {len(proxy_routes)}'
print(f'Routes: auth={len(auth_routes)}, static={len(static_routes)}, media={len(media_routes)}, proxy={len(proxy_routes)}')
print('All router imports OK')
" 2>&1</automated>
  </verify>
  <done>
    - routers/auth.py has 6 routes with correct URL paths matching monolith
    - routers/static.py has 4 routes serving static files
    - routers/media.py has 4 routes (trailer, cast, genres, watchlist/add)
    - routers/proxy.py has 1 route with regex path validation
    - All routers use APIRouter() with no prefix (D-14)
    - All authenticated routes use Depends(require_auth) (D-14, ARCH-01)
    - All rate-limited routes use Depends(check_rate_limit)
    - Imports reference jellyswipe.config for shared constants (D-05)
    - __init__.py is NOT modified yet (Plan 02 handles it)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Router → config.py | Trusted internal — same process, no boundary |
| Router → dependencies.py | Trusted internal — DI layer provides auth, DB, rate limits |
| Router → external APIs (TMDB, Jellyfin) | Untrusted — HTTP responses validated at provider level |
| Client → Router endpoints | Untrusted input — auth enforced via Depends(require_auth) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-01 | S | routers/proxy.py path param | mitigate | Regex validation preserves existing pattern: `^jellyfin/(?:[0-9a-fA-F]{32}|[0-9a-fA-F-]{36})/Primary$` |
| T-33-02 | T | All router responses | mitigate | XSSSafeJSONResponse used for all JSON responses — XSS defense from v1.5 preserved |
| T-33-03 | I | routers/auth.py sessions | accept | Session handling unchanged from Phase 31/32 — existing SessionMiddleware handles this |
| T-33-04 | D | config.py env vars | accept | Env vars validated at import time — same pattern as current monolith |
</threat_model>

<verification>
1. `python -c "from jellyswipe.config import TMDB_AUTH_HEADERS, JELLYFIN_URL"` — config imports cleanly
2. `python -c "from jellyswipe.routers.auth import auth_router"` — auth router imports cleanly
3. `python -c "from jellyswipe.routers.static import static_router"` — static router imports cleanly
4. `python -c "from jellyswipe.routers.media import media_router"` — media router imports cleanly
5. `python -c "from jellyswipe.routers.proxy import proxy_router"` — proxy router imports cleanly
6. Route count verification (see Task 2 verify)
</verification>

<success_criteria>
- config.py is the single source of truth for TMDB_AUTH_HEADERS, JELLYFIN_URL, _token_user_id_cache, env var constants
- 4 router modules exist with correct route counts: auth(6), static(4), media(4), proxy(1)
- All routers use APIRouter() with no prefix
- All authenticated routes use Depends(require_auth) from dependencies.py
- __init__.py is unmodified (Plan 02 handles it)
</success_criteria>

<output>
After completion, create `.planning/phases/33-router-extraction-and-endpoint-parity/33-01-SUMMARY.md`
</output>
