---
phase: 33-router-extraction-and-endpoint-parity
plan: 02
type: execute
wave: 2
depends_on: ["33-01"]
files_modified:
  - jellyswipe/routers/rooms.py
  - jellyswipe/__init__.py
autonomous: true
requirements:
  - ARCH-01
  - FAPI-02

must_haves:
  truths:
    - "routers/rooms.py handles all 10+ room routes including the swipe handler with BEGIN IMMEDIATE transaction"
    - "The swipe handler's BEGIN IMMEDIATE transaction logic is verbatim preserved (D-12)"
    - "The swipe handler uses DBConn dependency instead of direct get_db_closing() (D-13, fixes CR-01)"
    - "__init__.py is a thin app factory that imports and mounts all 5 routers (D-15)"
    - "Every original URL path responds with its original HTTP method and status code"
    - "SSE route /room/{code}/stream stays inline in __init__.py"
    - "Dead /plex/server-info route is deleted (D-10)"
  artifacts:
    - path: "jellyswipe/routers/rooms.py"
      provides: "10+ room routes including swipe with transaction integrity"
      contains: "rooms_router"
    - path: "jellyswipe/__init__.py"
      provides: "Thin app factory mounting all 5 routers + SSE route inline"
      contains: "app.include_router"
  key_links:
    - from: "jellyswipe/__init__.py"
      to: "jellyswipe/routers/auth.py"
      via: "from jellyswipe.routers.auth import auth_router; app.include_router(auth_router)"
      pattern: "include_router\\(auth_router\\)"
    - from: "jellyswipe/__init__.py"
      to: "jellyswipe/routers/rooms.py"
      via: "from jellyswipe.routers.rooms import rooms_router; app.include_router(rooms_router)"
      pattern: "include_router\\(rooms_router\\)"
    - from: "jellyswipe/routers/rooms.py"
      to: "jellyswipe/dependencies.py"
      via: "from jellyswipe.dependencies import require_auth, AuthUser, DBConn, check_rate_limit, get_provider"
      pattern: "from jellyswipe.dependencies import"
    - from: "jellyswipe/routers/rooms.py"
      to: "jellyswipe/config.py"
      via: "from jellyswipe.config import JELLYFIN_URL, _token_user_id_cache"
      pattern: "from jellyswipe.config import"
---

<objective>
Extract the rooms router (most complex — swipe transaction logic) and refactor __init__.py into a thin app factory mounting all 5 routers.

Purpose: Complete the router extraction by handling the critical rooms domain (10+ routes including the swipe handler's BEGIN IMMEDIATE transaction) and wiring all routers into the app factory. The SSE route stays inline per D-15. Dead /plex/server-info route is deleted per D-10.

Output: routers/rooms.py (new) + refactored __init__.py (thin factory)
</objective>

<execution_context>
@/Users/aherrington/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/aherrington/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-router-extraction-and-endpoint-parity/33-CONTEXT.md
@.planning/phases/33-router-extraction-and-endpoint-parity/33-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 output — config.py and router contracts -->

From jellyswipe/config.py (created in Plan 01):
```python
JELLYFIN_URL: str  # validated URL
TMDB_AUTH_HEADERS: dict  # TMDB auth
_token_user_id_cache: Dict[str, Tuple[str, float]]
TOKEN_USER_ID_CACHE_TTL_SECONDS: int = 300
IDENTITY_ALIAS_HEADERS: tuple
CLIENT_ID: str
_provider_singleton: Optional[JellyfinLibraryProvider]
```

From jellyswipe/dependencies.py:
```python
class AuthUser:
    jf_token: str
    user_id: str
def require_auth(request: Request) -> AuthUser
DBConn = Annotated[sqlite3.Connection, Depends(get_db_dep)]
def check_rate_limit(request: Request) -> None
def get_provider() -> JellyfinLibraryProvider
```

From jellyswipe/routers/auth.py (Plan 01): `auth_router: APIRouter`
From jellyswipe/routers/static.py (Plan 01): `static_router: APIRouter`
From jellyswipe/routers/media.py (Plan 01): `media_router: APIRouter`
From jellyswipe/routers/proxy.py (Plan 01): `proxy_router: APIRouter`

From jellyswipe/__init__.py — SSE route (stays inline, lines 803-873):
```python
@app.get('/room/{code}/stream')
def room_stream(code: str, request: Request):
    _require_login(request)
    def generate(): ...  # SSE generator with time.sleep()
    return StreamingResponse(generate(), ...)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create rooms router with swipe transaction integrity</name>
  <files>jellyswipe/routers/rooms.py</files>
  <read_first>
  - jellyswipe/__init__.py (FULL FILE — extraction source for all room routes)
  - jellyswipe/dependencies.py (require_auth, AuthUser, DBConn, check_rate_limit, get_provider)
  - jellyswipe/config.py (JELLYFIN_URL, _token_user_id_cache — created in Plan 01)
  - jellyswipe/auth.py (get_current_token pattern reference)
  </read_first>
  <action>
Create `jellyswipe/routers/rooms.py` with `APIRouter()` (no prefix per D-14).

Per D-06, extract the following routes from `__init__.py`:
- `POST /room` (lines 541-560) — create room, uses `_require_login` → `Depends(require_auth)`
- `POST /room/solo` (lines 562-585) — create solo room
- `POST /room/{code}/join` (lines 587-602) — join room
- `POST /room/{code}/swipe` (lines 604-686) — CRITICAL: swipe handler with BEGIN IMMEDIATE
- `GET /matches` (lines 688-699) — get matches per D-08
- `POST /room/{code}/quit` (lines 701-710) — quit room
- `POST /matches/delete` (lines 712-725) — delete match per D-08
- `POST /room/{code}/undo` (lines 727-741) — undo swipe
- `GET /room/{code}/deck` (lines 751-768) — get deck page
- `POST /room/{code}/genre` (lines 770-784) — set genre
- `GET /room/{code}/status` (lines 793-801) — room status

Per D-11: Move `_get_cursor()`, `_set_cursor()`, and `_resolve_movie_meta()` (lines 130-164) inline as private module-level functions in this file. They are room-domain helpers only.

Per D-12: The swipe handler body is a **verbatim copy**. Add the comment:
```python
# CRITICAL: BEGIN IMMEDIATE transaction — verbatim from Phase 31 __init__.py. Do not refactor.
```

Per D-13: The swipe handler switches from `get_db_closing()` to `DBConn` dependency. Change the swipe route signature to:
```python
@rooms_router.post('/room/{code}/swipe')
async def swipe(code: str, request: Request, user: AuthUser = Depends(require_auth), conn: DBConn = Depends(get_db_dep)):
```
Replace `with get_db_closing() as conn:` block with direct use of `conn` parameter. The `BEGIN IMMEDIATE` transaction works on the same injected connection. Replace all `request.state.user_id` with `user.user_id` and `request.state.jf_token` with `user.jf_token` in this handler.

For ALL other room routes that use `_require_login(request)`:
- Replace with `user: AuthUser = Depends(require_auth)` parameter
- Replace `request.state.user_id` → `user.user_id`
- Replace `request.state.jf_token` → `user.jf_token`

For routes using `get_db_closing()` (non-swipe): keep using `get_db_closing()` for now since they don't have the leak concern — only the swipe handler gets the `DBConn` dependency fix per D-13.

For the swipe handler's `JELLYFIN_URL` reference (line 642): import from `jellyswipe.config`.

For `_token_user_id_cache` references in the monolith's identity helpers: these stay in `__init__.py` for the SSE route (Phase 34 migrates them). Rooms router doesn't need them.

Define `make_error_response`, `log_exception`, and `_logger` as module-level helpers (same pattern as Plan 01 routers).

Import `from jellyswipe.config import JELLYFIN_URL`. Import `from jellyswipe.db import get_db_closing` for non-swipe DB operations. Import `from jellyswipe.dependencies import require_auth, AuthUser, DBConn, get_db_dep, check_rate_limit, get_provider`.

Export: `rooms_router = APIRouter()`
  </action>
  <verify>
    <automated>python -c "
from jellyswipe.routers.rooms import rooms_router
routes = [r for r in rooms_router.routes]
print(f'rooms routes: {len(routes)}')
# Check critical: swipe route exists and uses conn parameter
for r in rooms_router.routes:
    if hasattr(r, 'path') and '/swipe' in r.path:
        print(f'swipe route found: {r.methods} {r.path}')
        import inspect
        sig = inspect.signature(r.endpoint)
        params = list(sig.parameters.keys())
        print(f'swipe params: {params}')
        assert 'conn' in params, 'swipe handler must have conn parameter (DBConn dependency)'
assert len(routes) >= 10, f'expected >= 10 routes, got {len(routes)}'
print('rooms router OK')
"</automated>
  </verify>
  <done>
    - routers/rooms.py has >= 10 routes with correct URL paths
    - Swipe handler uses `conn: DBConn` dependency parameter (D-13)
    - Swipe handler contains `BEGIN IMMEDIATE` transaction comment (D-12)
    - _get_cursor(), _set_cursor(), _resolve_movie_meta() are module-level functions (D-11)
    - All authenticated routes use `user: AuthUser = Depends(require_auth)` (ARCH-01)
    - `from jellyswipe.config import JELLYFIN_URL` used for deep link construction
  </done>
</task>

<task type="auto">
  <name>Task 2: Refactor __init__.py into thin app factory</name>
  <files>jellyswipe/__init__.py</files>
  <read_first>
  - jellyswipe/__init__.py (FULL FILE — what remains after extraction)
  - jellyswipe/config.py (created in Plan 01 — shared state destination)
  - jellyswipe/dependencies.py (DI layer)
  - jellyswipe/routers/auth.py (auth_router export)
  - jellyswipe/routers/rooms.py (rooms_router export, created in Task 1)
  - jellyswipe/routers/static.py (static_router export)
  - jellyswipe/routers/media.py (media_router export)
  - jellyswipe/routers/proxy.py (proxy_router export)
  </read_first>
  <action>
Refactor `jellyswipe/__init__.py` to be a thin app factory per D-15. This is a major rewrite of the file:

**KEEP in __init__.py:**
1. The `create_app()` function — modified to be thin
2. `XSSSafeJSONResponse` class — still defined here, routers import it
3. `RequestIdMiddleware` class — middleware stays in the factory
4. `generate_request_id()` function — used by middleware
5. SSE route `@app.get('/room/{code}/stream')` — stays inline per D-15, Phase 34 migrates it
6. `app = create_app()` at bottom
7. `lifespan()` context manager
8. Static files mount: `app.mount('/static', StaticFiles(...))`
9. Templates import (needed by SSE route — actually SSE doesn't use templates, but static router does)

**REMOVE from __init__.py (moved to config.py or routers):**
1. All module-level globals that moved to `config.py`: TMDB_AUTH_HEADERS, _JELLYFIN_URL, _token_user_id_cache, TOKEN_USER_ID_CACHE_TTL_SECONDS, IDENTITY_ALIAS_HEADERS, CLIENT_ID, _provider_singleton, missing env var validation, validate_jellyfin_url call
2. ALL route handlers except SSE (`/room/{code}/stream`) — moved to routers
3. `_require_login()` bridge function — replaced by Depends(require_auth) in routers
4. `_check_rate_limit()` function — replaced by Depends(check_rate_limit) in routers
5. `make_error_response()` and `log_exception()` — moved to each router
6. `_get_cursor()`, `_set_cursor()`, `_resolve_movie_meta()` — moved to rooms router
7. All identity helper functions (`_jellyfin_user_token_from_request`, etc.) — these are used by the SSE route, so KEEP them for now (Phase 34 migrates SSE and these helpers)
8. Dead `/plex/server-info` route — DELETED per D-10

**ADD to __init__.py:**
1. Import all 5 routers:
```python
from jellyswipe.routers.auth import auth_router
from jellyswipe.routers.rooms import rooms_router
from jellyswipe.routers.media import media_router
from jellyswipe.routers.proxy import proxy_router
from jellyswipe.routers.static import static_router
```
2. In `create_app()`, mount all routers:
```python
app.include_router(auth_router)
app.include_router(rooms_router)
app.include_router(media_router)
app.include_router(proxy_router)
app.include_router(static_router)
```
Per D-14: routers use no prefix; routes define full paths; prefix is set in exactly one location (`app.include_router()` with default empty prefix).

**Import config globals that __init__.py still needs:**
- `from jellyswipe.config import _provider_singleton, _JELLYFIN_URL as JELLYFIN_URL, _token_user_id_cache, TOKEN_USER_ID_CACHE_TTL_SECONDS, IDENTITY_ALIAS_HEADERS`
- `DB_PATH` — move to config.py or keep in __init__.py (planner discretion per D-15 and "Agent's Discretion: whether _APP_ROOT stays in __init__.py")
- `load_dotenv()` — moved to config.py, remove from __init__.py

**SSE route helpers kept inline:**
The SSE route still uses `_require_login(request)` — keep this bridge function for now (Phase 34 replaces it). The SSE route's identity helpers (`_jellyfin_user_token_from_request`, etc.) also stay.

**Final __init__.py structure (~200-250 lines):**
1. Imports (FastAPI, middleware, etc.)
2. Import config globals and routers
3. `XSSSafeJSONResponse` class
4. `RequestIdMiddleware` class
5. `generate_request_id()` function
6. `_require_login()` bridge (for SSE only)
7. SSE identity helpers (for SSE only)
8. `lifespan()` context manager
9. `create_app()` — thin factory with middleware, router mounting, SSE route inline, static mount
10. `app = create_app()`

IMPORTANT: The SSE route's `_require_login(request)` call stays unchanged — it's the Phase 31 bridge that Phase 34 replaces. Don't convert it to Depends(require_auth).

The `get_provider()` function inside `create_app()` (lines 235-240) — keep it for the SSE route. Routers use `get_provider()` from dependencies.py. The SSE route calls `get_provider()` from the closure.

After refactoring, `__init__.py` should be ~200-250 lines (down from 924) containing only: app factory, middleware classes, SSE route, and config imports.
  </action>
  <verify>
    <automated>python -c "
import jellyswipe
app = jellyswipe.app
# Check all routes are registered
routes = []
for route in app.routes:
    if hasattr(route, 'path') and hasattr(route, 'methods'):
        routes.append((route.path, route.methods))
    elif hasattr(route, 'path'):
        routes.append((route.path, set()))
# Check critical routes exist
paths = [r[0] for r in routes]
assert '/auth/provider' in paths, f'missing /auth/provider. Got: {paths}'
assert '/room/{code}/swipe' in paths, f'missing swipe'
assert '/room/{code}/stream' in paths, f'missing SSE stream'
assert '/get-trailer/{movie_id}' in paths, f'missing trailer'
assert '/proxy' in paths, f'missing proxy'
assert '/' in paths, f'missing index'
assert '/matches' in paths, f'missing matches'
print(f'Total routes registered: {len(routes)}')
# Check /plex/server-info is GONE
assert '/plex/server-info' not in paths, 'dead /plex/server-info route should be deleted'
print('All routes verified, dead plex route deleted')
" 2>&1</automated>
  </verify>
  <done>
    - __init__.py is a thin app factory (~200-250 lines) with 5 router includes and SSE route inline
    - All 21 non-SSE routes are in their domain routers
    - SSE route /room/{code}/stream stays in __init__.py
    - Dead /plex/server-info route is deleted
    - `app.include_router()` called for all 5 routers with no prefix
    - `from jellyswipe.config import ...` used for shared globals
    - `uvicorn jellyswipe:app` starts without errors
    - Every original URL path still resolves correctly
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| __init__.py → config.py | Trusted internal — import-time initialization |
| __init__.py → routers/* | Trusted internal — same application wiring |
| SSE route → DB | Direct sqlite3 connection (Phase 34 migrates to async) |
| Swipe handler → DB | DBConn dependency ensures connection cleanup (CR-01 fix) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-05 | I | Swipe handler transaction | mitigate | BEGIN IMMEDIATE preserved verbatim — prevents race conditions in match detection |
| T-33-06 | D | SSE route in __init__.py | accept | SSE stays as-is — Phase 34 handles async migration and connection cleanup |
| T-33-07 | E | Router mounting order | accept | No side-channel risk — all routers mount at root, no prefix conflicts possible |
</threat_model>

<verification>
1. `python -c "import jellyswipe; print(len(jellyswipe.app.routes))"` — app has routes registered
2. `python -c "from jellyswipe.routers.rooms import rooms_router; print(len(rooms_router.routes))"` — rooms router has correct count
3. Route presence check (see Task 2 verify) — all original paths resolve
4. Dead route check — `/plex/server-info` not in paths
5. SSE route check — `/room/{code}/stream` still in paths
6. Swipe handler uses DBConn dependency (see Task 1 verify)
</verification>

<success_criteria>
- routers/rooms.py has >= 10 routes including swipe with BEGIN IMMEDIATE transaction
- Swipe handler uses `conn: DBConn` dependency (D-13, CR-01 fix)
- __init__.py is ~200-250 lines — thin app factory with 5 router includes + SSE inline
- Dead /plex/server-info route deleted (D-10)
- Every original URL path resolves (FAPI-02)
- SSE route stays in __init__.py (D-15)
- `uvicorn jellyswipe:app` starts without errors
</success_criteria>

<output>
After completion, create `.planning/phases/33-router-extraction-and-endpoint-parity/33-02-SUMMARY.md`
</output>
