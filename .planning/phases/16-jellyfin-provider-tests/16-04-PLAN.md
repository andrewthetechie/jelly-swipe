---
phase: 16-jellyfin-provider-tests
plan: 04
type: execute
wave: 2
depends_on: []
files_modified: [tests/test_jellyfin_library.py]
autonomous: true
requirements: [API-01]
user_setup: []
must_haves:
  truths:
    - "Tests mock HTTP requests to prevent real Jellyfin API calls"
    - "Tests verify network failures raise RuntimeError with descriptive messages"
    - "Tests verify empty Items array returns empty deck"
    - "Tests verify missing required fields raise RuntimeError"
    - "Tests verify 403 on image fetch raises PermissionError"
    - "Tests verify 404 on image fetch raises FileNotFoundError"
  artifacts:
    - path: "tests/test_jellyfin_library.py"
      provides: "Error and edge case tests"
      min_lines: 100
  key_links:
    - from: "tests/test_jellyfin_library.py"
      to: "jellyswipe/jellyfin_library.py"
      via: "import and method calls"
      pattern: "provider.fetch_deck|provider.fetch_library_image|provider.authenticate_user_session"
    - from: "test_fetch_deck_with_empty_items"
      to: "requests.Session"
      via: "mock"
      pattern: "mocker.patch.*requests.Session"
---

<objective>
Create error and edge case tests for JellyfinLibraryProvider that verify network failures, empty responses, missing fields, and HTTP error codes are handled correctly—all with mocked HTTP calls.

Purpose: Verify API-01 (mock HTTP requests) and error handling per D-04 (representative error cases) are correctly implemented.

Output: tests/test_jellyfin_library.py with 5-6 error and edge case tests.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/16-jellyfin-provider-tests/16-CONTEXT.md
@.planning/codebase/TESTING.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONVENTIONS.md
@.planning/research/ARCHITECTURE.md
@.planning/phases/14-test-infrastructure-setup/14-CONTEXT.md
@.planning/phases/15-database-module-tests/15-CONTEXT.md
jellyswipe/jellyfin_library.py

<interfaces>
<!-- Key methods from jellyfin_library.py for error and edge case tests -->

From jellyswipe/jellyfin_library.py:
```python
def fetch_deck(self, genre_name: Optional[str] = None) -> List[dict]
def fetch_library_image(self, path: str) -> Tuple[bytes, str]
def authenticate_user_session(self, username: str, password: str) -> dict
def _api(self, method: str, path: str, *, params: Optional[dict] = None, json_body: Any = None, retry: bool = True) -> dict
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create error and edge case tests for deck and image fetching</name>
  <files>tests/test_jellyfin_library.py</files>
  <action>Add the following tests to tests/test_jellyfin_library.py:

1. test_fetch_deck_with_empty_items (per D-04):
   - Mock env vars with API key
   - Create provider and set _access_token, _cached_user_id="user-123", _cached_library_id="lib-123"
   - Mock Session.request to return 200 with {"Items": []}
   - Call provider.fetch_deck()
   - Verify deck is empty list (not None)
   - Verify no exception raised (empty deck is valid)

2. test_fetch_deck_with_missing_item_fields (per D-04):
   - Mock env vars with API key
   - Create provider and set auth and caches
   - Mock Session.request to return 200 with {"Items": [{"Id": "movie-1"}]} (missing Name, Overview, etc.)
   - Call provider.fetch_deck()
   - Verify deck length is 1
   - Verify card uses empty strings for missing string fields (Name="", Overview="")
   - Verify card uses None for missing numeric fields (rating=None, year=None)

3. test_fetch_library_image_403_forbidden (per D-04):
   - Mock env vars with API key
   - Create provider and set _access_token
   - Mock Session.request to return 403 Forbidden
   - Call provider.fetch_library_image("jellyfin/movie-123/Primary")
   - Verify PermissionError is raised with message "Jellyfin image forbidden"

4. test_fetch_library_image_404_not_found (per D-04):
   - Mock env vars with API key
   - Create provider and set _access_token
   - Mock Session.request to return 404 Not Found
   - Call provider.fetch_library_image("jellyfin/movie-123/Primary")
   - Verify FileNotFoundError is raised with message "Jellyfin image not found"

5. test_fetch_library_image_invalid_path (per D-04):
   - Mock env vars with API key
   - Create provider
   - Call provider.fetch_library_image("invalid/path")
   - Verify PermissionError is raised with message "Invalid Jellyfin image path" (per line 365)

6. test_authenticate_user_session_missing_credentials (per D-04):
   - Mock env vars with API key
   - Create provider
   - Call provider.authenticate_user_session("", "") (empty username and password)
   - Verify RuntimeError is raised with message "Jellyfin login failed (missing username/password)"

7. test_authenticate_user_session_missing_token_or_user_id (per D-04):
   - Mock env vars with API key
   - Create provider
   - Mock Session.request to return 200 with {"AccessToken": "token-123"} (missing User.Id)
   - Call provider.authenticate_user_session("user", "pass")
   - Verify RuntimeError is raised with message "Jellyfin login failed (missing token or user id)"

8. test_api_non_json_response (per D-04):
   - Mock env vars with API key
   - Create provider and set _access_token
   - Mock Session.request to return 200 OK with invalid JSON (raise ValueError on .json())
   - Call provider._api("GET", "/Items")
   - Verify RuntimeError is raised with message "Jellyfin returned non-JSON body"

Use inline mock responses (per D-02). Group tests with "# ---- Error & Edge Case Tests" comment separator (per D-05).
</action>
  <verify>uv run pytest tests/test_jellyfin_library.py -k "empty or 403 or 404 or invalid or missing" -v</verify>
  <done>8 error and edge case tests pass: empty items, missing fields, 403, 404, invalid path, missing credentials, missing token/user_id, non-JSON response</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test → JellyfinLibraryProvider | Untrusted test data enters provider methods |
| Provider → requests.Session | Provider makes outbound HTTP calls (mocked in tests) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-17 | I (Information disclosure) | None | accept | Error tests use mock data, no PII exposure. |
| T-16-18 | T (Tampering) | mocker.patch usage | accept | Tests run in isolated environment; mocking is standard pytest practice. |
| T-16-19 | D (Denial of service) | None | accept | Error tests have no retry loops or long operations. |
| T-16-20 | R (Repudiation) | None | N/A | No authentication/authorization in test code. N/A. |
| T-16-21 | E (Elevation of privilege) | None | N/A | Tests have no privilege escalation mechanism. N/A. |
</threat_model>

<verification>
Phase-level verification:
1. pytest discovers all 8 error and edge case tests
2. All tests pass with no external HTTP calls
3. Mock assertions verify correct HTTP status codes (403, 404)
4. Error messages are descriptive and match implementation
5. Empty Items array returns empty deck (not error)
6. Missing fields use defaults (empty string, None) instead of raising
7. Invalid image path raises PermissionError before HTTP call
8. Non-JSON response raises RuntimeError
9. Missing credentials (empty strings) raise RuntimeError
10. Missing token or user_id in auth response raise RuntimeError
</verification>

<success_criteria>
1. tests/test_jellyfin_library.py has 8 error and edge case tests added (24 total)
2. All tests pass: `uv run pytest tests/test_jellyfin_library.py -k "empty or 403 or 404 or invalid or missing" -v`
3. No real HTTP calls made
4. Empty Items array returns empty deck without errors
5. Missing item fields use defaults (empty string, None)
6. 403 on image fetch raises PermissionError
7. 404 on image fetch raises FileNotFoundError
8. Invalid image path raises PermissionError before HTTP call
9. Missing credentials (empty strings) raise RuntimeError
10. Missing token or user_id in auth response raise RuntimeError
11. Non-JSON response raises RuntimeError
</success_criteria>

<output>
After completion, create `.planning/phases/16-jellyfin-provider-tests/16-04-SUMMARY.md`
</output>
