---
phase: 16-jellyfin-provider-tests
plan: 03
type: execute
wave: 2
depends_on: [16-02]
files_modified: [tests/test_jellyfin_library.py]
autonomous: true
requirements: [API-01, API-03, API-04]
user_setup: []
must_haves:
  truths:
    - "Tests mock HTTP requests to prevent real Jellyfin API calls"
    - "Tests verify deck fetching calls correct API with correct params"
    - "Tests verify item-to-card transformation extracts all required fields"
    - "Tests verify TMDB resolution returns correct title and year"
    - "Tests verify 'Recently Added' sort uses DateCreated descending"
    - "Tests verify genre filter uses correct API parameter"
  artifacts:
    - path: "tests/test_jellyfin_library.py"
      provides: "Deck fetching and transformation tests"
      min_lines: 120
  key_links:
    - from: "tests/test_jellyfin_library.py"
      to: "jellyswipe/jellyfin_library.py"
      via: "import and method calls"
      pattern: "provider.fetch_deck|provider.resolve_item_for_tmdb"
    - from: "test_fetch_deck_with_genre_filter"
      to: "requests.Session"
      via: "mock"
      pattern: "mocker.patch.*requests.Session"
---

<objective>
Create deck fetching and transformation tests for JellyfinLibraryProvider that verify deck retrieval, item-to-card transformation, TMDB resolution, and genre filtering—all with mocked HTTP calls.

Purpose: Verify API-03 (deck fetching, genre filtering) and API-04 (item-to-card transformation, TMDB resolution) are correctly implemented.

Output: tests/test_jellyfin_library.py with 5-6 deck and transformation tests.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/16-jellyfin-provider-tests/16-CONTEXT.md
@.planning/codebase/TESTING.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONVENTIONS.md
@.planning/research/ARCHITECTURE.md
@.planning/phases/14-test-infrastructure-setup/14-CONTEXT.md
@.planning/phases/15-database-module-tests/15-CONTEXT.md
@.planning/phases/16-jellyfin-provider-tests/16-01-PLAN.md
@.planning/phases/16-jellyfin-provider-tests/16-02-PLAN.md
jellyswipe/jellyfin_library.py

<interfaces>
<!-- Key methods from jellyfin_library.py for deck and transformation tests -->

From jellyswipe/jellyfin_library.py:
```python
def fetch_deck(self, genre_name: Optional[str] = None) -> List[dict]
def _item_to_card(self, it: dict) -> dict
def resolve_item_for_tmdb(self, movie_id: str) -> Any
```

Card output format (from line 262-270):
```python
{
    "id": str (movie ID),
    "title": str (movie name),
    "summary": str (overview),
    "thumb": str (poster path, e.g., "/proxy?path=jellyfin/{mid}/Primary"),
    "rating": Optional[float] (community or critic rating),
    "duration": str (formatted runtime, e.g., "2h 15m"),
    "year": Optional[int] (production year)
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create deck fetching and item-to-card transformation tests</name>
  <files>tests/test_jellyfin_library.py</files>
  <action>Add the following tests to tests/test_jellyfin_library.py:

1. test_fetch_deck_all_movies (per D-03, D-01):
   - Mock env vars with API key
   - Create provider and set _access_token, _cached_user_id="user-123", _cached_library_id="lib-123"
   - Mock Session.request to return 200 with {"Items": [{"Id": "movie-1", "Name": "Movie 1", "Overview": "Summary 1", "RunTimeTicks": 5400000000, "ProductionYear": 2024, "CommunityRating": 8.5}]}
   - Call provider.fetch_deck()
   - Verify GET /Items was called with params: ParentId=lib-123, UserId=user-123, IncludeItemTypes=Movie, Recursive=true, SortBy=Random, Limit=150
   - Verify deck length is 1
   - Verify card has correct id, title, summary, rating, duration ("1h 30m"), year
   - Verify thumb path format: "/proxy?path=jellyfin/movie-1/Primary"

2. test_fetch_deck_with_genre_filter (per D-03):
   - Mock env vars with API key
   - Create provider and set auth and caches
   - Mock Session.request to return 200 with {"Items": [{"Id": "movie-2", "Name": "Sci-Fi Movie", "Overview": "", "RunTimeTicks": 0, "ProductionYear": 2023}]}
   - Call provider.fetch_deck("Sci-Fi")
   - Verify GET /Items was called with Genres="Science Fiction" (mapped back from "Sci-Fi" per line 284)
   - Verify params include Limit=100 and SortBy=Random

3. test_fetch_deck_recently_added_sort (per D-03):
   - Mock env vars with API key
   - Create provider and set auth and caches
   - Mock Session.request to return 200 with {"Items": []}
   - Call provider.fetch_deck("Recently Added")
   - Verify GET /Items was called with SortBy="DateCreated", SortOrder="Descending", Limit=100
   - Verify items are not shuffled (do_shuffle=False per line 290)

4. test_item_to_card_transformation (per API-04):
   - Mock env vars with API key
   - Create provider
   - Create test item dict with all fields: Id, Name, Overview, RunTimeTicks, ProductionYear, CommunityRating, CriticRating
   - Call provider._item_to_card(item)
   - Verify all 7 card fields are extracted correctly
   - Verify CommunityRating is used, falls back to CriticRating if CommunityRating is None
   - Verify duration is formatted correctly (2h 15m for 8100000000 ticks, 45m for 2700000000 ticks)
   - Verify empty runtime returns "" (per line 30-31)

5. test_resolve_item_for_tmdb_success (per API-04):
   - Mock env vars with API key
   - Create provider and set _access_token, _cached_user_id="user-123"
   - Mock Session.request to return 200 with {"Id": "movie-123", "Name": "Test Movie", "OriginalTitle": "Original Name", "ProductionYear": 2024}
   - Call provider.resolve_item_for_tmdb("movie-123")
   - Verify GET /Items/movie-123 was called with Fields=Name,OriginalTitle,ProductionYear
   - Verify returned SimpleNamespace has title="Test Movie" (Name takes precedence) and year=2024

6. test_resolve_item_for_tmdb_fallback_to_user_endpoint (per API-04):
   - Mock env vars with API key
   - Create provider and set _access_token, _cached_user_id="user-123"
   - First Session.request to /Items/movie-123 raises RuntimeError (server rejects global lookup)
   - Second Session.request to /Users/user-123/Items/movie-123 returns 200 with valid data
   - Call provider.resolve_item_for_tmdb("movie-123")
   - Verify both endpoints were attempted (fallback per line 335-339)
   - Verify title and year are extracted from successful response

Group tests with "# ---- Deck Fetching Tests" and "# ---- Transformation Tests" comment separators (per D-05).
</action>
  <verify>uv run pytest tests/test_jellyfin_library.py -k "deck or tmdb or transform" -v</verify>
  <done>6 deck and transformation tests pass: all movies deck, genre filter deck, recently added deck, item-to-card transformation, TMDB resolution, TMDB fallback</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test → JellyfinLibraryProvider | Untrusted test data enters provider methods |
| Provider → requests.Session | Provider makes outbound HTTP calls (mocked in tests) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-12 | I (Information disclosure) | test_fetch_deck_all_movies | accept | Test uses mock movie data, not real library content. No PII exposure. |
| T-16-13 | T (Tampering) | mocker.patch usage | accept | Tests run in isolated environment; mocking is standard pytest practice. |
| T-16-14 | D (Denial of service) | None | accept | Deck fetching tests have no retry loops or long operations. |
| T-16-15 | R (Repudiation) | None | N/A | No authentication/authorization in test code. N/A. |
| T-16-16 | E (Elevation of privilege) | None | N/A | Tests have no privilege escalation mechanism. N/A. |
</threat_model>

<verification>
Phase-level verification:
1. pytest discovers all 6 deck and transformation tests
2. All tests pass with no external HTTP calls
3. Mock assertions verify correct API paths (/Items, /Items/{id}, /Users/{uid}/Items/{id})
4. Deck fetching verifies correct params for all genres, "Recently Added", and "All"
5. Item-to-card transformation verifies all 7 fields extracted correctly
6. TMDB resolution verifies fallback from /Items to /Users/{uid}/Items
7. Runtime formatting verified (hours+minutes, minutes-only, empty)
8. Thumb path format verified ("/proxy?path=jellyfin/{id}/Primary")
</verification>

<success_criteria>
1. tests/test_jellyfin_library.py has 6 deck and transformation tests added (16 total)
2. All tests pass: `uv run pytest tests/test_jellyfin_library.py -k "deck or tmdb or transform" -v`
3. No real HTTP calls made
4. Mock assertions verify correct API endpoints, params, and query strings
5. Genre filter maps "Sci-Fi" → "Science Fiction" in API call
6. "Recently Added" uses DateCreated descending sort
7. Item-to-card extracts all 7 fields with correct types
8. TMDB resolution falls back to user-scoped endpoint when global lookup fails
9. Runtime formatting works for all cases (hours+minutes, minutes-only, empty)
</success_criteria>

<output>
After completion, create `.planning/phases/16-jellyfin-provider-tests/16-03-SUMMARY.md`
</output>
