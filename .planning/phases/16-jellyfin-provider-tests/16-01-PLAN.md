---
phase: 16-jellyfin-provider-tests
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [tests/test_jellyfin_library.py]
autonomous: true
requirements: [API-01, API-02]
user_setup: []
must_haves:
  truths:
    - "Tests mock HTTP requests to prevent real Jellyfin API calls"
    - "Tests verify API key authentication sets token correctly"
    - "Tests verify username/password authentication makes correct API call and extracts token"
    - "Tests verify 401 response triggers reset and retry logic"
    - "Tests verify token caching prevents redundant auth calls"
    - "Tests verify user ID resolution falls back from /Users/Me to /Users"
  artifacts:
    - path: "tests/test_jellyfin_library.py"
      provides: "Authentication and user ID resolution tests"
      min_lines: 150
    - path: "jellyswipe/jellyfin_library.py"
      provides: "Module under test (no changes)"
  key_links:
    - from: "tests/test_jellyfin_library.py"
      to: "jellyswipe/jellyfin_library.py"
      via: "import and method calls"
      pattern: "from jellyswipe.jellyfin_library import JellyfinLibraryProvider"
    - from: "test_auth_with_api_key"
      to: "requests.Session"
      via: "mock"
      pattern: "mocker.patch.*requests.Session"
---

<objective>
Create authentication tests for JellyfinLibraryProvider that verify API key auth, username/password auth, 401 retry logic, token caching, and user ID resolution—all with mocked HTTP calls.

Purpose: Verify API-02 (authentication, token caching, user ID resolution) is correctly implemented, using D-01's mocking strategy to prevent real API calls.

Output: tests/test_jellyfin_library.py with 5-7 authentication tests covering success and error paths.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/16-jellyfin-provider-tests/16-CONTEXT.md
@.planning/codebase/TESTING.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONVENTIONS.md
@.planning/research/ARCHITECTURE.md
@.planning/phases/14-test-infrastructure-setup/14-CONTEXT.md
@.planning/phases/15-database-module-tests/15-CONTEXT.md
jellyswipe/jellyfin_library.py

<interfaces>
<!-- Key types and contracts from jellyfin_library.py for test setup -->

From jellyswipe/jellyfin_library.py (JellyfinLibraryProvider class):
```python
class JellyfinLibraryProvider(LibraryMediaProvider):
    def __init__(self, base_url: str) -> None
    def reset(self) -> None
    def ensure_authenticated(self) -> None
    def _login_from_env(self) -> None
    def _verify_items(self) -> None
    def _api(self, method: str, path: str, *, params: Optional[dict] = None, json_body: Any = None, retry: bool = True) -> dict
    def _user_id(self) -> str
```

Key authentication state attributes (for cache verification):
```python
self._access_token: Optional[str] = None
self._cached_user_id: Optional[str] = None
self._cached_library_id: Optional[str] = None
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create API key and username/password authentication tests</name>
  <files>tests/test_jellyfin_library.py</files>
  <action>Create tests/test_jellyfin_library.py with the following tests:

1. test_auth_with_api_key (per D-03, D-01):
   - Use mocker.patch('jellyswipe.jellyfin_library.requests.Session')
   - Mock env vars: JELLYFIN_API_KEY="test-api-key", JELLYFIN_URL="http://test.local"
   - Create JellyfinLibraryProvider instance
   - Call ensure_authenticated()
   - Verify _access_token is set to env var value (no HTTP call made)
   - Verify Session.request was NOT called (bypasses _login_from_env)

2. test_auth_with_username_password_success (per D-03, D-01):
   - Mock env vars: JELLYFIN_USERNAME="testuser", JELLYFIN_PASSWORD="testpass", JELLYFIN_URL="http://test.local"
   - Mock Session.request to return successful auth response with AccessToken
   - Create provider and call ensure_authenticated()
   - Verify POST /Users/AuthenticateByName was called with correct headers and JSON body
   - Verify _access_token is extracted from response

3. test_auth_with_username_password_network_error (per D-04):
   - Mock env vars with username/password
   - Mock Session.request to raise requests.RequestException
   - Create provider and call ensure_authenticated()
   - Verify RuntimeError is raised with message "Jellyfin authentication failed (network error)"

4. test_auth_with_invalid_credentials (per D-04):
   - Mock env vars with username/password
   - Mock Session.request to return 401 response
   - Create provider and call ensure_authenticated()
   - Verify RuntimeError is raised with message about invalid credentials

5. test_auth_with_missing_token_in_response (per D-04):
   - Mock env vars with username/password
   - Mock Session.request to return 200 OK with JSON lacking AccessToken
   - Create provider and call ensure_authenticated()
   - Verify RuntimeError is raised about missing access token

Use inline mock responses (per D-02) — don't create fixtures in conftest.py yet.
</action>
  <verify>uv run pytest tests/test_jellyfin_library.py::test_auth_with_api_key tests/test_jellyfin_library.py::test_auth_with_username_password_success -v</verify>
  <done>5 authentication tests pass: API key path, username/password success, network error, invalid credentials, missing token</done>
</task>

<task type="auto">
  <name>Task 2: Create 401 retry, token caching, and user ID resolution tests</name>
  <files>tests/test_jellyfin_library.py</files>
  <action>Add the following tests to tests/test_jellyfin_library.py:

1. test_401_triggers_reset_and_retry (per D-03, D-04):
   - Mock env vars with API key
   - Create provider and set _access_token to "old-token"
   - First Session.request call returns 401
   - Second Session.request call (after reset/reauth) returns 200 OK
   - Call provider._api("GET", "/Items")
   - Verify reset() was called (caches cleared, new Session created)
   - Verify ensure_authenticated() was called to get new token
   - Verify retry happens with retry=False flag (prevents infinite loop)

2. test_token_caching_prevents_redundant_auth (per D-03):
   - Mock env vars with API key
   - Create provider
   - Call ensure_authenticated() twice
   - Verify Session.request was NOT called on second call (uses cached token)
   - Verify _access_token remains set from first call

3. test_user_id_from_users_me_endpoint (per D-03):
   - Mock env vars with API key
   - Create provider and set _access_token
   - Mock Session.request to return 200 with {"Id": "user-123"}
   - Call provider._user_id()
   - Verify GET /Users/Me was called with auth headers
   - Verify _cached_user_id is set to "user-123"
   - Second call to _user_id() returns cached value without HTTP call

4. test_user_id_fallback_to_users_list (per D-03):
   - Mock env vars with API key and JELLYFIN_USERNAME="testuser"
   - Create provider and set _access_token
   - First Session.request to /Users/Me returns 400 (some servers reject API keys)
   - Second Session.request to /Users returns user list
   - Verify user matching JELLYFIN_USERNAME is selected
   - Verify _cached_user_id is set correctly

5. test_user_id_fallback_to_first_user (per D-04):
   - Mock env vars with API key (no JELLYFIN_USERNAME)
   - Create provider and set _access_token
   - Mock /Users/Me to return 400
   - Mock /Users to return user list with no name match
   - Verify first user in list is selected (per line 191 in jellyfin_library.py)
   - Verify _cached_user_id is set to first user's Id

Group tests with "# ---- Authentication Tests" and "# ---- User ID Resolution Tests" comment separators for readability (per D-05).
</action>
  <verify>uv run pytest tests/test_jellyfin_library.py -k "401 or caching or user_id" -v</verify>
  <done>5 additional tests pass: 401 retry logic, token caching, user ID from /Users/Me, user ID fallback to /Users, user ID fallback to first user</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test → JellyfinLibraryProvider | Untrusted test data enters provider methods |
| Provider → requests.Session | Provider makes outbound HTTP calls (mocked in tests) |
| Provider → Environment variables | Provider reads JELLYFIN_* env vars (controlled via monkeypatch) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-01 | I (Information disclosure) | test_auth_with_api_key | accept | Test uses hardcoded "test-api-key", not real credentials. No PII exposure. |
| T-16-02 | I (Information disclosure) | test_auth_with_username_password_success | accept | Test uses "testuser"/"testpass", not real credentials. No PII exposure. |
| T-16-03 | T (Tampering) | mocker.patch usage | accept | Tests run in isolated environment; mocking is standard pytest practice. No production impact. |
| T-16-04 | D (Denial of service) | 401 retry logic | accept | Retry limited to one attempt (retry=False flag). No infinite loop. |
| T-16-05 | R (Repudiation) | None | N/A | No authentication/authorization in test code. N/A. |
| T-16-06 | E (Elevation of privilege) | None | N/A | Tests have no privilege escalation mechanism. N/A. |
</threat_model>

<verification>
Phase-level verification:
1. pytest discovers all 10 authentication and user ID tests
2. All tests pass with no external HTTP calls (verified by mocker usage)
3. Mock assertions verify correct API endpoints and parameters
4. Token caching behavior verified (no redundant auth calls)
5. 401 retry logic verified (reset + re-authenticate + retry once)
</verification>

<success_criteria>
1. tests/test_jellyfin_library.py exists with 10 tests (5 auth + 5 user ID)
2. All tests pass: `uv run pytest tests/test_jellyfin_library.py -k "auth or user_id" -v`
3. No real HTTP calls made (all requests.Session calls are mocked)
4. Mock assertions verify correct API paths (/Users/AuthenticateByName, /Users/Me, /Users)
5. Token caching verified (cached user_id prevents redundant API calls)
6. 401 retry logic verified (reset called, new token fetched, retry once)
</success_criteria>

<output>
After completion, create `.planning/phases/16-jellyfin-provider-tests/16-01-SUMMARY.md`
</output>
