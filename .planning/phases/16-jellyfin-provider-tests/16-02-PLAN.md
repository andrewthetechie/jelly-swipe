---
phase: 16-jellyfin-provider-tests
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [tests/test_jellyfin_library.py]
autonomous: true
requirements: [API-01, API-03]
user_setup: []
must_haves:
  truths:
    - "Tests mock HTTP requests to prevent real Jellyfin API calls"
    - "Tests verify library ID resolution finds movies collection type"
    - "Tests verify genre listing calls correct API endpoints"
    - "Tests verify 'Science Fiction' → 'Sci-Fi' mapping works"
    - "Tests verify genre cache prevents redundant API calls"
    - "Tests verify fallback to /Genres when /Items/Filters fails"
  artifacts:
    - path: "tests/test_jellyfin_library.py"
      provides: "Library discovery and genre listing tests"
      min_lines: 100
  key_links:
    - from: "tests/test_jellyfin_library.py"
      to: "jellyswipe/jellyfin_library.py"
      via: "import and method calls"
      pattern: "provider._movies_library_id|provider.list_genres"
    - from: "test_movies_library_id_finds_movies_collection"
      to: "requests.Session"
      via: "mock"
      pattern: "mocker.patch.*requests.Session"
---

<objective>
Create library discovery tests for JellyfinLibraryProvider that verify movies library resolution, genre listing, genre mapping, and genre caching—all with mocked HTTP calls.

Purpose: Verify API-03 (library discovery, genre listing) is correctly implemented, using D-01's mocking strategy to prevent real API calls.

Output: tests/test_jellyfin_library.py with 4-5 library discovery tests.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/16-jellyfin-provider-tests/16-CONTEXT.md
@.planning/codebase/TESTING.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONVENTIONS.md
@.planning/research/ARCHITECTURE.md
@.planning/phases/14-test-infrastructure-setup/14-CONTEXT.md
@.planning/phases/15-database-module-tests/15-CONTEXT.md
jellyswipe/jellyfin_library.py

<interfaces>
<!-- Key methods from jellyfin_library.py for library discovery tests -->

From jellyswipe/jellyfin_library.py:
```python
def _movies_library_id(self) -> str
def list_genres(self) -> List[str]
```

Key internal state (for cache verification):
```python
self._cached_library_id: Optional[str] = None
self._genre_cache: Optional[List[str]] = None
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create library ID resolution and genre listing tests</name>
  <files>tests/test_jellyfin_library.py</files>
  <action>Add the following tests to tests/test_jellyfin_library.py:

1. test_movies_library_id_finds_movies_collection (per D-03):
   - Mock env vars with API key
   - Create provider and set _access_token, _cached_user_id="user-123"
   - Mock Session.request to return 200 with {"Items": [{"Id": "lib-123", "CollectionType": "movies", "Name": "Movies"}]}
   - Call provider._movies_library_id()
   - Verify GET /Users/user-123/Views was called with auth headers
   - Verify _cached_library_id is set to "lib-123"
   - Verify only items with CollectionType="movies" are matched

2. test_movies_library_id_raises_when_no_movies_collection (per D-04):
   - Mock env vars with API key
   - Create provider and set _access_token, _cached_user_id="user-123"
   - Mock Session.request to return 200 with {"Items": [{"Id": "lib-tv", "CollectionType": "tvshows"}]}
   - Call provider._movies_library_id()
   - Verify RuntimeError is raised with message about "no library with CollectionType=movies"

3. test_list_genres_from_items_filters (per D-03, D-01):
   - Mock env vars with API key
   - Create provider and set _access_token, _cached_user_id="user-123", _cached_library_id="lib-123"
   - Mock Session.request to return 200 with {"GenreFilters": [{"Name": "Action"}, {"Name": "Science Fiction"}]}
   - Call provider.list_genres()
   - Verify GET /Items/Filters was called with correct params (ParentId, UserId, IncludeItemTypes)
   - Verify genre list includes "Action" and "Sci-Fi" (mapped from "Science Fiction" per line 251)
   - Verify _genre_cache is set

4. test_genre_cache_prevents_redundant_api_calls (per D-03):
   - Mock env vars with API key
   - Create provider and set _access_token, _cached_user_id="user-123", _cached_library_id="lib-123"
   - Set _genre_cache to ["Action", "Sci-Fi"] (simulate previous call)
   - Call provider.list_genres()
   - Verify Session.request was NOT called (uses cache)
   - Verify cached genre list is returned

5. test_list_genres_fallback_to_genres_endpoint (per D-03):
   - Mock env vars with API key
   - Create provider and set _access_token, _cached_user_id="user-123", _cached_library_id="lib-123"
   - First Session.request to /Items/Filters returns empty GenreFilters (or missing key)
   - Second Session.request to /Genres returns {"Items": [{"Name": "Comedy"}]}
   - Call provider.list_genres()
   - Verify both endpoints were attempted (fallback per line 237-248)
   - Verify "Comedy" is in returned genre list

Use inline mock responses (per D-02) — create fixtures in conftest.py only if duplication emerges.
</action>
  <verify>uv run pytest tests/test_jellyfin_library.py -k "library or genre" -v</verify>
  <done>5 library discovery tests pass: library ID found, library ID not found error, genres from /Items/Filters, genre cache, genres fallback to /Genres</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test → JellyfinLibraryProvider | Untrusted test data enters provider methods |
| Provider → requests.Session | Provider makes outbound HTTP calls (mocked in tests) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-07 | I (Information disclosure) | test_list_genres_from_items_filters | accept | Test uses mock genre names, not real library data. No PII exposure. |
| T-16-08 | T (Tampering) | mocker.patch usage | accept | Tests run in isolated environment; mocking is standard pytest practice. |
| T-16-09 | D (Denial of service) | None | accept | Library discovery tests have no retry loops or long operations. |
| T-16-10 | R (Repudiation) | None | N/A | No authentication/authorization in test code. N/A. |
| T-16-11 | E (Elevation of privilege) | None | N/A | Tests have no privilege escalation mechanism. N/A. |
</threat_model>

<verification>
Phase-level verification:
1. pytest discovers all 5 library discovery tests
2. All tests pass with no external HTTP calls
3. Mock assertions verify correct API paths (/Users/{uid}/Views, /Items/Filters, /Genres)
4. Genre mapping verified ("Science Fiction" → "Sci-Fi")
5. Cache behavior verified (genre cache prevents redundant API calls)
6. Fallback logic verified (/Genres attempted when /Items/Filters fails)
</verification>

<success_criteria>
1. tests/test_jellyfin_library.py has 5 library discovery tests added (10 total with Plan 01)
2. All tests pass: `uv run pytest tests/test_jellyfin_library.py -k "library or genre" -v`
3. No real HTTP calls made
4. Mock assertions verify correct API endpoints and parameters
5. Genre "Science Fiction" is mapped to "Sci-Fi" in output
6. Genre cache prevents redundant API calls on second invocation
7. Fallback to /Genres works when /Items/Filters returns empty
</success_criteria>

<output>
After completion, create `.planning/phases/16-jellyfin-provider-tests/16-02-SUMMARY.md`
</output>
