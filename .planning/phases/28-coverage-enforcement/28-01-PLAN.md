---
phase: 28-coverage-enforcement
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
autonomous: true
requirements:
  - COV-01

must_haves:
  truths:
    - "pytest exits with code 1 when total jellyswipe coverage drops below 70%"
    - "Running uv run pytest tests/ locally shows coverage report and passes with current 75% coverage"
    - "CI workflow (test.yml) fails automatically if coverage drops below threshold"
  artifacts:
    - path: "pyproject.toml"
      provides: "pytest coverage threshold enforcement"
      contains: "--cov-fail-under=70"
  key_links:
    - from: "pyproject.toml"
      to: "pytest-cov"
      via: "addopts in [tool.pytest.ini_options]"
      pattern: "--cov-fail-under=70"
---

<objective>
Add `--cov-fail-under=70` to pytest configuration so CI enforces a 70% coverage threshold for the jellyswipe package.

Purpose: Prevent coverage regression — if future changes drop total coverage below 70%, pytest exits with code 1 and CI fails (COV-01).
Output: Modified pyproject.toml with enforced threshold.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-coverage-enforcement/28-CONTEXT.md

<interfaces>
<!-- Key interfaces the executor needs. Extracted from codebase. -->

From pyproject.toml (current state):
```toml
[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
addopts = "-v --tb=short --cov=jellyswipe --cov-report=term-missing"
```

From .github/workflows/test.yml (CI reads pyproject.toml automatically):
```yaml
- name: Run tests
  run: uv run pytest tests/
```

Current coverage baseline:
```
jellyswipe/__init__.py:          78%
jellyswipe/base.py:             100%
jellyswipe/db.py:                87%
jellyswipe/jellyfin_library.py:  11% (mocked in tests)
TOTAL:                           75%
Tests: 159 passing
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add --cov-fail-under=70 to pyproject.toml pytest config</name>
  <files>pyproject.toml</files>
  <read_first>
    - pyproject.toml (current addopts line in [tool.pytest.ini_options])
  </read_first>
  <action>
    Per D-03 and D-04: Append `--cov-fail-under=70` to the existing `addopts` line in the `[tool.pytest.ini_options]` section of pyproject.toml.

    Change this line:
    ```
    addopts = "-v --tb=short --cov=jellyswipe --cov-report=term-missing"
    ```

    To:
    ```
    addopts = "-v --tb=short --cov=jellyswipe --cov-report=term-missing --cov-fail-under=70"
    ```

    This is the ONLY change. Do not modify any other section of pyproject.toml.

    Per D-05: No separate `[tool.coverage]` section needed. Per D-06: No changes to `.github/workflows/test.yml` — it runs `uv run pytest tests/` which picks up pyproject.toml config automatically.
  </action>
  <verify>
    <automated>cd /Users/andrew/Documents/code/kino-swipe/.codenomad/worktrees/EPIC-09 && grep -q "\-\-cov-fail-under=70" pyproject.toml && echo "PASS: --cov-fail-under=70 found in pyproject.toml" || echo "FAIL: --cov-fail-under=70 not found"</automated>
  </verify>
  <acceptance_criteria>
    - pyproject.toml contains `--cov-fail-under=70` in the addopts line under `[tool.pytest.ini_options]`
    - No other sections of pyproject.toml are modified
    - Existing addopts flags are preserved: `-v --tb=short --cov=jellyswipe --cov-report=term-missing`
  </acceptance_criteria>
  <done>pyproject.toml has `--cov-fail-under=70` appended to addopts; test suite still passes with 159 tests.</done>
</task>

<task type="auto">
  <name>Task 2: Verify test suite passes with coverage threshold</name>
  <files>pyproject.toml</files>
  <read_first>
    - pyproject.toml (verify the change from Task 1)
  </read_first>
  <action>
    Run the full test suite to confirm the threshold is enforced and current coverage (75%) exceeds 70%.

    ```bash
    uv run pytest tests/ -v
    ```

    This will:
    1. Run all 159 tests (must pass)
    2. Generate coverage report via `--cov=jellyswipe --cov-report=term-missing`
    3. Check `--cov-fail-under=70` — exit code 0 means current coverage >= 70%, exit code 1 means it's below

    Verify the output shows:
    - "TOTAL ... 75%" (or higher) in the coverage table
    - "159 passed" in the test summary
    - Exit code 0 (success)

    If the threshold fails (exit code 1), DO NOT lower the threshold. Instead, report the actual coverage percentage — this would indicate a regression that needs investigation.
  </action>
  <verify>
    <automated>cd /Users/andrew/Documents/code/kino-swipe/.codenomad/worktrees/EPIC-09 && uv run pytest tests/ -v 2>&1 | tail -5 && echo "EXIT_CODE=$?"</automated>
  </verify>
  <acceptance_criteria>
    - uv run pytest tests/ exits with code 0
    - Coverage report shows TOTAL >= 70%
    - All 159 tests pass
    - Coverage summary includes "jellyswipe/__init__.py" with coverage >= 70%
  </acceptance_criteria>
  <done>Test suite passes with 159 tests, coverage report shows >= 70% total, pytest exits 0 — threshold enforcement is working correctly.</done>
</task>

</tasks>

<verification>
1. `grep -q "\-\-cov-fail-under=70" pyproject.toml` — flag present in config
2. `uv run pytest tests/` — exits 0 with all tests passing
3. Coverage report shows TOTAL >= 70%
4. No changes to .github/workflows/test.yml (CI picks up pyproject.toml automatically)
</verification>

<success_criteria>
- pyproject.toml `[tool.pytest.ini_options]` addopts includes `--cov-fail-under=70`
- All 159 tests pass
- Total coverage is >= 70% (currently 75%)
- CI workflow will fail (exit code 1) if coverage drops below threshold
</success_criteria>

<output>
After completion, create `.planning/phases/28-coverage-enforcement/28-01-SUMMARY.md`
</output>
