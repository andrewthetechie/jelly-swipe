---
phase: 13
plan: 02
type: execute
wave: 2
depends_on: [13-01]
files_modified:
  - pyproject.toml
  - uv.lock
  - jellyswipe/db.py
  - README.md
  - docker-compose.yml
  - .planning/PROJECT.md
autonomous: true
requirements: []
user_setup: []
must_haves:
  truths:
    - "pyproject.toml does not contain plexapi dependency"
    - "uv.lock is regenerated without plexapi"
    - "jellyswipe/db.py no longer references plex_id columns"
    - "README.md contains only Jellyfin configuration examples"
    - "docker-compose.yml contains only Jellyfin environment variables"
    - "PROJECT.md core value reflects Jellyfin-only support"
  artifacts:
    - path: "pyproject.toml"
      provides: "Dependencies without plexapi"
      min_lines: 20
    - path: "uv.lock"
      provides: "Lockfile regenerated without Plex dependencies"
      min_lines: 100
    - path: "jellyswipe/db.py"
      provides: "Database schema without Plex columns"
      min_lines: 30
    - path: "README.md"
      provides: "Jellyfin-only documentation"
      min_lines: 150
    - path: "docker-compose.yml"
      provides: "Jellyfin-only compose configuration"
      min_lines: 15
    - path: ".planning/PROJECT.md"
      provides: "Updated project context"
      min_lines: 100
  key_links:
    - from: "pyproject.toml"
      to: "uv.lock"
      via: "uv lock regeneration"
      pattern: "plexapi removal triggers lockfile update"
    - from: "README.md"
      to: "docker-compose.yml"
      via: "Configuration examples"
      pattern: "Jellyfin env vars documented in both"
---

<objective>
Remove Plex dependencies, update database schema, and update all documentation to Jellyfin-only.

Purpose: Complete the Plex removal by cleaning up dependencies, database schema references, and all user-facing documentation per user decisions D-07 through D-15.

Output: plexapi removed from dependencies, database schema updated to remove Plex columns, all documentation reflects Jellyfin-only support.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-remove-plex-support/13-CONTEXT.md
@.planning/phases/13-remove-plex-support/13-RESEARCH.md
@pyproject.toml
@jellyswipe/db.py
@README.md
@docker-compose.yml
@.planning/PROJECT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove plexapi from pyproject.toml and regenerate lockfile</name>
  <files>pyproject.toml, uv.lock</files>
  <action>Remove plexapi from pyproject.toml dependencies (line 13: "plexapi>=4.18.1"). Then regenerate uv.lock by running `uv lock --upgrade` to remove all Plex-related transitive dependencies and lock the current Jellyfin-only dependency set.

Do NOT change any other dependencies in pyproject.toml - only remove plexapi.</action>
  <verify>
    <automated>! grep -q "plexapi" pyproject.toml && ! grep -qi "plex" uv.lock && uv lock --check 2>&1 | head -1</automated>
  </verify>
  <done>plexapi removed from pyproject.toml, uv.lock regenerated without Plex dependencies</done>
</task>

<task type="auto">
  <name>Task 2: Update database schema to remove plex_id columns</name>
  <files>jellyswipe/db.py</files>
  <action>Update jellyswipe/db.py to remove Plex-specific database schema:

1. In swipes table CREATE TABLE (line 23), remove "plex_id TEXT" column and replace with "user_id TEXT" for clarity
2. In matches table CREATE TABLE (lines 24-28), remove "plex_id TEXT" column and replace with "user_id TEXT"
3. Update UNIQUE constraint from "(room_code, movie_id, plex_id)" to "(room_code, movie_id, user_id)" (line 27)
4. In migration logic (lines 34-40), remove ADD COLUMN plex_id statements and replace with user_id ADD COLUMN logic:
   - Check if 'user_id' not in matches columns, add it
   - Check if 'user_id' not in swipes columns, add it

The plex_id column was legacy naming that stored either Plex user IDs or Jellyfin user IDs. After Plex removal, rename it to user_id for clarity. Existing databases will have their columns renamed via the migration logic on next startup.

Note: This migration assumes operators can accept that existing Plex user data in matches/swipes tables will become inaccessible after column rename. This is acceptable per phase context (D-05: Plex user data loss is acceptable).</action>
  <verify>
    <automated>! grep -q "plex_id" jellyswipe/db.py && grep -q "user_id TEXT" jellyswipe/db.py</automated>
  </verify>
  <done>Database schema updated to use user_id instead of plex_id, migration logic in place</done>
</task>

<task type="auto">
  <name>Task 3: Update README.md to Jellyfin-only documentation</name>
  <files>README.md</files>
  <action>Update README.md to remove all Plex references and update to Jellyfin-only:

1. Line 1: Change title from "Jelly-Swipe" to "Jelly Swipe" (already correct)
2. Line 5: Change fork description to remove "to add Jellyfin support" since it's now Jellyfin-only
3. Line 35: Change "Plex Integration:" to "Jellyfin Integration:"
4. Lines 46-53: Replace entire "Media backend (Plex or Jellyfin)" section with simple "Media backend: Jellyfin" section stating the app is Jellyfin-only
5. Lines 55-58: Remove "Upgrade note" section about Plex.tv
6. Lines 62-73: Update Environment variables table:
   - Remove MEDIA_PROVIDER row
   - Remove PLEX_URL row
   - Remove PLEX_TOKEN row
   - Keep all Jellyfin variables (JELLYFIN_URL, JELLYFIN_API_KEY, etc.)
7. Lines 75-92: Update "Jellyfin user identity contract" and "Jellyfin operator checks" to remove "legacy plex_id DB column" references (now it's user_id)
8. Lines 96-104: Remove "Plex mode" .env example entirely, keep only Jellyfin example
9. Line 121: Change "Media backend: Plex or Jellyfin" to "Media backend: Jellyfin"
10. Lines 157: Update TMDB application summary to mention Jellyfin library instead of Plex
11. Lines 171-186: Update Docker compose examples to remove Plex env vars, show only Jellyfin configuration
12. Lines 189-201: Update Docker run examples to remove Plex env vars, show only Jellyfin configuration

Keep all other sections unchanged (screenshots, features, TMDB instructions, development section with uv, etc.).</action>
  <verify>
    <automated>! grep -i "plex" README.md && grep -q "Jellyfin" README.md</automated>
  </verify>
  <done>README.md contains only Jellyfin references, no Plex mentions</done>
</task>

<task type="auto">
  <name>Task 4: Update docker-compose.yml to Jellyfin-only</name>
  <files>docker-compose.yml</files>
  <action>Update docker-compose.yml to remove all Plex environment variables:

1. Remove line 8: "# MEDIA_PROVIDER=plex   # default; set to jellyfin for Jellyfin-only env (see README)"
2. Remove line 9: "- PLEX_URL=https://your-plex-ip:32400"
3. Remove line 10: "- PLEX_TOKEN=your-plex-token-here"
4. Uncomment and update Jellyfin variables (lines 14-17) to be active:
   - Change from commented to active format
   - Update placeholder values to be more descriptive
5. Add a comment at top of environment section: "# Jellyfin-only configuration"

The resulting environment section should look like:
```yaml
    environment:
      # Jellyfin-only configuration
      - JELLYFIN_URL=http://your-jellyfin-host:8096
      - JELLYFIN_API_KEY=your-jellyfin-api-key
      - FLASK_SECRET=pick-a-random-string
      - TMDB_API_KEY=your-tmdb-v3-key
```

Alternatively, you can use username/password auth instead of API key - either is valid. Document both options.</action>
  <verify>
    <automated>! grep -i "plex\|media_provider" docker-compose.yml && grep -q "JELLYFIN" docker-compose.yml</automated>
  </verify>
  <done>docker-compose.yml contains only Jellyfin environment variables</done>
</task>

<task type="auto">
  <name>Task 5: Update PROJECT.md to reflect Jellyfin-only support</name>
  <files>.planning/PROJECT.md</files>
  <action>Update .planning/PROJECT.md to reflect Jellyfin-only architecture:

1. Line 5: Update description to remove "first-class Jellyfin alternative alongside Plex" and state it's now Jellyfin-only
2. Line 11: Update Core Value from "Users can run a swipe session backed by either Plex or Jellyfin" to "Users can run a swipe session backed by Jellyfin"
3. Line 28-31: Remove validated requirements mentioning Plex (Plex-backed sessions, Plex metadata, Plex end-user flows) - these are no longer applicable
4. Line 35: Remove "Either/or configuration" requirement (validating Plex/Jellyfin choice)
5. Line 68: Update Current state to remove "media_provider with PlexLibraryProvider and JellyfinLibraryProvider" and state it's now JellyfinLibraryProvider only
6. Line 75: Update Context to remove Plex description, keep only Jellyfin
7. Line 86-92: Update Key Decisions table to remove "Either-or media provider per instance" and "Two instances for Plex + Jellyfin together" decisions (no longer relevant)

Keep all v1.2 requirements, jellyfin-specific requirements, and other sections intact.</action>
  <verify>
    <automated>! grep -i "either.*plex\|plex.*jellyfin\|two instances" .planning/PROJECT.md | grep -v "phase 13"</automated>
  </verify>
  <done>PROJECT.md reflects Jellyfin-only support, no dual-provider references</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator→app | Environment variables and configuration |
| app→database | SQLite database schema and migrations |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-05 | S | pyproject.toml | accept | Removing dependency reduces attack surface |
| T-13-06 | I | Database migration | mitigate | Migration adds user_id columns with proper checks; existing Plex data becomes inaccessible (acceptable per phase context) |
| T-13-07 | T | Documentation | accept | No security impact - documentation only |
| T-13-08 | S | Environment variables | mitigate | Remove Plex env vars from validation, keep only Jellyfin (existing validation in __init__.py) |
</threat_model>

<verification>
After completing this plan, verify:
- pyproject.toml has no plexapi dependency
- uv.lock passes validation: `uv lock --check`
- jellyswipe/db.py has no plex_id references
- README.md has no Plex references
- docker-compose.yml has no Plex env vars
- PROJECT.md reflects Jellyfin-only support
</verification>

<success_criteria>
1. plexapi is not listed in pyproject.toml dependencies
2. uv.lock is regenerated and passes validation without Plex dependencies
3. jellyswipe/db.py uses user_id instead of plex_id throughout
4. README.md contains zero Plex references, only Jellyfin configuration
5. docker-compose.yml contains only Jellyfin environment variables
6. PROJECT.md core value and context reflect Jellyfin-only support
7. All documentation is consistent with Jellyfin-only architecture
</success_criteria>

<output>
After completion, create `.planning/phases/13-remove-plex-support/13-02-SUMMARY.md`
</output>
