---
phase: 13
plan: 03
type: execute
wave: 3
depends_on: [13-01, 13-02]
files_modified:
  - jellyswipe/__init__.py
  - jellyswipe/jellyfin_library.py
  - pyproject.toml
autonomous: true
requirements: []
user_setup: []
must_haves:
  truths:
    - "Application imports successfully without Plex dependencies"
    - "Application starts with only Jellyfin environment variables"
    - "No Plex imports are attempted at runtime"
    - "All Jellyfin routes are accessible and functional"
    - "Docker image builds successfully without Plex dependencies"
  artifacts:
    - path: "jellyswipe/__init__.py"
      provides: "Working Jellyfin-only Flask app"
      min_lines: 200
    - path: "jellyswipe/jellyfin_library.py"
      provides: "Functional Jellyfin provider"
      min_lines: 400
    - path: "pyproject.toml"
      provides: "Valid dependencies without Plex"
      min_lines: 20
  key_links:
    - from: "jellyswipe/__init__.py"
      to: "jellyswipe/jellyfin_library.py"
      via: "Direct instantiation"
      pattern: "JellyfinLibraryProvider initialization with JELLYFIN_URL"
    - from: "pyproject.toml"
      to: "uv.lock"
      via: "Dependency resolution"
      pattern: "Jellyfin-only dependency tree"
---

<objective>
Verify the application works correctly with only Jellyfin configuration and perform final cleanup.

Purpose: Ensure the Plex removal is complete and the application is fully functional as a Jellyfin-only system, meeting all success criteria from the phase goal per user decision D-18.

Output: Verified working Jellyfin-only application with no Plex references, ready for deployment.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-remove-plex-support/13-CONTEXT.md
@.planning/phases/13-remove-plex-support/13-RESEARCH.md
@.planning/phases/13-remove-plex-support/13-01-SUMMARY.md
@.planning/phases/13-remove-plex-support/13-02-SUMMARY.md
@jellyswipe/__init__.py
@jellyswipe/jellyfin_library.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify application imports successfully without Plex</name>
  <files>jellyswipe/__init__.py, jellyswipe/jellyfin_library.py, jellyswipe/base.py</files>
  <action>Verify the application can be imported without any Plex-related errors. Run Python to import the jellyswipe package and check for any import errors or missing dependencies. This confirms that all Plex imports have been removed and the code is syntactically correct.

Use: `python -c "import jellyswipe; print('Import successful')"`

Also verify that no Plex imports are attempted by checking if any files still import from plexapi or reference Plex classes.</action>
  <verify>
    <automated>python -c "import jellyswipe; print('Import successful')" 2>&1 && ! grep -r "from plexapi\|import plexapi\|PlexLibraryProvider" jellyswipe/*.py</automated>
  </verify>
  <done>Application imports successfully, no Plex imports found in code</done>
</task>

<task type="auto">
  <name>Task 2: Verify application starts with Jellyfin-only configuration</name>
  <files>jellyswipe/__init__.py</files>
  <action>Verify the Flask application starts successfully with only Jellyfin environment variables set. Create a minimal test that:

1. Sets only Jellyfin-required environment variables (JELLYFIN_URL, JELLYFIN_API_KEY or JELLYFIN_USERNAME/JELLYFIN_PASSWORD, FLASK_SECRET, TMDB_API_KEY)
2. Attempts to create/initialize the Flask app
3. Verifies no Plex-related errors occur

Use a temporary .env file or inline env vars for this test. The application should validate only Jellyfin configuration and not attempt to access Plex environment variables.

This confirms that D-10 and D-18 are satisfied: app assumes Jellyfin-only and validates only Jellyfin configuration.</action>
  <verify>
    <automated>
      JELLYFIN_URL=http://test:8096 JELLYFIN_API_KEY=test FLASK_SECRET=test TMDB_API_KEY=test python -c "
import os
os.environ['JELLYFIN_URL'] = 'http://test:8096'
os.environ['JELLYFIN_API_KEY'] = 'test'
os.environ['FLASK_SECRET'] = 'test'
os.environ['TMDB_API_KEY'] = 'test'
import jellyswipe
print('App initialized successfully')
" 2>&1 | grep -q "App initialized successfully"
    </automated>
  </verify>
  <done>Application starts and initializes with only Jellyfin environment variables</done>
</task>

<task type="auto">
  <name>Task 3: Verify no Plex environment variables are referenced</name>
  <files>jellyswipe/__init__.py, jellyswipe/jellyfin_library.py, jellyswipe/db.py</files>
  <action>Perform a comprehensive search across all Python files in jellyswipe/ to ensure no Plex environment variables are referenced anywhere in the code. Check for:
- PLEX_URL
- PLEX_TOKEN
- PLEX_CLIENT_IDENTIFIER
- MEDIA_PROVIDER

Use grep to search for these patterns. If any are found, remove them.

This confirms D-03, D-09, and D-10 are satisfied: Plex env vars are completely removed.</action>
  <verify>
    <automated>! grep -r "PLEX_URL\|PLEX_TOKEN\|PLEX_CLIENT_IDENTIFIER\|MEDIA_PROVIDER" jellyswipe/*.py</automated>
  </verify>
  <done>No Plex environment variables referenced in any Python code</done>
</task>

<task type="auto">
  <name>Task 4: Verify Docker image builds successfully</name>
  <files>Dockerfile, pyproject.toml, uv.lock</files>
  <action>Verify the Docker image builds successfully without Plex dependencies. Run a Docker build to ensure:
1. pyproject.toml and uv.lock are valid
2. uv sync completes without errors
3. No Plex dependencies are pulled in
4. The final image is created successfully

Use: `docker build -t jellyswipe-test .`

This confirms that plexapi has been successfully removed from the dependency tree and the Docker build process works with the new Jellyfin-only configuration.</action>
  <verify>
    <automated>docker build -t jellyswipe-test . 2>&1 | tail -5 | grep -q "Successfully tagged jellyswipe-test"</automated>
  </verify>
  <done>Docker image builds successfully without Plex dependencies</done>
</task>

<task type="auto">
  <name>Task 5: Final code review and cleanup</name>
  <files>jellyswipe/__init__.py</files>
  <action>Perform a final review of jellyswipe/__init__.py to ensure:
1. All Plex-specific routes are removed (/auth/plex-url, /auth/check-returned-pin)
2. All Plex-specific logic is removed (watchlist Plex handling, proxy Plex handling)
3. No dead code or unused imports remain
4. Code is clean and well-structured after Plex removal
5. Comments and docstrings are accurate (no Plex references)

Look for any remaining TODO comments related to Plex and remove or update them.

This ensures the codebase is clean and maintainable after the Plex removal.</action>
  <verify>
    <automated>! grep -i "plex\|TODO.*plex\|FIXME.*plex" jellyswipe/__init__.py</automated>
  </verify>
  <done>jellyswipe/__init__.py is clean, no Plex references or dead code</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build environment→image | Docker build process |
| runtime environment→app | Environment variable injection |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-09 | T | Import verification | accept | Verification task confirms no Plex imports |
| T-13-10 | S | Environment variables | mitigate | Verification confirms only Jellyfin env vars are accepted (existing validation) |
| T-13-11 | I | Docker build | accept | Standard build process, no new security concerns |
| T-13-12 | D | Code cleanup | accept | Maintains code quality, no security impact |

## Note on Security

This plan is primarily verification-focused. The actual security mitigations were implemented in prior plans (removing Plex auth flows, simplifying env var validation). This plan confirms those mitigations are working correctly.
</threat_model>

<verification>
After completing this plan, verify:
- Python import succeeds without errors
- Application initializes with Jellyfin-only env vars
- No Plex env vars are referenced in code
- Docker image builds successfully
- Code is clean with no Plex references
- All success criteria from Phase 13 are met
</verification>

<success_criteria>
1. Application imports successfully with `python -c "import jellyswipe"`
2. Application starts with only Jellyfin environment variables (no Plex vars needed)
3. No Plex imports or environment variable references found in any Python file
4. Docker image builds successfully: `docker build -t jellyswipe-test .`
5. No dead code, TODOs, or comments referencing Plex remain
6. All Phase 13 success criteria are satisfied:
   - No plexapi or Plex-related imports remain
   - pyproject.toml and uv.lock contain no Plex dependencies
   - Application starts with only Jellyfin configuration
   - No Plex environment variables are referenced
7. Application is ready for deployment as Jellyfin-only system
</success_criteria>

<output>
After completion, create `.planning/phases/13-remove-plex-support/13-03-SUMMARY.md`
</output>
