---
phase: "03"
plan: "02"
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - README.md
  - app.py
autonomous: true
requirements:
  - JAUTH-03
---

# Plan 02 — Secret hygiene + manual re-login / reset notes

<objective>
Ensure **Jellyfin auth failures never leak secrets** in JSON or logs (JAUTH-03), and that **README** documents the roadmap **re-login / token refresh** exercise (`03-CONTEXT` D-10; roadmap success criterion 3). Adjust `app.py` route handlers only if they surface raw upstream exceptions for Jellyfin paths.
</objective>

<threat_model>
| ID | Threat | Severity | Mitigation |
|----|--------|----------|------------|
| T-01 | `jsonify({'error': str(e)})` exposes token-bearing upstream message | high | Catch `RuntimeError` from provider; return fixed message for Jellyfin auth routes |
| T-02 | Verbose logging of request headers | medium | No `logger.info` of `Authorization` (if adding logs in this plan, redact) |
</threat_model>

<tasks>

<task id="03-02-01" type="execute">
<read_first>
- README.md
- app.py
- .planning/phases/03-jellyfin-authentication-http-client/03-CONTEXT.md
</read_first>
<action>
1. Audit Jellyfin-facing routes (`/auth/jellyfin-login`, any `get_provider()` error handling paths used by Jellyfin UX) and confirm errors returned to the browser are **generic** (no token/password substrings from caught exceptions).
2. In **README** under the existing Jellyfin operator / manual section, add an explicit **numbered** checklist for: invalidate creds (wrong password or revoked API key) → expect auth failure → restore valid creds → restart app or trigger provider `reset` path → confirm deck or `/Items` succeeds again (word to match acceptance grep below).
3. If README already satisfies the checklist with only minor wording gaps, make the smallest edit that makes the **re-login** exercise impossible to miss.
</action>
<acceptance_criteria>
- `grep -qiE "re-login|relogin|reset" README.md` exits 0 (operator re-auth story visible).
- `grep -qi "Jellyfin login failed" app.py` exits 0 (generic browser login error path), OR documented deviation if routes change names but remain non-leaky.
- Manual scan: `jellyfin_login` does not return raw upstream exception text on forced failure (document test steps in SUMMARY).
</acceptance_criteria>
</task>

</tasks>

<verification>
- `grep -qi "Jellyfin" README.md` and `grep -qi "token\\|401\\|revoke\\|password" README.md` — used as a quick doc sanity check; log result in SUMMARY.
</verification>

<success_criteria>
- JAUTH-03: No secret material in JSON errors or logs for Jellyfin auth surfaces touched by this plan.
- Roadmap SC3: Manual re-login / recovery steps are documented in README for operators.
</success_criteria>
