---
phase: "10"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - uv.lock
  - .python-version
autonomous: true
requirements:
  - UV-01
  - UV-02
  - DEP-01
---

# Plan 01 — `pyproject.toml`, `uv.lock`, Python 3.13 pin

<objective>
Establish **uv** as the dependency source of truth: root **`pyproject.toml`** (application layout, **not** a publishable wheel yet), committed **`uv.lock`** resolved for **CPython 3.13**, and **`.python-version`** so local `uv` uses 3.13. Migrate dependency names from **`requirements.txt`** without changing application code (Phase 11 moves packages).
</objective>

<threat_model>
| ID | Threat | Severity | Mitigation |
|----|--------|----------|------------|
| T-01 | Supply-chain / typosquatting when upgrading deps | medium | Use `uv add`/`uv lock` only from PyPI index defaults; review lockfile diff; keep names aligned with prior `requirements.txt` |
| T-02 | Accidentally widening `requires-python` | low | Set exactly `>=3.13,<3.14` per `10-CONTEXT.md` D-01 |
</threat_model>

<must_haves>
- `pyproject.toml` exists with `name = "jellyswipe"` and `requires-python = ">=3.13,<3.14"`.
- `uv.lock` exists and is committed-ready.
- `.python-version` contains `3.13` (local uv / editors).
</must_haves>

<tasks>

<task id="10-01-01" type="execute">
<read_first>
- requirements.txt
- .planning/phases/10-uv-python-3-13-lockfile/10-CONTEXT.md
- .planning/REQUIREMENTS.md
</read_first>
<action>
1. If `pyproject.toml` is missing, create an **application** project matching `uv init --app --bare` output: `[project]` with `name = "jellyswipe"`, `version = "1.2.0"` (or `0.1.0` if you prefer semver placeholder — pick one and keep it), **`requires-python = ">=3.13,<3.14"`**, `dependencies = []` initially. Do **not** add `[build-system]` unless `uv lock`/`uv sync` later fails without it (uv 0.9+ app projects often need none).
2. Run **`uv add`** for these packages together (single command acceptable): `flask`, `plexapi`, `werkzeug`, `requests`, `python-dotenv`, `gunicorn` — allow **latest compatible** versions (no manual upper caps unless resolver errors).
3. Write **`.python-version`** with exactly the line `3.13` (no patch pin required unless team standard says otherwise).
4. Run **`uv lock --python 3.13`** (or `UV_PYTHON=3.13 uv lock`) so the lockfile targets 3.13; re-run **`uv lock --upgrade`** once if D-03 “newest compatible” upgrade pass is separate — ensure final lock is consistent.
5. Do **not** delete `requirements.txt` in this task (Docker/README still reference it until Phase 12 unless you batch Phase 12 in same PR — default **keep file** for now).
</action>
<acceptance_criteria>
- `test -f pyproject.toml` exits 0.
- `grep -q 'name = "jellyswipe"' pyproject.toml` exits 0.
- `grep -q 'requires-python = ">=3.13,<3.14"' pyproject.toml` exits 0.
- `grep -qE 'flask|plexapi|werkzeug|requests|python-dotenv|gunicorn' pyproject.toml` exits 0 (each dependency name appears in dependencies list).
- `test -f uv.lock` exits 0.
- `test -f .python-version` exits 0 and `grep -qx '3.13' .python-version` exits 0.
</acceptance_criteria>
</task>

</tasks>

<verification>
- From repo root with uv installed: `uv sync` completes using `.python-version` / `--python 3.13` without resolver errors.
</verification>

<success_criteria>
- UV-01: `pyproject.toml` + `uv.lock` are present and are the authoritative dependency declaration going forward.
- UV-02: `requires-python` and `.python-version` target 3.13 line.
- DEP-01: Lock includes newest resolver-selected versions that install on 3.13 (validated in Plan 02).
</success_criteria>
