---
phase: 40-full-migration-validation-and-sync-db-removal
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - alembic/env.py
  - tests/test_migrations.py
autonomous: true
requirements:
  - VAL-02
must_haves:
  truths:
    - "D-01: VAL-02 test module chosen for minimal duplication vs `tests/conftest.py` Alembic/bootstrap (planner placed tests in `tests/test_migrations.py`)."
    - "D-03: Migration parity uses an isolated temp-file DB fixture distinct from route-scale fixtures."
    - "D-04: Proof path is fresh SQLite file → Alembic upgrade head → table/`alembic_version` presence assertions before idempotency subprocess."
    - "VAL-02: an empty on-disk SQLite file reaches head schema via Alembic, and a second operator-style `alembic upgrade head` on the same already-current file exits cleanly (idempotent)."
    - "Subprocess coverage mirrors real upgrades; in-process `upgrade_to_head` may still be used for table assertions but does not replace the subprocess idempotency check (40-CONTEXT D-02)."
    - "Core tables and `alembic_version` are asserted after the first upgrade so regressions in baseline revisions are caught in CI."
  artifacts:
    - path: "tests/test_migrations.py"
      provides: "Migration parity tests (empty DB → head; subprocess idempotent second pass)"
      contains: "subprocess"
    - path: "alembic/env.py"
      provides: "Alembic online/offline URL resolution that allows subprocess CLI to target a temp DB via DATABASE_URL"
      contains: "_resolve_url"
  key_links:
    - from: "tests/test_migrations.py"
      to: "alembic/versions/0001_phase36_baseline.py"
      via: "table presence expectations after upgrade"
      pattern: "rooms|auth_sessions|swipes|matches|alembic_version"
    - from: "alembic/env.py"
      to: "jellyswipe/migrations.py"
      via: "normalize / resolve DB URL"
      pattern: "get_database_url"
---

<objective>
Add automated migration parity coverage for Phase 40 (VAL-02): prove a fresh file-backed SQLite database upgrades to Alembic head with expected core tables/version row, then prove operator-style idempotency by running `alembic upgrade head` again via subprocess (40-CONTEXT D-02–D-04).

**Precondition (D-09):** Implement this wave only after Phase 39 persistence work is merged to the integration branch executing Phase 40 (no overlap with incomplete Phase 39).

Purpose: Catch migration regressions independently of route tests and establish the subprocess gate requested in research before sync-DB removal work continues.

**Tracked decisions:** cites **D-01, D-03, D-04** in `must_haves.truths`; **D-09** precondition above.

Output:
- Tests under `tests/test_migrations.py` using an isolated temp DB fixture (D-03)
- If required for subprocess fidelity, adjust `alembic/env.py` so CLI upgrades honor `DATABASE_URL` (or equivalent) targeting the temp file instead of the static `alembic.ini` default only
</objective>

<execution_context>
@/Users/aherrington/.codex/get-shit-done/workflows/execute-plan.md
@/Users/aherrington/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/40-full-migration-validation-and-sync-db-removal/40-CONTEXT.md
@.planning/phases/40-full-migration-validation-and-sync-db-removal/40-RESEARCH.md
@.planning/phases/40-full-migration-validation-and-sync-db-removal/40-PATTERNS.md
@.planning/phases/40-full-migration-validation-and-sync-db-removal/40-VALIDATION.md
@jellyswipe/migrations.py
@alembic/env.py
@alembic.ini
@alembic/versions/0001_phase36_baseline.py
@tests/conftest.py

<interfaces>
From `40-CONTEXT.md` (D-04 minimum): fresh file DB → upgrade head → table presence checks; optionally strengthen with `alembic_version`.

Baseline tables from `0001_phase36_baseline.py`: `rooms`, `auth_sessions`, `swipes`, `matches`.

Pytest driver: `[tool.pytest.ini_options]` in `pyproject.toml` — full suite uses `uv run pytest`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Ensure Alembic CLI can target an isolated DATABASE_URL for subprocess parity tests</name>
  <files>alembic/env.py</files>
  <read_first>
    - alembic/env.py
    - jellyswipe/migrations.py
    - alembic.ini
    - .planning/phases/40-full-migration-validation-and-sync-db-removal/40-RESEARCH.md
  </read_first>
  <action>
Adjust `_resolve_url()` in `alembic/env.py` so subprocess `alembic upgrade head` can target the same temp SQLite file as programmatic upgrades when `DATABASE_URL` (sync `sqlite:///...`) is present in the process environment — without requiring destructive edits to `alembic.ini`.

Acceptable precedence: prefer `DATABASE_URL` / `DB_PATH`-derived URL through `get_database_url()` when those env vars are set, otherwise fall back to `config.get_main_option("sqlalchemy.url")`.

Keep offline/online runners using a single normalization path compatible with SQLite batch mode (`render_as_batch=True` unchanged).

Do **not** import the FastAPI app root; dependency direction remains `env.py` → `jellyswipe.migrations.get_database_url` + models metadata only (MIG-03 alignment).
  </action>
  <verify>
    <automated>uv run pytest tests/test_migrations.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `alembic/env.py` defines `_resolve_url` with `DATABASE_URL`-first (or equivalent documented) precedence so subprocess upgrades target the isolated test database
    - `importlib.reload` smoke or unit-style check optional; hard gate is Task 2 `uv run pytest tests/test_migrations.py -q`
  </acceptance_criteria>
  <done>Subprocess Alembic can reliably upgrade an isolated DB file using env-driven URL resolution.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migration parity tests — empty DB, upgrade head, core tables/version, subprocess idempotent second pass</name>
  <files>tests/test_migrations.py</files>
  <read_first>
    - tests/conftest.py
    - jellyswipe/migrations.py
    - .planning/phases/40-full-migration-validation-and-sync-db-removal/40-PATTERNS.md
    - alembic/versions/0001_phase36_baseline.py
  </read_first>
  <behavior>
    - Fresh empty file path under pytest `tmp_path`; first upgrade yields required tables + `alembic_version`.
    - Second pass: subprocess from repo root runs `uv run alembic upgrade head` (or `[sys.executable, "-m", "alembic", "upgrade", "head"]` with equivalent env) against the **same** file with `DATABASE_URL` set — must exit code 0 and remain at head (`alembic_version.version_num` unchanged / still present).
    - Optionally assert head revision equals `0001_phase36` (update assertion if migrations grow before execution).
  </behavior>
  <action>
Create `tests/test_migrations.py` implementing VAL-02 per D-02–D-04:

**First pass:** use `upgrade_to_head(build_sqlite_url(path))` (or `_alembic_config` + `command.upgrade`) to upgrade the empty SQLite file deterministically inside the pytest process.

**Assertions (minimum — D-04):**
- Query sqlite master or SQLAlchemy inspect to prove tables exist named: `rooms`, `auth_sessions`, `swipes`, `matches`.
- Row in `alembic_version` with non-empty version string matching current head expectation.

**Second pass — subprocess:** set `DATABASE_URL` to the canonical sync SQLite URL (`sqlite:/// absolute-path`), inherit `cwd` as repository root, pass a clean `env` dict (copy of `os.environ`) with `PYTHONPATH` including repo root if required on CI (match local `uv run` ergonomics).

**Subprocess argv:** Prefer `sys.executable` + `-m alembic` using project’s `uv` convention: document `uv run alembic upgrade head` wrapping if that is simpler for PATH on developer machines vs CI parity.

Reject path injection vectors: derive DB path exclusively from pytest `tmp_path` / resolved `Path`; never concatenate untrusted inputs into shell commands (use argv list subprocess only).

Add focused offline comments if revision head label drifts (`0001_phase36`).
  </action>
  <verify>
    <automated>uv run pytest tests/test_migrations.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_migrations.py` exists and contains `def test_`
    - `tests/test_migrations.py` contains `subprocess`
    - `tests/test_migrations.py` contains `"rooms"` and `"matches"` (table name assertions — string grep signal)
    - `tests/test_migrations.py` contains `alembic_version`
    - `uv run pytest tests/test_migrations.py -q` exits 0
  </acceptance_criteria>
  <done>VAL-02 automated coverage ships: empty-db upgrade proof + subprocess idempotency.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pytest subprocess → filesystem | Writes under `tmp_path`; must not mutate developer `data/` or production paths |
| env vars → Alembic URL | `DATABASE_URL` drives schema upgrades; spoofing affects only test process |

## STRIDE-lite Threat Register

| Threat ID | Category | Risk | Mitigation |
|-----------|----------|------|------------|
| T-40-M01 | T | subprocess argv/shell injection if paths concatenated poorly | Always `subprocess.run([...])` list form; paths from `Path(tmp_path / "migrate.db").resolve()` only |
| T-40-M02 | E | wrong `cwd`/`PYTHONPATH` causes silent upgrade against wrong DB | Assert post-condition on unique temp path; stderr checked on subprocess failure (`check=True`) |
| T-40-M03 | E | env.py precedence bug upgrades default `./data/jellyswipe.db` | Tests use dedicated temp file + env `DATABASE_URL`; fail if subprocess hits wrong file (assert table creation only on intended path via connection open to explicit path before/after) |
| T-40-M04 | Repudiation | low — migration tests flaky on CI timeouts | Prefer short subprocess; deterministic single revision head |

_rates: LOW–MEDIUM for subprocess/env coupling; MEDIUM mitigations documented in tasks._

</threat_model>

<verification>
`uv run pytest tests/test_migrations.py -q` green; optionally run quick UoW smoke during development: `uv run pytest tests/test_dependencies.py::TestGetDbUow::test_yields_uow_and_commits_on_success -q`.
</verification>

<success_criteria>
Automated migration parity tests satisfy VAL-02 (empty upgrade + subprocess idempotent second pass) matching `40-CONTEXT.md` D-02–D-04.

</success_criteria>

<output>
After completion, create `.planning/phases/40-full-migration-validation-and-sync-db-removal/40-01-SUMMARY.md` and update `40-VALIDATION.md` task rows for Plan 01 / VAL-02 with concrete filenames and verification commands once implementation exists.
</output>
