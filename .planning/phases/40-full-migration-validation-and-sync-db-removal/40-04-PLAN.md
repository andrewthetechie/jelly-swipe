---
phase: 40-full-migration-validation-and-sync-db-removal
plan: 04
type: execute
wave: 4
depends_on:
  - .planning/phases/40-full-migration-validation-and-sync-db-removal/40-01-PLAN.md
  - .planning/phases/40-full-migration-validation-and-sync-db-removal/40-02-PLAN.md
  - .planning/phases/40-full-migration-validation-and-sync-db-removal/40-03-PLAN.md
files_modified:
  - scripts/phase40_val04_guard.sh
  - .planning/phases/40-full-migration-validation-and-sync-db-removal/40-VALIDATION.md
  - .planning/REQUIREMENTS.md
  - .github/workflows/test.yml
autonomous: true
requirements:
  - VAL-04
  - VAL-03
must_haves:
  truths:
    - "D-06: VAL-04 guard + script scope checks **only** under `jellyswipe/` — `alembic/` sync engine usage is explicitly out of scope for the ban."
    - "D-07: Guard + manual review confirm no table-creating `init_db()` or ad hoc DDL paths ship in app code; stubs documented in Deferred if transitional."
    - "D-13: Executor maintains `40-VALIDATION.md` as the authoritative requirement-evidence matrix (Nyquist-aligned) covering ADB-03, VAL-02, VAL-03, VAL-04."
    - "D-14: Any intentional deferrals (including D-12 `run_sync` retention) are mirrored in BOTH `40-VALIDATION.md` Deferred section AND `40-CONTEXT.md` `<deferred>` before milestone sign-off."
    - "D-15: Flip `REQUIREMENTS.md` checkboxes only after Plan 03/04 verification artifacts prove each ID; CONTEXT is narrative only."
    - "VAL-04: scripted scan limited to `jellyswipe/` proves no `import sqlite3`, no `get_db_closing`, no table-creating `init_db` DDL path, and no `SQLModel` usage in app package (per D-05–D-08)."
    - "VAL-03: full `uv run pytest` serves as milestone integration gate after guard script lands."
    - "VAL-02 + ADB-03 evidence cross-linked inside `40-VALIDATION.md` matrix (plans 01–03 outputs) plus ROADMAP traceability (**D-16**)."
    - "If `run_sync` persists on `DatabaseUnitOfWork`, document rationale under Deferred/technical debt with `db_uow.py` pointer (**D-12**)."
  artifacts:
    - path: "scripts/phase40_val04_guard.sh"
      provides: "CI/local guard enforcing VAL-04 patterns on jellyswipe/"
      contains: "jellyswipe"
    - path: ".planning/phases/40-full-migration-validation-and-sync-db-removal/40-VALIDATION.md"
      provides: "Signed requirement matrix + deferred section synced with CONTEXT"
      contains: "ADB-03"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Checkboxes flipped for Phase 40 requirements once evidence attached"
      contains: "ADB-03"
  key_links:
    - from: ".github/workflows/test.yml"
      to: "scripts/phase40_val04_guard.sh"
      via: "CI step optional but recommended (**D-11**)"
      pattern: "phase40_val04_guard"
    - from: "40-VALIDATION.md"
      to: ".planning/ROADMAP.md"
      via: "Phase 40 success criteria checklist"
      pattern: "VAL-02"
---

<objective>
Close Phase 40 with **VAL-04** automation, documentation sign-off (**40-VALIDATION.md** matrix + deferred section), and **REQUIREMENTS.md** checkbox updates when justified — after proving **VAL-03** via full pytest.

Purpose: Make the application-layer boundary auditable and milestone-complete without expanding scope beyond `jellyswipe/` scanning (40-CONTEXT D-05, D-11, D-15).

Output:
- Executable guard script (shell or python) invoked locally + optional CI hook
- Updated `40-VALIDATION.md` with concrete evidence links / commands
- Updated `REQUIREMENTS.md` entries for **ADB-03**, **VAL-02**, **VAL-03**, **VAL-04** when verification satisfied
</objective>

<execution_context>
@/Users/aherrington/.codex/get-shit-done/workflows/execute-plan.md
@/Users/aherrington/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/40-full-migration-validation-and-sync-db-removal/40-CONTEXT.md
@.planning/phases/40-full-migration-validation-and-sync-db-removal/40-RESEARCH.md
@.planning/phases/39-room-swipe-match-and-sse-persistence-conversion/39-VALIDATION.md
@pyproject.toml
@jellyswipe/

<interfaces>
**VAL-04 scan patterns (scope = `jellyswipe/` only):**
- `import sqlite3` / `from sqlite3`
- `sqlite3.connect`
- `get_db_closing`
- `init_db` legacy table-creation (string + function definition heuristics)
- `SQLModel` import / typing usage

**False positive controls:** allow comments referencing banned tokens only if script uses word-boundary aware `rg` or AST — document accepted suppressions.

**pytest:** `uv run pytest` per `[tool.pytest.ini_options]`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add VAL-04 guard script scoped to jellyswipe/</name>
  <files>scripts/phase40_val04_guard.sh, .github/workflows/test.yml</files>
  <read_first>
    - .planning/phases/40-full-migration-validation-and-sync-db-removal/40-RESEARCH.md
    - .planning/phases/40-full-migration-validation-and-sync-db-removal/40-PATTERNS.md
    - pyproject.toml
  </read_first>
  <action>
Create `scripts/phase40_val04_guard.sh` (executable) that **fails non-zero** when banned patterns appear under `jellyswipe/`.

Implementation guidance:
- Use `rg` with path argument `jellyswipe/`; exit 1 on matches.
- Patterns minimally: `^import sqlite3`, `from sqlite3 import`, `sqlite3.connect`, `get_db_closing`, `SQLModel`, and `def init_db` if still dangerous (tune to avoid docstring noise — prefer structured comment explaining each pattern).
- Print remediation hint referencing Phase 40 docs.

Optional: integrate into `.github/workflows/test.yml` as a discrete step adjacent to pytest (recommended: run guard before pytest; pytest remains authoritative VAL-03).

Ensure script tolerates CI environments without silent skips (`set -euo pipefail`).

Repository already ships `.github/workflows/test.yml`; extend it unless maintainers deliberately keep guard local-only (**D-11** discretion).
  </action>
  <verify>
    <automated>bash scripts/phase40_val04_guard.sh</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/phase40_val04_guard.sh` exists and is executable
    - Script contains literal pattern `sqlite3.connect` OR equivalent coverage noted in comments
    - Script contains literal pattern `SQLModel`
    - `bash scripts/phase40_val04_guard.sh` exits 0 on clean tree
  </acceptance_criteria>
  <done>VAL-04 scanning reproducible locally and optionally in CI.</done>
</task>

<task type="auto">
  <name>Task 2: Final VAL-03 integration gate — full pytest</name>
  <files>pyproject.toml</files>
  <read_first>
    - pyproject.toml
    - .planning/phases/40-full-migration-validation-and-sync-db-removal/40-VALIDATION.md
  </read_first>
  <action>
Run **`uv run pytest`** (inherits coverage opts from `[tool.pytest.ini_options]`).

Capture summary snippet (pass counts / skipped reasoning) in `40-03-SUMMARY.md` or `40-04-SUMMARY.md` whichever is final integration note.

If failures occur, triage: guard false negative vs real regression — never weaken VAL-04 patterns to greenwash.
  </action>
  <verify>
    <automated>uv run pytest</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest` exits 0
  </acceptance_criteria>
  <done>VAL-03 satisfied at milestone integration level.</done>
</task>

<task type="manual" mixed="true">
  <name>Task 3: Update 40-VALIDATION.md matrix, deferred section, sign-off</name>
  <files>.planning/phases/40-full-migration-validation-and-sync-db-removal/40-VALIDATION.md</files>
  <read_first>
    - .planning/phases/40-full-migration-validation-and-sync-db-removal/40-VALIDATION.md
    - .planning/phases/39-room-swipe-match-and-sse-persistence-conversion/39-VALIDATION.md
    - .planning/phases/40-full-migration-validation-and-sync-db-removal/40-CONTEXT.md
  </read_first>
  <action>
Rewrite Per-Task Verification Map rows with **real** commands:
- Plan 01: `uv run pytest tests/test_migrations.py -q`
- Plan 02/03 subsets + final gate
- Plan 04 guard + full pytest evidence

Populate **Deferred / intentionally out of scope** with any retained `run_sync` technical debt (**D-12**) or explicitly mark removal.

Set frontmatter keys:
```
wave_0_complete: true   # After evidence exists
```
…only when truthful.

Complete **Validation Sign-Off** checklist; include explicit ROADMAP Phase 40 success criteria rows mapped to artifacts (**D-16**).

Approve field transitions from `pending` to dated sign-off once human reviewer satisfied (executor records timestamp narrative).
  </action>
  <verify>
    <automated>rg "wave_0_complete: true" .planning/phases/40-full-migration-validation-and-sync-db-removal/40-VALIDATION.md</automated>
  </verify>
  <acceptance_criteria>
    - `40-VALIDATION.md` Per-Task table references **ADB-03**, **VAL-02**, **VAL-03**, **VAL-04**
    - `40-VALIDATION.md` includes **Deferred** subsection mentioning `run_sync` disposition
    - `40-VALIDATION.md` Validation Sign-Off checklist has ROADMAP trace note
    - Frontmatter presents `wave_0_complete: true` when objectively satisfied
  </acceptance_criteria>
  <done>Canonical planning verification artifact matches executed reality (**D-13–D-16**).</done>
</task>

<task type="manual">
  <name>Task 4: REQUIREMENTS.md checkbox updates post-verification</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md
    - .planning/phases/40-full-migration-validation-and-sync-db-removal/40-VALIDATION.md
  </read_first>
  <action>
Flip markdown checkboxes for **ADB-03**, **VAL-02**, **VAL-03**, **VAL-04** from `[ ]` to `[x]` **only after** Tasks 1–3 evidence exists and aligns with wording in `REQUIREMENTS.md`.

Annotate footer change log stanza (“Last updated”) with date (**2026-xx-xx**) per repository convention.

Ensure traceability table at bottom mirrors checked state (`Status: Complete` rows for Phase 40 requirements).

Do **not** mark MVC/PAR Phase 39 items here unless Phase 39 executed — scope remains Phase 40 lines only.
  </action>
  <verify>
    <automated>rg "\\[x\\].*ADB-03" .planning/REQUIREMENTS.md && rg "\\[x\\].*VAL-02" .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - Lines for **ADB-03**, **VAL-02**, **VAL-03**, **VAL-04** show `[x]` when verified true
    - `.planning/REQUIREMENTS.md` traceability rows for those IDs cite Phase 40 **Complete**
  </acceptance_criteria>
  <done>External requirement registry matches milestone completion (**D-15**).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| VAL-04 script ↔ repository | Incorrect `rg` scope could miss violations (`tests/` exemptions could hide `jellyswipe/` regressions)|

## STRIDE-lite Threat Register

| Threat ID | Category | Risk | Mitigation |
|-----------|----------|------|------------|
| T-40-V01 | Evasion | Matcher misses multiline imports / dynamic importlib | Extend patterns after first audit; optionally add AST tool later without blocking Phase 40 |
| T-40-V02 | False negative — SQLModel extras | MEDIUM | Separate pattern for dependency group already clean in `pyproject.toml` yet future addition flagged |
| T-40-D03 | Doc-only checkbox updates | Signing requirements without failing tests blocked | Mandatory `uv run pytest` + guard success before REQ edits |

_rates: MEDIUM for scanning completeness; mitigation = conservative pattern list + CI enforcement._

</threat_model>

<verification>
`bash scripts/phase40_val04_guard.sh && uv run pytest`

</verification>

<success_criteria>
Milestone-close artifacts exist: deterministic VAL-04 guard on `jellyswipe/`, green full suite (**VAL-03**), updated validation matrix/checklists, REQ checkboxes truthful.

</success_criteria>

<output>
Create `.planning/phases/40-full-migration-validation-and-sync-db-removal/40-04-SUMMARY.md` consolidating links to summaries 01–04 and final CI evidence.

OPTIONAL: Refresh `.planning/STATE.md` progress fields when parent agent coordinates milestone closure (not required silently here).

</output>
