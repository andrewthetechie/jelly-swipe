---
phase: 40-full-migration-validation-and-sync-db-removal
plan: 02
type: execute
wave: 2
depends_on:
  - .planning/phases/40-full-migration-validation-and-sync-db-removal/40-01-PLAN.md
files_modified:
  - jellyswipe/migrations.py
  - jellyswipe/__init__.py
  - tests/conftest.py
  - tests/test_routes_room.py
  - tests/test_routes_sse.py
  - tests/test_routes_xss.py
  - tests/test_error_handling.py
  - tests/test_db.py
  - tests/test_infrastructure.py
  - tests/test_auth.py
  - tests/test_dependencies.py
  - tests/test_db_runtime.py
autonomous: true
requirements:
  - VAL-03
  - ADB-03
must_haves:
  truths:
    - "D-09: Execute Plan 02 only after Phase 39 is merged on the branch carrying Phase 40."
    - "VAL-03: pytest suite stops depending on synchronous `get_db()` / `get_db_closing()`/`sqlite3` connections from legacy `jellyswipe/db.py` helpers for bootstrap and assertions."
    - "ADB-03 (partial): `jellyswipe/migrations.py` URL resolution must not require importing a `sqlite3`-bearing `jellyswipe/db.py` solely for default `DB_PATH` fallback — decouple defaults per 40-RESEARCH risk #1."
    - "Tests reuse the established Alembic + async runtime harness (`_bootstrap_temp_db_runtime`, sessionmaker/UoW) instead of widening new sync seams."
  artifacts:
    - path: "jellyswipe/migrations.py"
      provides: "get_database_url without fragile import-time dependency on jellyswipe.db for DB_PATH"
      contains: "get_database_url"
    - path: "tests/conftest.py"
      provides: "Fixtures yielding async DB access paths (UoW / AsyncSession)"
      contains: "_bootstrap_temp_db_runtime"
    - path: "tests/test_routes_room.py"
      provides: "Route tests migrated off jellyswipe.db.get_db / get_db_closing where applicable"
      contains: "@pytest.fixture"
  key_links:
    - from: "tests/conftest.py"
      to: "jellyswipe/db_uow.py"
      via: "`asyncio.run` + DatabaseUnitOfWork for tests needing SQL"
      pattern: "get_sessionmaker|DatabaseUnitOfWork|get_db_uow"
    - from: "jellyswipe/__init__.py"
      to: "jellyswipe/migrations.py"
      via: "test-time DB_PATH / env alignment"
      pattern: "DB_PATH"
---

<objective>
Remove the automated test harness dependency on synchronous `jellyswipe.db` helpers (`db_connection`, `get_db()`, `get_db_closing()`) and decouple migration URL fallback from importing `jellyswipe/db.py` (**ADB-03** prerequisite path).

Purpose: unblock deletion/shrinkage of `jellyswipe/db.py` in Plan 03 while keeping route/auth/SSE/error-handling tests behavior-equivalent (**VAL-03**).

Output:
- Neutral default DB path holder or env-only resolution integrated into `get_database_url()` (`jellyswipe/migrations.py`) without `import jellyswipe.db` for URL defaults
- `tests/conftest.py` async-first fixtures aligned with `_bootstrap_temp_db_runtime`
- All inventory call sites updated per `40-RESEARCH.md` (“Tests importing jellyswipe.db”)
</objective>

<execution_context>
@/Users/aherrington/.codex/get-shit-done/workflows/execute-plan.md
@/Users/aherrington/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/40-full-migration-validation-and-sync-db-removal/40-CONTEXT.md
@.planning/phases/40-full-migration-validation-and-sync-db-removal/40-RESEARCH.md
@.planning/phases/40-full-migration-validation-and-sync-db-removal/40-PATTERNS.md
@jellyswipe/db.py
@jellyswipe/db_runtime.py
@jellyswipe/db_uow.py
@jellyswipe/migrations.py
@jellyswipe/__init__.py
@jellyswipe/dependencies.py
@tests/conftest.py

<interfaces>
Inventory from `40-RESEARCH.md` / `40-PATTERNS.md`: migrate `tests/conftest.py` `db_connection`, `tests/test_routes_*`, `tests/test_error_handling.py`, infra/db tests asserting module surface — prefer `DatabaseUnitOfWork` inside `asyncio.run` patterns established in Phase 37–39.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Decouple jellyswipe.migrations.get_database_url from jellyswipe.db import</name>
  <files>jellyswipe/migrations.py, jellyswipe/__init__.py</files>
  <read_first>
    - jellyswipe/migrations.py
    - jellyswipe/__init__.py
    - jellyswipe/db.py
    - .planning/phases/40-full-migration-validation-and-sync-db-removal/40-PATTERNS.md
  </read_first>
  <action>
Eliminate module-level `import jellyswipe.db` from `migrations.py` used only for reading `DB_PATH`.

Introduce neutral path resolution:
- Preserve precedence: explicit `DATABASE_URL`, `DB_PATH` env vars, explicit `db_path` arg to wrappers, default data path fallback matching today’s semantics (`Path(...) / \"data\" / \"jellyswipe.db"` pattern from research).

Migrate **runtime-mutable DB path holder** (`DB_PATH`) to a neutral module (**e.g.** `jellyswipe/db_paths.py` exporting `APPLICATION_DB_PATH`) or consolidate into migrations layer per planner preference (D-10 preview).

Update `jellyswipe/__init__.py` monkeypatch/`DB_PATH` test wiring to mutate the neutral holder consumed by migrations + async runtime bootstrap (coordinate with Phase 39 close state).

Preserve `build_sqlite_url`, `normalize_sync_database_url`, `upgrade_to_head` contracts.

After change: `grep -n \"import jellyswipe.db\" jellyswipe/migrations.py` returns no matches OR only appears if still required temporarily — objective is removal of URL coupling to sqlite3 module side effects.
  </action>
  <verify>
    <automated>uv run pytest tests/test_dependencies.py tests/test_db_runtime.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `jellyswipe/migrations.py` contains `def get_database_url`
    - `jellyswipe/migrations.py` does **not** reference `jellyswipe.db` for fallback URL computation (preferred grep: `rg \"jellyswipe\\.db\" jellyswipe/migrations.py` → empty)
    - `uv run pytest tests/test_dependencies.py tests/test_db_runtime.py -q` exits 0
  </acceptance_criteria>
  <done>migrations helpers resolve SQLite URLs without importing legacy db module solely for globals.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor tests/conftest.py db_connection fixture off get_db()</name>
  <files>tests/conftest.py</files>
  <read_first>
    - tests/conftest.py
    - jellyswipe/db_runtime.py
    - jellyswipe/db_uow.py
    - jellyswipe/dependencies.py
    - tests/test_repositories.py
  </read_first>
  <action>
Replace any `conn = jellyswipe.db.get_db()` yielding synchronous connections with async equivalents:

Suggested patterns:
- Provide `async def` fixtures using `@pytest_asyncio.fixture` if asyncio plugin present; otherwise wrap `asyncio.run` around narrowly scoped helpers that open `DatabaseUnitOfWork` via `async with` / sessionmaker factories.

Maintain `_bootstrap_temp_db_runtime` ordering: monkeypatch/env `DATABASE_URL` + `APPLICATION_DB_PATH` + `upgrade_to_head` then `initialize_runtime`.

Ensure tests that depended on synchronous `sqlite3.Connection` primitives adopt SQLAlchemy `AsyncSession`/repositories parity.

Preserve shared `tmp_path`/DB uniqueness semantics for parallel-safe tests.
  </action>
  <verify>
    <automated>uv run pytest tests/test_dependencies.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `tests/conftest.py` does not contain `jellyswipe.db.get_db()` (literal grep absent)
    - `tests/conftest.py` references `_bootstrap_temp_db_runtime`
    - `uv run pytest tests/test_dependencies.py -q` exits 0
  </acceptance_criteria>
  <done>Synchronous test-bootstrap connection seam removed.</done>
</task>

<task type="auto">
  <name>Task 3: Migrate route/SSE/error/infrastructure/db tests off get_db/get_db_closing assertions</name>
  <files>tests/test_routes_room.py, tests/test_routes_sse.py, tests/test_routes_xss.py, tests/test_error_handling.py, tests/test_db.py, tests/test_infrastructure.py, tests/test_auth.py</files>
  <read_first>
    - .planning/phases/40-full-migration-validation-and-sync-db-removal/40-RESEARCH.md
    - jellyswipe/repositories/__init__.py
    - tests/conftest.py
  </read_first>
  <action>
Ripgrep-guided migration:
- Replace `from jellyswipe.db import get_db, get_db_closing` usages with AsyncSession/UoW test helpers seeded through async repositories or httpx/TestClient parity already used elsewhere.

Rewrite `tests/test_db.py` and `tests/test_infrastructure.py` expectations to match **planned** shrunk `db` surface (temporary dual assertions acceptable mid-wave but end state must justify removal of sync getters).

Maintain XSS/SSE/route contracts — only swap persistence inspection paths.

Normalize `monkeypatch.setattr(..., \"DB_PATH\", ...)` patches to whichever neutral holder Plan 03 will keep (coordinate naming with Task 1).
  </action>
  <verify>
    <automated>uv run pytest tests/test_routes_room.py tests/test_routes_sse.py tests/test_routes_xss.py tests/test_error_handling.py tests/test_db.py tests/test_infrastructure.py tests/test_auth.py -q</automated>
  </verify>
  <acceptance_criteria>
    - No `get_db_closing()` string remains in cited test files (`rg \"get_db_closing\" tests/test_routes_room.py tests/test_routes_sse.py tests/test_routes_xss.py tests/test_error_handling.py` empty)
    - `uv run pytest tests/test_routes_room.py tests/test_routes_sse.py tests/test_routes_xss.py tests/test_error_handling.py tests/test_db.py tests/test_infrastructure.py tests/test_auth.py -q` exits 0
  </acceptance_criteria>
  <done>Route/security/error harness no longer hinges on synchronous sqlite helpers in tests touched by Phase 39 inventory.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tests manipulating DB paths | Incorrect patch could write real user data dirs |
| async fixture lifetime | leaked engines/sessions causing flaky SSE tests |

## STRIDE-lite Threat Register

| Threat ID | Category | Risk | Mitigation |
|-----------|----------|------|------------|
| T-40-T01 | Spoofing/auth bypass surrogate | refactoring tests hides missing auth stubs | Maintain route authorization suites green; rerun `tests/test_route_authorization.py` in wave regression batch |
| T-40-E02 | Elevation misuse if tests skip transactions | asyncio.run per test keeps isolated loop scope | Narrow `asyncio.run` blocks; reuse session factories from runtime |
| T-40-D03 | Denial/flake async engine churn | MEDIUM | Fixture-scoped runtime where possible |

_rates: MEDIUM for regression risk mitigated via focused pytest subsets then full gate in Plan 04._

</threat_model>

<verification>
After substantive commits: smoke `uv run pytest tests/test_dependencies.py::TestGetDbUow::test_yields_uow_and_commits_on_success -q` plus alternating migration module from Plan 01. Before wave exit: broaden batch per `40-VALIDATION.md` sampling.

</verification>

<success_criteria>
Tests and migration bootstrap no longer require synchronous `sqlite3` connection helpers exposed by legacy `dbConnection` seams; **`jellyswipe/migrations.py` import graph** no longer mandates `sqlite3`-bearing legacy module purely for globals.

</success_criteria>

<output>
After completion, create `.planning/phases/40-full-migration-validation-and-sync-db-removal/40-02-SUMMARY.md`; refresh `40-VALIDATION.md` rows for Wave 02 / VAL-03.

</output>
