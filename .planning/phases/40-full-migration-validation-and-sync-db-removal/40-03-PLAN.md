---
phase: 40-full-migration-validation-and-sync-db-removal
plan: 03
type: execute
wave: 3
depends_on:
  - .planning/phases/40-full-migration-validation-and-sync-db-removal/40-01-PLAN.md
  - .planning/phases/40-full-migration-validation-and-sync-db-removal/40-02-PLAN.md
files_modified:
  - jellyswipe/db.py
  - jellyswipe/bootstrap.py
  - jellyswipe/__init__.py
  - jellyswipe/db_runtime.py
autonomous: true
requirements:
  - ADB-03
  - VAL-03
must_haves:
  truths:
    - "`jellyswipe/` runtime package exposes **no** `import sqlite3` for application DB work; no `get_db` / `get_db_closing` context managers that open raw `sqlite3` connections."
    - "Session cleanup / maintenance previously via sync `cleanup_expired_auth_sessions` uses async equivalents (`cleanup_expired_auth_sessions_async` or inlined async repository delete) wired through startup lifespan."
    - "Startup bootstrap uses async-only maintenance entry points orchestrating `upgrade_to_head` → `initialize_runtime` without reintroducing ad hoc DDL (MIG-04 alignment)."
  artifacts:
    - path: "jellyswipe/db.py"
      provides: "Deleted or shrunk residue exporting only WAL/helper constants absent sqlite3 seams (D-10)"
      contains: "(module removed entirely) OR async-only maintenance helpers relocated from legacy sync surface"
    - path: "jellyswipe/bootstrap.py"
      provides: "`main()` uses async-compatible startup contracts only"
      contains: "asyncio.run|initialize_runtime|upgrade_to_head"
    - path: "jellyswipe/db_runtime.py"
      provides: "Canonical engine/sessionmaker ownership for maintenance jobs"
      contains: "initialize_runtime"
  key_links:
    - from: "jellyswipe/__init__.py"
      to: "jellyswipe/db_runtime.py"
      via: "lifespan attaches async cleanup"
      pattern: "cleanup_expired_auth_sessions"
    - from: "jellyswipe/db_uow.py"
      to: "jellyswipe/repositories/"
      via: "`run_sync` bridge if retained documents PAR-04 (not sqlite3)"
      pattern: "run_sync\\("
---

<objective>
Collapse or delete **`jellyswipe/db.py`**: eliminate runtime `sqlite3`, `get_db_closing`, and `get_db` from the operational package (**ADB-03** completion) while migrating remaining maintenance callers (`cleanup_expired_auth_sessions`) to async (**VAL-03** parity).

Purpose: Satisfy milestone ban on synchronous raw SQLite connectors in application code (40-CONTEXT D-05, D-10).

Output:
- Residual-only `db.py` **or full removal**, with surviving helpers relocated to neutral/async modules (`db_runtime`, `bootstrap`, `db_paths`).
- Verified async cleanup + startup path with no hidden `asyncio.run` wrappers except existing bootstrap shell.
</objective>

<execution_context>
@/Users/aherrington/.codex/get-shit-done/workflows/execute-plan.md
@/Users/aherrington/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/40-full-migration-validation-and-sync-db-removal/40-CONTEXT.md
@.planning/phases/40-full-migration-validation-and-sync-db-removal/40-RESEARCH.md
@jellyswipe/db.py
@jellyswipe/db_runtime.py
@jellyswipe/bootstrap.py
@jellyswipe/__init__.py
@jellyswipe/db_uow.py

<interfaces>
**Removal inventory** — `db.py`:
- Sync: `sqlite3.connect`, `get_db`, `get_db_closing`, `configure_sqlite_connection`, synchronous `cleanup_expired_auth_sessions`, duplicate sync maintenance wrappers enumerated in RESEARCH (#3–#4).

**Keep / relocate:** async WAL helpers, `*_async` cleanup orchestrators, pragmatic thin compatibility exports if importer graph still resolves during transition (`__init__.py` layering).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Swap cleanup_expired_auth_sessions callers to async implementation</name>
  <files>jellyswipe/__init__.py, jellyswipe/db.py, jellyswipe/bootstrap.py</files>
  <read_first>
    - jellyswipe/db.py
    - jellyswipe/__init__.py
    - jellyswipe/bootstrap.py
    - jellyswipe/db_runtime.py
  </read_first>
  <action>
Trace all references to **`cleanup_expired_auth_sessions`** (sync) and **`cleanup_expired_auth_sessions_async`**.

Prefer replacing sync entry with async coroutine awaited from FastAPI lifespan or scheduled maintenance hook already wired post–Phase 37.

If a synchronous call remains unavoidable transiently during refactor, constrain to CLI-only shim **outside** packaged runtime — default: migrate fully now.

Eliminate redundant `cleanup_orphan_swipes` vs `_async` pairs by routing through single async authoritative implementation per RESEARCH #4 inventory (extend search beyond cited files).

Update `bootstrap.py`/`__init__.py` to never import sync getters.
  </action>
  <verify>
    <automated>uv run pytest tests/test_auth.py tests/test_dependencies.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `jellyswipe/` contains no production call path that invokes a synchronous `cleanup_expired_auth_sessions` deleting tokens via `sqlite3` (prefer `cleanup_expired_auth_sessions_async` or repository async delete only)
    - `uv run pytest tests/test_auth.py tests/test_dependencies.py -q` exits 0
  </acceptance_criteria>
  <done>No production startup path invokes sync sqlite-backed session purge.</done>
</task>

<task type="auto">
  <name>Task 2: Remove sqlite3 + get_db + get_db_closing from jellyswipe package surface</name>
  <files>jellyswipe/db.py, jellyswipe/__init__.py, jellyswipe/db_runtime.py</files>
  <read_first>
    - jellyswipe/__init__.py
    - jellyswipe/db.py
    - tests/test_db.py
    - .planning/phases/40-full-migration-validation-and-sync-db-removal/40-PATTERNS.md
  </read_first>
  <action>
Delete or refactor `sqlite3` imports and synchronous connection constructors from `db.py`:

**Target absent strings (VAL-04 pre-check):**
- Module-level `sqlite3`
- Definitions `def get_db` / `@contextmanager` `get_db_closing`

Relocate permissible helpers (`ensure_sqlite_wal_mode` async variants) adjacent to runtime engine setup.

Ensure `prepare_runtime_database` sync duplicates consolidated into `_async` or removed if dead.

Finalize **module shape** (`D-10`): either fully delete file + update imports, or leave **residue module** exporting only path constants & async helpers with explicit module docstring stating “no sync DB API”.

Update `jellyswipe/__init__.py` exports to avoid star-import reviving removed symbols.
  </action>
  <verify>
    <automated>uv run pytest tests/test_db.py tests/test_infrastructure.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `rg \"^import sqlite3|^from sqlite3\" jellyswipe/` returns **empty**
    - `rg \"def get_db\\(|get_db_closing\" jellyswipe/` returns **empty**
    - `uv run pytest tests/test_db.py tests/test_infrastructure.py -q` exits 0 (tests rewritten in Plan 02 to new surface)
  </acceptance_criteria>
  <done>Application package no longer offers synchronous sqlite connection factories.</done>
</task>

<task type="auto">
  <name>Task 3: Full regression slice + document run_sync retention (D-12)</name>
  <files>jellyswipe/db_uow.py, .planning/phases/40-full-migration-validation-and-sync-db-removal/40-VALIDATION.md</files>
  <read_first>
    - jellyswipe/db_uow.py
    - .planning/phases/40-full-migration-validation-and-sync-db-removal/40-CONTEXT.md
  </read_first>
  <action>
Run broad automated regression batch after structural deletion:
`uv run pytest tests/test_routes_room.py tests/test_routes_sse.py tests/test_route_authorization.py tests/test_routes_xss.py tests/test_error_handling.py tests/test_dependencies.py -q`

If failures implicate `DatabaseUnitOfWork.run_sync`, assess PAR-04 necessity — **do not** reintroduce `sqlite3`; adjust SQLAlchemy-level sync function only.

Pre-populate `40-VALIDATION.md` **Deferred** stub row if `run_sync` must remain, citing D-12 rationale (link to `db_uow.py` excerpt). Full matrix polish happens Plan 04.
  </action>
  <verify>
    <automated>uv run pytest tests/test_routes_room.py tests/test_routes_sse.py tests/test_route_authorization.py tests/test_routes_xss.py tests/test_error_handling.py tests/test_dependencies.py -q</automated>
  </verify>
  <acceptance_criteria>
    - Above command exits 0
    - `40-VALIDATION.md` contains `run_sync` **or** explicit statement it was removed (choose one truth)
  </acceptance_criteria>
  <done>Runtime collapse verified without route/security regressions; D-12 state captured in validation artifact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| async cleanup scheduling | Incorrect ordering could delete tokens mid-request |
| module deletion | orphaned imports crashing app startup |

## STRIDE-lite Threat Register

| Threat ID | Category | Risk | Mitigation |
|-----------|----------|------|------------|
| T-40-S01 | S | Removing cleanup enables stale token reuse | Maintain PAR-01 tests + auth dependency coverage |
| T-40-D02 | Denial | startup exception if migrations path missing | `bootstrap.main` guarded flow unchanged |
| T-40-T03 | Tampering | shim module reintroduces covert sync API | VAL-04 scan upcoming Plan 04 |

_rates: MEDIUM around maintenance parity; LOW after tests green._

</threat_model>

<verification>
`uv run pytest` full suite (**VAL-03** gate — run before declaring Plan 03 complete).

</verification>

<success_criteria>
`jellyswipe/` contains no operational `sqlite3` DB connectors or sync `get_db*` helpers; startup + cleanup run through async runtime stack.

</success_criteria>

<output>
After completion, create `.planning/phases/40-full-migration-validation-and-sync-db-removal/40-03-SUMMARY.md`; update `40-VALIDATION.md` rows for Wave 03 / **ADB-03**.

</output>
