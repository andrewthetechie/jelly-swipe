---
phase: "06"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/01-configuration-startup/01-VERIFICATION.md
autonomous: true
requirements:
  - CFG-01
  - CFG-02
  - CFG-03
---

# Plan 01 — Phase 1 verification artifact (CFG-01..03) + live env smoke evidence

<objective>
Create `.planning/phases/01-configuration-startup/01-VERIFICATION.md` as the auditable verification record for **CFG-01**, **CFG-02**, and **CFG-03**, including **live runtime** startup evidence for **both** `MEDIA_PROVIDER=plex` and `MEDIA_PROVIDER=jellyfin` configurations per `06-CONTEXT.md` D-03/D-04.
</objective>

<threat_model>
| ID | Threat | Severity | Mitigation |
|----|--------|----------|------------|
| T-06-01 | Secrets pasted into verification evidence tables | high | Record only redacted excerpts; never paste `PLEX_TOKEN`, `JELLYFIN_API_KEY`, passwords, or full `Authorization` headers |
| T-06-02 | False “missing env” results due to `.env` side effects | medium | Call out `env -i` / clean shells explicitly in procedures where strict isolation matters |
</threat_model>

<tasks>

<task id="06-01-01" type="execute">
<read_first>
- .planning/phases/06-verification-closure-foundation-abstraction/06-CONTEXT.md
- .planning/phases/01-configuration-startup/01-CONTEXT.md
- .planning/REQUIREMENTS.md
- app.py
- README.md
- docker-compose.yml
</read_first>
<action>
1. Create `.planning/phases/01-configuration-startup/01-VERIFICATION.md` with YAML frontmatter (`phase: 1`, `status: draft|complete`, `verified: YYYY-MM-DD`) and a **traceability table** mapping **CFG-01/02/03** to evidence rows.
2. Document **live** startup checks:
   - **Plex mode**: `MEDIA_PROVIDER=plex` with required Plex vars present; app boots; record command used + safe output excerpt + date.
   - **Jellyfin mode**: `MEDIA_PROVIDER=jellyfin` with **no** `PLEX_URL`/`PLEX_TOKEN` required for boot per `01-CONTEXT.md` D-10; app boots with Jellyfin bundle per D-06; record command used + safe output excerpt + date.
3. Document README/compose operator expectations for **CFG-03** as **observed** checks (what you verified in the running environment), not just “README exists”.
4. If any check fails, record **FAIL** with repro steps and stop flipping any downstream requirement statuses until fixed (defer requirement updates to Plan 03 only on PASS).
</action>
<acceptance_criteria>
- `test -f .planning/phases/01-configuration-startup/01-VERIFICATION.md` exits 0.
- `grep -n "CFG-01" .planning/phases/01-configuration-startup/01-VERIFICATION.md` exits 0.
- `grep -n "CFG-02" .planning/phases/01-configuration-startup/01-VERIFICATION.md` exits 0.
- `grep -n "CFG-03" .planning/phases/01-configuration-startup/01-VERIFICATION.md` exits 0.
- `grep -n "MEDIA_PROVIDER" .planning/phases/01-configuration-startup/01-VERIFICATION.md` exits 0.
- `grep -nE "PLEX_TOKEN|JELLYFIN_API_KEY|password|Authorization:" .planning/phases/01-configuration-startup/01-VERIFICATION.md` exits **1** (no literal secret markers in file).
</acceptance_criteria>
</task>

</tasks>

<verification>
1. Re-read `01-VERIFICATION.md` and confirm each CFG row has a **dated** evidence row and a clear PASS/FAIL/PARTIAL status.
2. Confirm no secret material appears in the verification file (same grep as acceptance).
</verification>

<success_criteria>
- CFG-01/02/03 are each independently evidenced in `01-VERIFICATION.md` with live runtime notes where required by `06-CONTEXT.md`.
</success_criteria>
