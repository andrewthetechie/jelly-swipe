---
phase: "01"
plan: "02"
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - README.md
  - docker-compose.yml
autonomous: true
requirements:
  - CFG-03
---

# Plan 02 — README and compose operator docs

<objective>
Document both Plex and Jellyfin env modes in `README.md` and add compose comments for `MEDIA_PROVIDER` and Jellyfin variables, including the “two instances for both backends” operator note (CFG-03, CONTEXT D-08, D-09).
</objective>

<threat_model>
| ID | Threat | Severity | Mitigation |
|----|--------|----------|------------|
| T-03 | Example secrets committed | low | Use obvious placeholders only (`your-token-here`, `http://your-jellyfin:8096`) |
</threat_model>

<tasks>

<task id="01-02-01" type="execute">
<read_first>
- README.md
- docker-compose.yml
- .planning/PROJECT.md
- .planning/phases/01-configuration-startup/01-CONTEXT.md
</read_first>
<action>
1. In `README.md`, add a section **Media backend (Plex or Jellyfin)** after the opening intro or before **Requirements**: explain `MEDIA_PROVIDER` (`plex` default, `jellyfin`), link behavior to one backend per deployment.
2. Add a markdown **table** with columns: Variable | Required when | Description — rows must include at minimum: `MEDIA_PROVIDER`, `FLASK_SECRET`, `TMDB_API_KEY`, `PLEX_URL`, `PLEX_TOKEN`, `JELLYFIN_URL`, `JELLYFIN_API_KEY`, `JELLYFIN_USERNAME`, `JELLYFIN_PASSWORD` with clear “Plex only / Jellyfin only / always” in the middle column.
3. Add two minimal `.env` style fenced blocks: one labeled Plex mode, one labeled Jellyfin mode (non-empty values can be placeholders).
4. Add an explicit note: running Plex and Jellyfin **in one process** is not supported — operators who want both need **two instances** (align wording with `.planning/PROJECT.md`).
5. In `docker-compose.yml`, under `environment:`, add a comment line for `MEDIA_PROVIDER` and commented example lines for `JELLYFIN_URL` and credential vars pointing to README (do not remove existing Plex example lines).
</action>
<acceptance_criteria>
- `grep -q "MEDIA_PROVIDER" README.md` exits 0.
- `grep -q "two instances" README.md` exits 0 (case-insensitive acceptable via human review; prefer lowercase phrase “two instances”).
- `grep -q "JELLYFIN" docker-compose.yml` exits 0.
- `grep -q "README" docker-compose.yml` exits 0 (pointer comment contains README).
</acceptance_criteria>
</task>

</tasks>

<verification>
- `grep -q "Media backend" README.md` OR `grep -qi "jellyfin" README.md` with `grep -qi "MEDIA_PROVIDER" README.md` both exit 0.
</verification>

<success_criteria>
- CFG-03: README + compose reference list provider-specific variables and two-instance rule.
</success_criteria>
