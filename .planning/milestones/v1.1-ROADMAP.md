# Milestone archive — v1.1 Jelly Swipe rename

**Archived:** 2026-04-24  
**Status:** SHIPPED  
**Source:** Snapshot of v1.1 scope from live `.planning/ROADMAP.md` / `REQUIREMENTS.md` at close.

---

## Overview

**Milestone:** Jelly Swipe product rename and maintainer packaging under **AndrewTheTechie**, with a single upstream fork reference in `README.md` and `LICENSE` only.

**Delivery model:** No new numbered GSD phase directories under `.planning/phases/`; requirements **BRAND-01–BRAND-04** were implemented in application and docs (notably commit `9d964d4` on `main`).

## Scope (what shipped)

- Default SQLite path `data/jellyswipe.db`; legacy `kinoswipe.db` still gitignored for upgrades.
- Plex `CLIENT_ID`, `X-Plex-Product`, and Plex.tv auth URL product string → **JellySwipe** / **AndrewTheTechie** identifier.
- Jellyfin `MediaBrowser` client / device id strings → **JellySwipe**; default `JELLYFIN_DEVICE_ID` env default updated.
- `templates/index.html` and `data/index.html`: page title, heading, PWA meta, in-browser Jellyfin authorization header.
- `static/manifest.json` and `data/manifest.json`: id, names, description.
- `docker-compose.yml` service **jelly-swipe**, image **andrewthetechie/jelly-swipe:latest**; `.github/workflows/docker-image.yml` publish tag aligned.
- **LICENSE** copyright **AndrewTheTechie** + one fork line to upstream repo.
- **README** Jelly-Swipe branding, fork line, upgrade notes (DB + Plex re-auth).
- **Unraid:** `unraid_template/jelly-swipe.html` (renamed from `kino-swipe.html`).
- **CI:** `.github/workflows/release-ghcr.yml` — push to `ghcr.io/andrewthetechie/jelly-swipe` on GitHub **release** (added in commit `3a61fb8`).
- Planning mirrors: `PROJECT.md`, `STATE.md`, `MILESTONES.md`, `.planning/codebase/*`, `.cursor/rules/gsd-project.md`.

## Phases

_Not applicable — this milestone did not add `NN-name/` phase folders under `.planning/phases/`._

## Milestone summary

**Key decisions**

- Keep upstream attribution to **Bergasha/kino-swipe** only in `README.md` / `LICENSE` (and Unraid overview fork line), per product policy.

**Technical debt / follow-ups**

- `gsd-sdk query milestone.complete` still returns `version required for phases archive` in this repo; v1.1 close used the same manual archive pattern as v1.0.
- Backlog phases **999.1–999.3** remain on the live roadmap (see `.planning/ROADMAP.md`).

---

_For current project status, see `.planning/ROADMAP.md`_
