---
phase: 20-safe-dom-rendering
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - data/index.html
autonomous: true
requirements:
  - DOM-01
  - DOM-02
  - DOM-03
user_setup: []

must_haves:
  truths:
    - Match cards (m.title, m.summary) render using textContent instead of innerHTML
    - Match card images (m.thumb) use setAttribute() or property assignment instead of innerHTML
    - No innerHTML usage remains for user-controlled content in match rendering
    - Malicious scripts in match data render as literal text, not executed
  artifacts:
    - path: data/index.html
      provides: "Safe DOM construction for match cards"
      min_lines: 800
  key_links:
    - from: "openMatches() function"
      to: "matches-list DOM element"
      via: "appendChild() with createElement()"
      pattern: "createElement.*appendChild"
    - from: "createCard() function"
      to: "swipe-deck DOM element"
      via: "appendChild() with createElement()"
      pattern: "createElement.*appendChild"
---

<objective>
Refactor match card and movie card rendering in data/index.html to use safe DOM APIs (textContent, createElement, setAttribute) instead of innerHTML, preventing script injection from user-controlled data.

Purpose: Eliminate XSS vulnerability in frontend rendering by ensuring all user-controlled content (titles, summaries, actor names) is rendered using textContent, and attributes (src, alt) are set using safe DOM methods. data/index.html is a PWA-oriented copy of the main template and requires identical safe DOM construction.

Output: data/index.html with safe DOM construction for all user-controlled content rendering.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-safe-dom-rendering/20-CONTEXT.md
@.planning/REQUIREMENTS.md
@data/index.html
@.planning/phases/20-safe-dom-rendering/20-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor openMatches() function for safe DOM construction</name>
  <files>data/index.html</files>
  <action>
Replace the innerHTML-based match card rendering in the openMatches() function (lines 502-531) with safe DOM construction methods. This is the same refactoring as Task 1 in Plan 01, but applied to data/index.html.

**Current vulnerable pattern (lines 502-531):**
```javascript
data.forEach(m => {
    const card = document.createElement('div');
    card.className = 'mini-poster';
    const plexLink = `https://app.plex.tv/desktop/#!/server/${serverId}/details?key=%2Flibrary%2Fmetadata%2F${m.movie_id}`;
    const openLabel = mediaProvider === "jellyfin" ? "OPEN IN JELLYFIN" : "OPEN IN PLEX";
    card.innerHTML = `  // VULNERABLE: innerHTML with user data
        <div class="mini-inner">
            <div class="mini-front">
                <img src="${m.thumb}" alt="${m.title}">
                <div style="position:absolute; bottom:0; width:100%; background:linear-gradient(transparent, black); font-size:12px; padding:8px 4px; font-weight:bold; color:#e5a00d;">
                   ${m.title}
               </div>
            </div>
            <div class="mini-back">
                <div class="mini-title-text">${m.title}</div>
                <div class="stats-row" style="justify-content:center;">
                    ${m.rating ? `<span class="stat-badge">IMDb ${m.rating}</span>` : ''}
                    ${m.duration ? `<span class="stat-badge">${m.duration}</span>` : ''}
                    ${m.year ? `<span class="stat-badge">${m.year}</span>` : ''}
                </div>
                <a href="${plexLink}" class="plex-open-btn" target="_blank" rel="noopener noreferrer">${openLabel}</a>
                <button class="menu-btn" style="width:90%; padding:8px; font-size:0.7rem; background:#333; color:#e5a00d; border:1px solid #e5a00d; margin-top:5px;" onclick="addToWatchlist(event, '${m.movie_id}')">SAVE TO WATCHLIST</button>
                <button class="menu-btn" style="width:90%; padding:8px; font-size:0.7rem; background:#d32f2f; margin-top:5px; color:black;" onclick="deleteMatch(event, '${m.movie_id}')">DELETE</button>
            </div>
        </div>
    `;
    card.onclick = (e) => { if (!e.target.closest('a') && !e.target.closest('button')) card.classList.toggle('flipped'); };
    list.appendChild(card);
});
```

**Safe DOM construction pattern:**
```javascript
data.forEach(m => {
    const card = document.createElement('div');
    card.className = 'mini-poster';

    // Create mini-inner container
    const miniInner = document.createElement('div');
    miniInner.className = 'mini-inner';

    // Create mini-front
    const miniFront = document.createElement('div');
    miniFront.className = 'mini-front';

    // Create image with safe src assignment
    const img = document.createElement('img');
    img.src = m.thumb; // Safe: property assignment
    img.alt = 'Movie poster';
    miniFront.appendChild(img);

    // Create title overlay div
    const titleOverlay = document.createElement('div');
    titleOverlay.style.position = 'absolute';
    titleOverlay.style.bottom = '0';
    titleOverlay.style.width = '100%';
    titleOverlay.style.background = 'linear-gradient(transparent, black)';
    titleOverlay.style.fontSize = '12px';
    titleOverlay.style.padding = '8px 4px';
    titleOverlay.style.fontWeight = 'bold';
    titleOverlay.style.color = '#e5a00d';
    titleOverlay.textContent = m.title; // Safe: textContent escapes HTML
    miniFront.appendChild(titleOverlay);

    miniInner.appendChild(miniFront);

    // Create mini-back
    const miniBack = document.createElement('div');
    miniBack.className = 'mini-back';

    // Title text
    const titleText = document.createElement('div');
    titleText.className = 'mini-title-text';
    titleText.textContent = m.title; // Safe: textContent
    miniBack.appendChild(titleText);

    // Stats row
    const statsRow = document.createElement('div');
    statsRow.className = 'stats-row';
    statsRow.style.justifyContent = 'center';

    if (m.rating) {
        const ratingBadge = document.createElement('span');
        ratingBadge.className = 'stat-badge';
        ratingBadge.textContent = `IMDb ${m.rating}`;
        statsRow.appendChild(ratingBadge);
    }
    if (m.duration) {
        const durationBadge = document.createElement('span');
        durationBadge.className = 'stat-badge';
        durationBadge.textContent = m.duration;
        statsRow.appendChild(durationBadge);
    }
    if (m.year) {
        const yearBadge = document.createElement('span');
        yearBadge.className = 'stat-badge';
        yearBadge.textContent = m.year;
        statsRow.appendChild(yearBadge);
    }
    miniBack.appendChild(statsRow);

    // Open in Plex/Jellyfin button
    const plexLink = `https://app.plex.tv/desktop/#!/server/${serverId}/details?key=%2Flibrary%2Fmetadata%2F${m.movie_id}`;
    const openLabel = mediaProvider === "jellyfin" ? "OPEN IN JELLYFIN" : "OPEN IN PLEX";
    const plexBtn = document.createElement('a');
    plexBtn.href = plexLink;
    plexBtn.className = 'plex-open-btn';
    plexBtn.target = '_blank';
    plexBtn.rel = 'noopener noreferrer';
    plexBtn.textContent = openLabel;
    miniBack.appendChild(plexBtn);

    // Watchlist button
    const watchlistBtn = document.createElement('button');
    watchlistBtn.className = 'menu-btn';
    watchlistBtn.style.width = '90%';
    watchlistBtn.style.padding = '8px';
    watchlistBtn.style.fontSize = '0.7rem';
    watchlistBtn.style.background = '#333';
    watchlistBtn.style.color = '#e5a00d';
    watchlistBtn.style.border = '1px solid #e5a00d';
    watchlistBtn.style.marginTop = '5px';
    watchlistBtn.textContent = 'SAVE TO WATCHLIST';
    watchlistBtn.onclick = (event) => addToWatchlist(event, m.movie_id);
    miniBack.appendChild(watchlistBtn);

    // Delete button
    const deleteBtn = document.createElement('button');
    deleteBtn.className = 'menu-btn';
    deleteBtn.style.width = '90%';
    deleteBtn.style.padding = '8px';
    deleteBtn.style.fontSize = '0.7rem';
    deleteBtn.style.background = '#d32f2f';
    deleteBtn.style.marginTop = '5px';
    deleteBtn.style.color = 'black';
    deleteBtn.textContent = 'DELETE';
    deleteBtn.onclick = (event) => deleteMatch(event, m.movie_id);
    miniBack.appendChild(deleteBtn);

    miniInner.appendChild(miniBack);
    card.appendChild(miniInner);

    // Click handler for flip
    card.onclick = (e) => {
        if (!e.target.closest('a') && !e.target.closest('button')) {
            card.classList.toggle('flipped');
        }
    };

    list.appendChild(card);
});
```

**Key changes per D-02 (DOM-02):**
- Replace `card.innerHTML = ...` with explicit DOM construction
- Use `createElement()` for all elements
- Use `textContent` for all text content (titles, stats, button labels)
- Use property assignment (`img.src`, `img.alt`, `btn.href`) for attributes
- Maintain exact same visual output and functionality

**What to preserve:**
- All CSS classes and inline styles
- All event handlers (onclick)
- The flip behavior on card click
- Conditional rendering for rating/duration/year badges
</action>
<verify>
<automated>python -c "import re; html = open('data/index.html').read(); pattern = r'card\.innerHTML\s*=\s*`[^`]*\$\{m\.(title|thumb|summary|movie_id)'; matches = re.findall(pattern, html, re.MULTILINE); print('VULNERABLE patterns found:' if matches else 'No vulnerable patterns'); [print(f'  Line {html[:html.find(m)].count(chr(10)) + 1}: {m[:80]}...') for m in matches if m]; exit(1 if matches else 0)"</automated>
</verify>
<done>
The openMatches() function in data/index.html constructs match cards using safe DOM APIs. All user-controlled data (m.title, m.thumb) is rendered using textContent or safe property assignment. No innerHTML remains for user-controlled content in match rendering.
</done>
</task>

<task type="auto">
  <name>Task 2: Refactor createCard() and watchTrailer() for safe DOM construction</name>
  <files>data/index.html</files>
  <action>
Replace innerHTML-based DOM construction in createCard() function (lines 582-635) and watchTrailer() function (lines 637-655) with safe DOM construction methods. This is the same refactoring as Task 2 in Plan 01, but applied to data/index.html.

**Part A: createCard() refactoring (lines 586-603)**

Current vulnerable pattern (lines 586-603):
```javascript
c.innerHTML = `  // VULNERABLE: innerHTML with user data
    <div class="card-inner">
        <div class="card-front"><img src="${m.thumb}"></div>
        <div class="card-back">
            <div class="movie-title">${m.title}</div>
            <div class="stats-row">
                ${m.rating ? `<span class="stat-badge">IMDb ${m.rating}</span>` : ''}
                ${m.duration ? `<span class="stat-badge">${m.duration}</span>` : ''}
                ${m.year ? `<span class="stat-badge">${m.year}</span>` : ''}
            </div>
            <div id="vid-${m.id}" class="trailer-box"></div>
            <button class="trailer-btn" onclick="watchTrailer(event, '${m.id}', this)">WATCH TRAILER</button>
            <div class="back-content"><p>${m.summary || 'No description available.'}</p></div>
            <div id="cast-${m.id}" class="cast-row"></div>
            <div style="font-size:0.75rem; color:#e5a00d; text-align:center; margin-top: auto; padding-bottom:10px;">Tap to flip back</div>
        </div>
    </div>
`;
```

Safe DOM construction:
```javascript
// Create card-inner
const cardInner = document.createElement('div');
cardInner.className = 'card-inner';

// Create card-front
const cardFront = document.createElement('div');
cardFront.className = 'card-front';
const frontImg = document.createElement('img');
frontImg.src = m.thumb; // Safe: property assignment
frontImg.alt = 'Movie poster';
cardFront.appendChild(frontImg);
cardInner.appendChild(cardFront);

// Create card-back
const cardBack = document.createElement('div');
cardBack.className = 'card-back';

// Movie title
const movieTitle = document.createElement('div');
movieTitle.className = 'movie-title';
movieTitle.textContent = m.title; // Safe: textContent per D-01 (DOM-01)
cardBack.appendChild(movieTitle);

// Stats row
const statsRow = document.createElement('div');
statsRow.className = 'stats-row';

if (m.rating) {
    const ratingBadge = document.createElement('span');
    ratingBadge.className = 'stat-badge';
    ratingBadge.textContent = `IMDb ${m.rating}`;
    statsRow.appendChild(ratingBadge);
}
if (m.duration) {
    const durationBadge = document.createElement('span');
    durationBadge.className = 'stat-badge';
    durationBadge.textContent = m.duration;
    statsRow.appendChild(durationBadge);
}
if (m.year) {
    const yearBadge = document.createElement('span');
    yearBadge.className = 'stat-badge';
    yearBadge.textContent = m.year;
    statsRow.appendChild(yearBadge);
}
cardBack.appendChild(statsRow);

// Trailer box (empty container)
const trailerBox = document.createElement('div');
trailerBox.id = `vid-${m.id}`;
trailerBox.className = 'trailer-box';
cardBack.appendChild(trailerBox);

// Trailer button
const trailerBtn = document.createElement('button');
trailerBtn.className = 'trailer-btn';
trailerBtn.textContent = 'WATCH TRAILER';
trailerBtn.onclick = (event) => watchTrailer(event, m.id, trailerBtn);
cardBack.appendChild(trailerBtn);

// Back content with summary
const backContent = document.createElement('div');
backContent.className = 'back-content';
const summaryP = document.createElement('p');
summaryP.textContent = m.summary || 'No description available.'; // Safe: textContent per D-01 (DOM-01)
backContent.appendChild(summaryP);
cardBack.appendChild(backContent);

// Cast row (empty container, populated later)
const castRow = document.createElement('div');
castRow.id = `cast-${m.id}`;
castRow.className = 'cast-row';
cardBack.appendChild(castRow);

// Tap to flip instruction
const flipHint = document.createElement('div');
flipHint.style.fontSize = '0.75rem';
flipHint.style.color = '#e5a00d';
flipHint.style.textAlign = 'center';
flipHint.style.marginTop = 'auto';
flipHint.style.paddingBottom = '10px';
flipHint.textContent = 'Tap to flip back';
cardBack.appendChild(flipHint);

cardInner.appendChild(cardBack);
c.appendChild(cardInner);
```

**Part B: Cast loading refactoring (lines 608-633 inside click handler)**

Current vulnerable pattern (lines 616-623):
```javascript
castEl.innerHTML = data.cast.map(actor => `  // VULNERABLE: innerHTML with actor.name
    <div class="cast-member">
        ${actor.profile_path
            ? `<img src="${actor.profile_path}" alt="${actor.name}" loading="lazy">`
            : `<div class="no-photo">🎬</div>`}
        <span>${actor.name}</span>
    </div>
`).join('');
```

Safe DOM construction:
```javascript
if (data.cast && data.cast.length > 0) {
    // Clear loading text
    castEl.textContent = '';

    data.cast.forEach(actor => {
        const castMember = document.createElement('div');
        castMember.className = 'cast-member';

        if (actor.profile_path) {
            const actorImg = document.createElement('img');
            actorImg.src = actor.profile_path; // Safe: property assignment
            actorImg.alt = actor.name; // Safe: alt property
            actorImg.loading = 'lazy';
            castMember.appendChild(actorImg);
        } else {
            const noPhoto = document.createElement('div');
            noPhoto.className = 'no-photo';
            noPhoto.textContent = '🎬';
            castMember.appendChild(noPhoto);
        }

        const actorName = document.createElement('span');
        actorName.textContent = actor.name; // Safe: textContent per D-01 (DOM-01)
        castMember.appendChild(actorName);

        castEl.appendChild(castMember);
    });
} else {
    castEl.textContent = '';
}
```

**Part C: watchTrailer() refactoring (lines 651)**

Current vulnerable pattern (line 651):
```javascript
container.innerHTML = `<iframe src="https://www.youtube.com/embed/${data.youtube_key}?autoplay=1&playsinline=1" allow="autoplay; encrypted-media" allowfullscreen></iframe>`;
```

Safe DOM construction per D-05:
```javascript
const iframe = document.createElement('iframe');
iframe.src = `https://www.youtube.com/embed/${data.youtube_key}?autoplay=1&playsinline=1`; // Safe: property assignment
iframe.allow = 'autoplay; encrypted-media';
iframe.allowFullscreen = true;
container.appendChild(iframe);
```

**Key changes:**
- Replace all `innerHTML` assignments with explicit DOM construction
- Use `textContent` for all text content (titles, summaries, actor names, button labels)
- Use property assignment for attributes (src, alt, href, id, className)
- Maintain exact same visual output and functionality
- Preserve all event handlers and onclick bindings

**What to preserve:**
- All CSS classes and inline styles
- The flip behavior on card click
- Lazy loading for actor images
- Conditional rendering for rating/duration/year badges
- Trailer loading/unloading behavior
</action>
<verify>
<automated>python -c "
import re
html = open('data/index.html').read()

# Check for innerHTML with user data patterns
patterns = [
    (r'c\.innerHTML\s*=\s*`[^`]*\$\{m\.(title|thumb|summary|movie_id)', 'createCard innerHTML'),
    (r'castEl\.innerHTML\s*=\s*data\.cast\.map\(actor', 'cast innerHTML'),
    (r'container\.innerHTML\s*=\s*`[^`]*\$\{data\.youtube_key}', 'trailer innerHTML'),
]

vulnerable_found = False
for pattern, desc in patterns:
    matches = re.findall(pattern, html, re.MULTILINE | re.DOTALL)
    if matches:
        print(f'VULNERABLE {desc} found:')
        for m in matches[:3]:  # Show first 3 matches
            print(f'  {m[:100]}...')
        vulnerable_found = True

if not vulnerable_found:
    print('No vulnerable innerHTML patterns with user data found')
    exit(0)
else:
    exit(1)
"</automated>
</verify>
<done>
The createCard() function in data/index.html constructs movie cards using safe DOM APIs. All user-controlled content (m.title, m.summary, actor.name, actor.profile_path) is rendered using textContent or safe property assignment. The watchTrailer() function uses safe property assignment for iframe src. Cast loading uses safe DOM construction. No innerHTML remains for user-controlled content in movie card rendering.
</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Server → Client | Movie/actor/cast data from Jellyfin API (now trusted per Phase 19) |
| Browser DOM | Rendering of user-controlled content in innerHTML (vulnerability being fixed) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-06 | I (Info Disclosure) | innerHTML with m.title | mitigate | Replace innerHTML with textContent (Task 1, Task 2) |
| T-20-07 | I (Info Disclosure) | innerHTML with m.summary | mitigate | Replace innerHTML with textContent (Task 2) |
| T-20-08 | I (Info Disclosure) | innerHTML with actor.name | mitigate | Replace innerHTML with textContent (Task 2) |
| T-20-09 | I (Info Disclosure) | innerHTML with m.thumb URLs | mitigate | Replace innerHTML with img.src property assignment (Task 1, Task 2) |
| T-20-10 | E (Elevation of Privilege) | Script injection via movie data | mitigate | textContent prevents script execution (Tasks 1-2) |

**Note:** data/index.html is a PWA-oriented copy of the main template and requires identical security fixes. This plan ensures defense-in-depth across all frontend entry points.
</threat_model>

<verification>
Overall phase checks:
- Verify no innerHTML assignments contain template literal interpolations with user data
- Verify all text content uses textContent property
- Verify all attributes use setAttribute() or direct property assignment
- Manual test: Add `<script>alert('XSS')</script>` to a movie title in database and verify it renders as literal text
</verification>

<success_criteria>
1. Movie titles, summaries, actor names, and character names are rendered using textContent (not innerHTML)
2. Image sources and movie IDs are set using setAttribute() or DOM property assignment (not innerHTML)
3. All innerHTML usages for user-controlled content have been removed or refactored to safe DOM construction
4. Malicious script tags in movie data render as literal text in the browser (not executed)
</success_criteria>

<output>
After completion, create `.planning/phases/20-safe-dom-rendering/20-02-SUMMARY.md`
</output>
