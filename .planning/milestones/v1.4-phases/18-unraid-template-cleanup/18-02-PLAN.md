---
phase: "18"
plan: "02"
type: execute
wave: 2
depends_on:
  - "18-01"
files_modified:
  - .github/workflows/unraid-template-lint.yml
  - scripts/lint-unraid-template.py
autonomous: true
requirements:
  - CI-01
must_haves:
  truths:
    - "GitHub Actions workflow exists and triggers on template changes"
    - "Python script parses Unraid template XML and extracts variables"
    - "Script compares variables against recognized set and fails on unknown variables"
    - "Workflow blocks PR merge when lint fails"
    - "Validation passes with current Jellyfin template"
  artifacts:
    - path: ".github/workflows/unraid-template-lint.yml"
      provides: "CI workflow for template validation"
      contains: "Unraid Template Lint, push/PR triggers"
    - path: "scripts/lint-unraid-template.py"
      provides: "Python lint script for Unraid templates"
      exports: ["extract_variables", "main"]
  key_links:
    - from: ".github/workflows/unraid-template-lint.yml"
      to: "scripts/lint-unraid-template.py"
      via: "Workflow runs script"
      pattern: "python scripts/lint-unraid-template.py"
    - from: "scripts/lint-unraid-template.py"
      to: "unraid_template/jelly-swipe.html"
      via: "XML parsing"
      pattern: "xml.etree.ElementTree"
---

# Plan 02 — Create CI lint workflow to validate Unraid template variables

<objective>
Create a GitHub Actions workflow and Python script that lint the Unraid template to verify all environment variables are a strict subset of recognized app env vars, blocking PRs on invalid templates (CI-01, CONTEXT D-03).
</objective>

<threat_model>
| ID | Threat | Severity | Mitigation |
|----|--------|----------|------------|
| T-18-03 | Lint script allows unknown variables, defeating validation purpose | high | Script must fail with exit code 1 when unknown variables detected; fail-fast in workflow |
| T-18-04 | Lint script syntax errors prevent PR validation | low | Test script locally with `python scripts/lint-unraid-template.py unraid_template/jelly-swipe.html` before committing |
</threat_model>

<must_haves>
- GitHub Actions workflow file `.github/workflows/unraid-template-lint.yml` exists
- Workflow triggers on push and pull_request for `unraid_template/jelly-swipe.html`
- Python script `scripts/lint-unraid-template.py` exists and parses the Unraid template XML
- Script extracts all variable names from both `<Variable>` and `<Config>` sections
- Script compares against recognized set: `JELLYFIN_URL`, `JELLYFIN_API_KEY`, `TMDB_API_KEY`, `FLASK_SECRET`, `DB_PATH`, `JELLYFIN_DEVICE_ID`
- Script fails (exit code 1) if any unknown variable is found
- Workflow fails if script fails, blocking PR merge
- Success (exit code 0) allows merge to proceed
</must_haves>

<tasks>

<task type="auto">
  <name>Task 1: Create CI lint workflow and Python script for template validation</name>
  <files>.github/workflows/unraid-template-lint.yml, scripts/lint-unraid-template.py</files>
  <action>
Read .github/workflows/test.yml, unraid_template/jelly-swipe.html, and .planning/phases/18-unraid-template-cleanup/18-CONTEXT.md to understand workflow patterns and template structure.

1. Create directory scripts/ if it doesn't exist
2. Create file scripts/lint-unraid-template.py with this Python script:
   #!/usr/bin/env python3
   """
   Lint Unraid template to verify environment variables are a strict subset of
   recognized app env vars. Fails with exit code 1 if unknown variables found.
   """

   import sys
   import xml.etree.ElementTree as ET
   from pathlib import Path

   # Recognized environment variables that the app expects
   RECOGNIZED_VARS = {
       "JELLYFIN_URL",
       "JELLYFIN_API_KEY",
       "JELLYFIN_USERNAME",
       "JELLYFIN_PASSWORD",
       "TMDB_API_KEY",
       "FLASK_SECRET",
       "DB_PATH",
       "JELLYFIN_DEVICE_ID",
   }

   def extract_variables(template_path: Path) -> set[str]:
       """Extract all variable names from <Variable> and <Config> sections."""
       tree = ET.parse(template_path)
       root = tree.getroot()

       variables = set()

       # Extract from <Variable> sections (legacy format)
       for variable in root.findall(".//Variable"):
           name_elem = variable.find("Name")
           if name_elem is not None and name_elem.text:
               variables.add(name_elem.text.strip())

       # Extract from <Config> sections (modern format)
       for config in root.findall(".//Config"):
           name_attr = config.get("Name")
           if name_attr:
               variables.add(name_attr)

       return variables

   def main():
       if len(sys.argv) != 2:
           print("Usage: lint-unraid-template.py <template-file>")
           sys.exit(1)

       template_path = Path(sys.argv[1])
       if not template_path.exists():
           print(f"Error: Template file not found: {template_path}")
           sys.exit(1)

       template_vars = extract_variables(template_path)
       unknown_vars = template_vars - RECOGNIZED_VARS

       if unknown_vars:
           print(f"❌ Unraid template validation failed")
           print(f"Unknown environment variables found: {sorted(unknown_vars)}")
           print(f"Recognized variables: {sorted(RECOGNIZED_VARS)}")
           sys.exit(1)

       print(f"✓ Unraid template validation passed")
       print(f"All {len(template_vars)} variables are recognized")
       sys.exit(0)

   if __name__ == "__main__":
       main()
3. Make the script executable: chmod +x scripts/lint-unraid-template.py
4. Create file .github/workflows/unraid-template-lint.yml with this workflow:
   name: Unraid Template Lint

   on:
     push:
       paths:
         - 'unraid_template/jelly-swipe.html'
     pull_request:
       paths:
         - 'unraid_template/jelly-swipe.html'

   jobs:
     lint:
       runs-on: ubuntu-latest
       timeout-minutes: 5
       steps:
         - name: Checkout code
           uses: actions/checkout@v4

         - name: Set up Python 3.13
           uses: actions/setup-python@v5
           with:
             python-version: '3.13'

         - name: Lint Unraid template
           run: python scripts/lint-unraid-template.py unraid_template/jelly-swipe.html
5. Test the script locally: python scripts/lint-unraid-template.py unraid_template/jelly-swipe.html (should exit 0)
</action>
  <verify>
test -f scripts/lint-unraid-template.py && \
test -x scripts/lint-unraid-template.py && \
test -f .github/workflows/unraid-template-lint.yml && \
grep -q "Unraid Template Lint" .github/workflows/unraid-template-lint.yml && \
grep -q "unraid_template/jelly-swipe.html" .github/workflows/unraid-template-lint.yml && \
grep -q "RECOGNIZED_VARS" scripts/lint-unraid-template.py && \
python scripts/lint-unraid-template.py unraid_template/jelly-swipe.html && \
grep -q "extract_variables\|xml.etree.ElementTree" scripts/lint-unraid-template.py
  </verify>
  <done>
GitHub Actions workflow exists and triggers on template changes; Python script parses Unraid template XML and extracts variables; script compares variables against recognized set and fails on unknown variables; workflow blocks PR merge when lint fails; validation passes with current Jellyfin template
  </done>
</task>

</tasks>

<verification>
- `python scripts/lint-unraid-template.py unraid_template/jelly-swipe.html` exits 0.
- `yamllint .github/workflows/unraid-template-lint.yml` exits 0 (if yamllint is available).
- `grep -c "on:" .github/workflows/unraid-template-lint.yml` is 1 (workflow has trigger).
- `grep -c "runs-on:" .github/workflows/unraid-template-lint.yml` is 1 (workflow has job).
</verification>

<success_criteria>
- CI-01: CI workflow lints Unraid template to verify variables are a strict subset of recognized app env vars
</success_criteria>

<output>
After completion, create `.planning/phases/18-unraid-template-cleanup/18-02-SUMMARY.md`
</output>
