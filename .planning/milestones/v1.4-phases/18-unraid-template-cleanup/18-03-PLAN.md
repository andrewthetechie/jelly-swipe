---
phase: "18"
plan: "03"
type: execute
wave: 3
depends_on:
  - "18-01"
  - "18-02"
files_modified:
  - README.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "README contains Unraid Template section"
    - "Documentation includes template file location"
    - "Documentation lists required environment variables"
    - "Documentation mentions CI validation workflow"
    - "No outdated Plex references remain in README"
  artifacts:
    - path: "README.md"
      provides: "Documentation for Unraid template usage"
      contains: "Unraid Template, JELLYFIN_URL, JELLYFIN_API_KEY, CI validation"
  key_links:
    - from: "README.md"
      to: "unraid_template/jelly-swipe.html"
      via: "File path reference"
      pattern: "unraid_template/jelly-swipe.html"
    - from: "README.md"
      to: ".github/workflows/unraid-template-lint.yml"
      via: "CI workflow reference"
      pattern: "CI validation|lint"
---

# Plan 03 — Document Unraid template and CI validation in README

<objective>
Add README section documenting the Unraid template variables and CI validation step, helping operators understand the template structure and how to maintain it correctly (CONTEXT D-03, project documentation best practices).
</objective>

<threat_model>
| ID | Threat | Severity | Mitigation |
|----|--------|----------|------------|
| T-18-05 | Documentation becomes stale if template changes | low | Document the validation workflow, not specific variable names; CI will catch drift automatically |
| T-18-06 | Missing documentation for new users | low | Add clear section with template file location and variable requirements |
</threat_model>

<must_haves>
- README contains section about Unraid template
- Documentation includes template file location (`unraid_template/jelly-swipe.html`)
- Documentation lists required environment variables (JELLYFIN_URL, JELLYFIN_API_KEY, TMDB_API_KEY, FLASK_SECRET)
- Documentation mentions CI validation workflow prevents template drift
- Documentation notes that all variables must be recognized by the lint script
</must_haves>

<tasks>

<task type="auto">
  <name>Task 1: Add Unraid Template documentation to README</name>
  <files>README.md</files>
  <action>
Read README.md, unraid_template/jelly-swipe.html, .planning/phases/18-unraid-template-cleanup/18-CONTEXT.md, and .github/workflows/unraid-template-lint.yml to understand current documentation and CI setup.

1. In README.md, locate the section about deployment or installation
2. Add a new subsection titled "Unraid Template" after main deployment instructions or as standalone section before "Requirements"
3. In this new section, include:
   - Note that Unraid template is provided at unraid_template/jelly-swipe.html
   - Required environment variables: JELLYFIN_URL, JELLYFIN_API_KEY, TMDB_API_KEY, FLASK_SECRET
   - Brief description that template uses Jellyfin API key authentication (username/password not exposed in template)
   - Note that all masked fields are blank by default and require user-provided values
   - Mention that CI validation ensures template variables remain subset of recognized app env vars
   - Reference to CI workflow .github/workflows/unraid-template-lint.yml for details
4. Ensure documentation flows naturally with existing sections and doesn't duplicate information
5. Update any outdated references to Plex variables in README (search for PLEX_URL or PLEX_TOKEN and replace with Jellyfin equivalents if found)
</action>
  <verify>
grep -q "Unraid Template" README.md && \
grep -q "unraid_template/jelly-swipe.html" README.md && \
grep -q "JELLYFIN_URL" README.md && \
grep -q "JELLYFIN_API_KEY" README.md && \
grep -q "CI validation\|CI workflow\|lint" README.md && \
! grep "PLEX_URL\|PLEX_TOKEN" README.md
  </verify>
  <done>
README contains Unraid Template section; documentation includes template file location; documentation lists required environment variables; documentation mentions CI validation workflow; no outdated Plex references remain in README
  </done>
</task>

</tasks>

<verification>
- `grep -c "Unraid" README.md` is at least 2 (appears in title and description).
- `grep -A 10 "Unraid Template" README.md | grep -c "JELLYFIN"` is at least 2 (both URL and API_KEY mentioned).
- `grep -A 10 "Unraid Template" README.md | grep -q "blank\|empty"` exits 0 (mentions default blank values).
</verification>

<success_criteria>
- Operators can locate and understand the Unraid template from README
- Operators know which variables are required
- Operators understand that CI validation prevents template drift
</success_criteria>

<output>
After completion, create `.planning/phases/18-unraid-template-cleanup/18-03-SUMMARY.md`
</output>
