---
phase: 20
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_route_authorization.py
  - tests/conftest.py
autonomous: true
requirements:
  - VER-01
  - VER-02
  - VER-03
user_setup: []
must_haves:
  truths:
    - "Spoofed alias headers are rejected with 401 unauthorized across all protected routes"
    - "Request-body user_id injection cannot create cross-user write side effects"
    - "Delegate and token valid identity flows remain functional after auth hardening"
  artifacts:
    - path: "tests/test_route_authorization.py"
      provides: "Route-level security regression matrix for VER-01/VER-02/VER-03"
      min_lines: 200
    - path: "tests/conftest.py"
      provides: "Reusable fixtures for route client and identity-path setup (if required)"
      min_lines: 100
  key_links:
    - from: "tests/test_route_authorization.py"
      to: "jellyswipe/__init__.py"
      via: "protected route behavior assertions"
      pattern: "401 unauthorized + valid identity path compatibility"
---

<objective>
Deliver a route-level regression suite that proves authorization hardening resists spoofing/injection attacks while preserving legitimate delegate and token-based access.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-security-regression-tests/20-CONTEXT.md
@.planning/phases/20-security-regression-tests/20-RESEARCH.md
@.planning/phases/20-security-regression-tests/20-VALIDATION.md
@.planning/phases/20-security-regression-tests/20-PATTERNS.md
@jellyswipe/__init__.py
@tests/conftest.py
</context>

<tasks>

<task id="20-01-01" type="execute">
<read_first>
- jellyswipe/__init__.py
- tests/conftest.py
- .planning/phases/20-security-regression-tests/20-CONTEXT.md
- .planning/phases/20-security-regression-tests/20-RESEARCH.md
</read_first>
<action>
Create route-test harness support for protected endpoint security regression coverage:
1. Add `tests/test_route_authorization.py` with Flask `test_client` setup.
2. Add or extend fixtures needed for active room/session setup and provider behavior patching.
3. Ensure fixtures avoid `mocker` dependency; use `monkeypatch` and `unittest.mock`.
4. Include reusable helper assertions for unauthorized response contract (`401` + `{"error":"Unauthorized"}`).
</action>
<verify>
<automated>python -m py_compile tests/conftest.py tests/test_route_authorization.py</automated>
<automated>rg "test_client|Unauthorized|401" tests/test_route_authorization.py</automated>
</verify>
<acceptance_criteria>
- `tests/test_route_authorization.py` exists and uses Flask `test_client`.
- Test fixtures rely on `monkeypatch`/`unittest.mock` (no required `mocker` fixture).
- Unauthorized assertion helper (or repeated assertion pattern) checks exact `401` and JSON payload.
- `python -m py_compile tests/conftest.py tests/test_route_authorization.py` exits 0.
</acceptance_criteria>
</task>

<task id="20-01-02" type="execute">
<read_first>
- tests/test_route_authorization.py
- jellyswipe/__init__.py
- .planning/phases/20-security-regression-tests/20-CONTEXT.md
</read_first>
<action>
Implement negative-path security matrix coverage:
1. Parametrize spoofed alias header tests across protected routes: `/room/swipe`, `/matches`, `/matches/delete`, `/undo`, `/watchlist/add`.
2. Assert each spoofed-header request returns exact unauthorized contract.
3. Add `/room/swipe` body `user_id` injection tests that assert unauthorized response.
4. Add DB side-effect checks ensuring injected `user_id` does not create cross-user swipe/match writes.
</action>
<verify>
<automated>pytest -q tests/test_route_authorization.py -k "spoof or injection"</automated>
<automated>rg "X-Provider-User-Id|X-Jellyfin-User-Id|X-Emby-UserId" tests/test_route_authorization.py</automated>
</verify>
<acceptance_criteria>
- Spoofed-header test matrix covers all 3 alias headers and all protected routes in scope.
- Injection test(s) for `/room/swipe` assert `401` + unauthorized payload.
- Injection test(s) verify no cross-user DB side effects for unauthorized body `user_id`.
- `pytest -q tests/test_route_authorization.py -k "spoof or injection"` exits 0.
</acceptance_criteria>
</task>

<task id="20-01-03" type="execute">
<read_first>
- tests/test_route_authorization.py
- jellyswipe/__init__.py
- .planning/phases/20-security-regression-tests/20-CONTEXT.md
- .planning/REQUIREMENTS.md
</read_first>
<action>
Implement valid-flow regression coverage:
1. Add delegate-path happy tests across all protected routes.
2. Add token-path happy tests across all protected routes.
3. Assert each route returns non-401 expected behavior for valid identity context.
4. Run targeted route-security suite and full pytest suite; document known external fixture blocker if full suite still fails outside Phase 20 files.
</action>
<verify>
<automated>pytest -q tests/test_route_authorization.py -k "valid or delegate or token"</automated>
<automated>pytest -q</automated>
</verify>
<acceptance_criteria>
- Delegate and token valid-flow tests exist for each protected route in scope.
- Valid-flow tests prove legitimate requests are not blocked by Phase 18/19 hardening.
- `pytest -q tests/test_route_authorization.py` exits 0.
- Full `pytest -q` result is captured; if failure occurs, failure source is explicitly documented in summary.
</acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> API | Requests may include spoofed identity headers or injected body user identifiers |
| API auth helper -> route handlers | Verified identity output determines authorization and data scope |
| route handlers -> DB | Unauthorized requests must not create/modify user-scoped data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-01 | S | Alias header spoofing | mitigate | Matrix tests across all protected routes assert strict unauthorized |
| T-20-02 | T | Body user_id injection | mitigate | Injection tests assert unauthorized and no DB side effects |
| T-20-03 | D | Security regressions from future changes | mitigate | Route-level regression suite runs in CI/local pytest workflow |
| T-20-04 | E | Legitimate flow breakage after hardening | mitigate | Delegate/token happy-path tests across all protected routes |
</threat_model>

<verification>
After completion:
1. Route security tests exist and cover spoofed-header, injection, and valid-flow scenarios.
2. `tests/test_route_authorization.py` targeted suite passes.
3. Full `pytest -q` outcome is captured with clear blocker attribution if non-phase failures remain.
4. Requirements `VER-01`, `VER-02`, and `VER-03` are demonstrably satisfied by automated tests.
</verification>

<success_criteria>
1. VER-01 satisfied: alias header spoof attempts are covered and rejected.
2. VER-02 satisfied: body `user_id` injection cannot bypass identity or create cross-user effects.
3. VER-03 satisfied: delegate/token valid flows continue functioning across protected routes.
4. Security regression suite is maintainable, route-level, and ready for ongoing CI enforcement.
</success_criteria>

<output>
After completion, create `.planning/phases/20-security-regression-tests/20-01-SUMMARY.md`.
</output>
