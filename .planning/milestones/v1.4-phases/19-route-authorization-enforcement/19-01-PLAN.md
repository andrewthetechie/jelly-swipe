---
phase: 19
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - jellyswipe/__init__.py
  - .planning/STATE.md
autonomous: true
requirements:
  - SEC-03
  - SEC-04
  - SEC-05
user_setup: []
must_haves:
  truths:
    - "/room/swipe never accepts request-body user_id as identity source"
    - "All protected routes in scope return 401 with {'error':'Unauthorized'} when identity cannot be verified"
    - "User-scoped data operations remain filtered by verified identity"
    - "Identity rejection internals are not exposed to clients"
  artifacts:
    - path: "jellyswipe/__init__.py"
      provides: "Strict route authorization enforcement and uniform unauthorized contract"
      min_lines: 350
  key_links:
    - from: "jellyswipe/__init__.py"
      to: ".planning/phases/19-route-authorization-enforcement/19-CONTEXT.md"
      via: "D-01..D-07 decisions"
      pattern: "verified identity only"
---

<objective>
Enforce Phase 19 route authorization decisions by standardizing unauthorized behavior and ensuring all user-scoped route operations use verified identity only.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-route-authorization-enforcement/19-CONTEXT.md
@.planning/phases/19-route-authorization-enforcement/19-RESEARCH.md
@.planning/phases/19-route-authorization-enforcement/19-VALIDATION.md
@.planning/phases/19-route-authorization-enforcement/19-PATTERNS.md
@jellyswipe/__init__.py
</context>

<tasks>

<task id="19-01-01" type="execute">
<read_first>
- jellyswipe/__init__.py
- .planning/phases/19-route-authorization-enforcement/19-CONTEXT.md
- .planning/phases/19-route-authorization-enforcement/19-RESEARCH.md
</read_first>
<action>
Implement shared unauthorized response handling and route-level verified identity gating in `jellyswipe/__init__.py`:
1. Add helper that returns exactly `jsonify({'error': 'Unauthorized'}), 401`.
2. Update `/room/swipe`, `/matches`, `/matches/delete`, `/undo`, and `/watchlist/add` to use verified identity checks before route business logic.
3. Ensure `/matches` no longer returns `[]` for unauthorized requests.
4. Keep identity rejection reasons server-side only; do not include `_identity_rejection_reason()` output in responses.
</action>
<verify>
<automated>python -m py_compile jellyswipe/__init__.py</automated>
<automated>rg "return jsonify\\(\\{'error': 'Unauthorized'\\}\\), 401" jellyswipe/__init__.py</automated>
</verify>
<acceptance_criteria>
- `jellyswipe/__init__.py` contains a shared unauthorized response helper that returns `{'error': 'Unauthorized'}` with HTTP `401`.
- `/matches` unauthorized branch returns `401`, not an empty array.
- `/matches/delete` and `/undo` unauthorized branches return `401`, not `400`.
- `python -m py_compile jellyswipe/__init__.py` exits with status 0.
</acceptance_criteria>
</task>

<task id="19-01-02" type="execute">
<read_first>
- jellyswipe/__init__.py
- .planning/phases/19-route-authorization-enforcement/19-CONTEXT.md
- .planning/REQUIREMENTS.md
</read_first>
<action>
Remove request-body identity fallback and enforce verified identity data scoping:
1. In `/room/swipe`, remove use of request-body `user_id` as identity fallback.
2. Keep match insert/query/delete operations scoped by verified identity where identity is used.
3. Preserve existing room/session behavior (`active_room`, `my_user_id`) while ensuring provider identity controls user-scoped authorization and writes.
4. Run static checks for remaining identity fallback patterns.
</action>
<verify>
<automated>rg "user_id = _provider_user_id_from_request\\(\\)" jellyswipe/__init__.py</automated>
<automated>rg "data.get\\('user_id'\\)" jellyswipe/__init__.py</automated>
</verify>
<acceptance_criteria>
- `/room/swipe` identity assignment uses `_provider_user_id_from_request()` directly and does not fallback to body `user_id`.
- No protected-route logic uses `data.get('user_id')` to establish requester identity.
- User-scoped `matches` operations retain verified identity filters (`user_id = ?` bound from verified identity).
</acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> API | Request headers/body are untrusted and may attempt identity spoofing |
| API -> Jellyfin | Trusted server-side identity resolution path via delegated identity/token validation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-01 | S | Body/header identity spoofing | mitigate | Use verified identity helper only; remove body fallback |
| T-19-02 | T | Mixed unauthorized semantics | mitigate | Standardize protected routes to exact `401` contract |
| T-19-03 | I | Internal auth reason disclosure | mitigate | Keep rejection details server-side only |
| T-19-04 | E | Cross-user data access | mitigate | Keep DB operations scoped to verified identity |
</threat_model>

<verification>
After completion:
1. `python -m py_compile jellyswipe/__init__.py` passes.
2. All protected routes in scope return `401` + `{'error': 'Unauthorized'}` when identity is missing/unverified.
3. `/room/swipe` no longer uses request-body `user_id` as identity fallback.
4. User-scoped DB operations remain tied to verified identity.
</verification>

<success_criteria>
1. SEC-03 satisfied: `/room/swipe` does not accept request-body `user_id` as identity.
2. SEC-04 satisfied: protected routes return strict `401` when identity cannot be verified.
3. SEC-05 satisfied: user-scoped operations remain restricted to verified identity.
4. Plan is executable without ambiguity and ready for `/gsd-execute-phase 19`.
</success_criteria>

<output>
After completion, create `.planning/phases/19-route-authorization-enforcement/19-01-SUMMARY.md`.
</output>
