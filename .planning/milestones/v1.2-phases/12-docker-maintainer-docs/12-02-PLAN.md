---
phase: 12-docker-maintainer-docs
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [README.md]
autonomous: true
requirements: [DOC-01, DIST-01]
user_setup: []

must_haves:
  truths:
    - "README.md contains a new 'Development' section after 'Deployment'"
    - "Development section documents uv commands for local setup"
    - "Python 3.13 requirement is mentioned"
    - "No documentation implies pip install jellyswipe from PyPI"
  artifacts:
    - path: "README.md"
      provides: "Maintainer/contributor onboarding documentation with uv workflow"
      min_lines: 220 (adds ~15 lines for Development section)
  key_links:
    - from: "README.md Development section"
      to: "local development workflow"
      via: "uv sync, uv run commands"
      pattern: "uv sync|uv run"
    - from: "README.md Deployment section"
      to: "operator documentation"
      via: "Docker instructions remain in Deployment"
      pattern: "## Deployment"
---

<objective>
Add Development section to README.md documenting uv-based local development workflow for maintainers and contributors.

Purpose: Provide clear, uv-specific documentation for setting up the development environment, running the app locally, and managing dependencies. This separates operator-facing (Deployment) from maintainer-facing (Development) concerns.

Output: README.md with new Development section covering uv sync, uv run, uv add, uv lock --upgrade, and Python 3.13 requirement, with no PyPI references.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-docker-maintainer-docs/12-CONTEXT.md
@.planning/phases/10-uv-python-3-13-lockfile/10-CONTEXT.md

# Current README structure (relevant sections)

Existing README.md has these sections in order:
- Screenshots
- Features
- Media backend (Plex or Jellyfin)
- Upgrade note (Kino Swipe → Jelly Swipe)
- Environment variables
- Jellyfin user identity contract (Phase 5)
- Jellyfin operator checks (manual)
- Minimal .env examples
- Coming Soon
- Requirements
- TMDB API instructions
- Deployment (ends at line 205 with TMDB attribution)

The new "Development" section should be inserted AFTER the "Deployment" section, before the end of file.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Development section to README.md with uv documentation</name>
  <files>README.md</files>
  <action>Add a new "## Development" section after the "## Deployment" section (after line 205, before the TMDB attribution image line). The section should include:

## Development

For local development and contributing, use **uv** for dependency management. This project requires **Python 3.13**.

### First-time setup

```bash
# Install dependencies from the committed lockfile
uv sync
```

This creates a virtual environment in `.venv/` and installs all dependencies from `pyproject.toml` and `uv.lock`.

### Running the app locally

**Development server (auto-reload):**
```bash
uv run python -m jellyswipe
```

**Production-style server (for testing):**
```bash
uv run gunicorn -b 0.0.0.0:5005 -k gevent --worker-connections 1000 jellyswipe:app
```

### Managing dependencies

**Add a new dependency:**
```bash
uv add <package-name>
```

**Update the lockfile after dependency changes:**
```bash
uv lock --upgrade
```

Commit both `pyproject.toml` and `uv.lock` when adding or updating dependencies.

### Notes

- The app is installed as a package (`jellyswipe/` layout), not a script. Run it via `uv run python -m jellyswipe` or `uv run gunicorn jellyswipe:app`.
- All production deployment uses Docker (see Deployment section above). Local dev uses uv for fast iteration.
- Distribution is Docker-only (Docker Hub and GHCR). There is no PyPI package to install via pip.

Insert this section between the end of "## Deployment" and the TMDB attribution image. Ensure the Development section uses ONLY uv commands and makes NO references to pip install from PyPI or installing jellyswipe as a package from PyPI per D-06, D-07, D-08, D-09, D-10.</action>
  <verify>grep -A 50 "## Development" README.md exists and contains uv sync, uv run, uv add, uv lock commands; grep -i "pip install jellyswipe" README.md returns no results (no PyPI installation references)</verify>
  <done>README.md has Development section after Deployment with uv commands, Python 3.13 requirement note, and no PyPI references</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| contributor → README.md | Documentation guides contributor actions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-04 | I (Information disclosure) | README.md | accept | No secrets in documentation. Environment variable examples use placeholders. |
| T-12-05 | S (Spoofing) | Installation instructions | mitigate | Explicitly state "no PyPI package" to prevent contributors from attempting pip install from untrusted sources |
</threat_model>

<verification>
- README.md contains "## Development" section
- Development section appears after "## Deployment" section
- uv sync, uv run, uv add, uv lock --upgrade commands are documented
- Python 3.13 requirement is mentioned
- No references to "pip install jellyswipe" from PyPI
- No references to PyPI as an installation method
- Existing Deployment section remains unchanged
- README.md is valid Markdown with no formatting errors
</verification>

<success_criteria>
1. README.md has new "Development" section in correct location (after Deployment)
2. Development section documents uv sync for first-time setup
3. Development section documents uv run python -m jellyswipe for dev server
4. Development section documents uv run gunicorn jellyswipe:app for production-style testing
5. Development section documents uv add for adding dependencies
6. Development section documents uv lock --upgrade for updating lockfile
7. Python 3.13 requirement is mentioned
8. No documentation implies pip install jellyswipe from PyPI
9. No PyPI mentioned as an installation method
10. Deployment section remains intact and unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/12-docker-maintainer-docs/12-02-SUMMARY.md`
</output>
