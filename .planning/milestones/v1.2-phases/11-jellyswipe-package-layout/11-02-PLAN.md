---
phase: 11-jellyswipe-package-layout
plan: 02
type: execute
wave: 2
depends_on: [11-01]
files_modified:
  - jellyswipe/__init__.py
  - jellyswipe/db.py
  - app.py
autonomous: true
requirements:
  - PKG-01
must_haves:
  truths:
    - "jellyswipe/__init__.py creates Flask app at module import time"
    - "Environment validation runs when jellyswipe package is imported"
    - "Database functions (get_db, init_db) accessible via jellyswipe.get_db() and jellyswipe.init_db()"
    - "Flask routes are registered in the app"
  artifacts:
    - path: "jellyswipe/__init__.py"
      provides: "Flask app instantiation and route registration"
      contains: "app = Flask("
      min_lines: 400
    - path: "jellyswipe/db.py"
      provides: "Database functions (get_db, init_db, schema migrations)"
      contains: "def get_db", "def init_db"
      min_lines: 50
    - path: "app.py"
      provides: "Monolithic Flask app (to be deprecated after this plan)"
      note: "This file will be kept for now but marked as deprecated"
  key_links:
    - from: "jellyswipe/__init__.py"
      to: "jellyswipe/db.py"
      via: "relative import"
      pattern: "from .db import get_db, init_db"
    - from: "jellyswipe/__init__.py"
      to: "jellyswipe.factory.py"
      via: "relative import"
      pattern: "from .factory import get_provider"
    - from: "jellyswipe/db.py"
      to: "SQLite database"
      via: "sqlite3.connect"
      pattern: "sqlite3.connect"
---

<objective>
Move database functions to **jellyswipe/db.py** and create Flask app in **jellyswipe/__init__.py**.

Purpose: Per D-01, D-03, D-05 — Flask app is created at package import time; database functions are separated into their own module.
Output: jellyswipe/__init__.py with Flask app and routes; jellyswipe/db.py with database functions.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-jellyswipe-package-layout/11-CONTEXT.md
@.planning/phases/11-jellyswipe-package-layout/11-01-SUMMARY.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md

# Existing code to migrate
@app.py (lines 71-100): get_db() and init_db() functions
@app.py (lines 1-560): Full Flask app with routes, env validation, SSE

# Provider modules (already moved in plan 11-01)
@jellyswipe/factory.py — get_provider() function
@jellyswipe/base.py — LibraryMediaProvider abstract class
@jellyswipe/plex_library.py — PlexLibraryProvider
@jellyswipe/jellyfin_library.py — JellyfinLibraryProvider
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create jellyswipe/db.py with database functions</name>
  <files>jellyswipe/db.py</files>
  <action>
    Create `jellyswipe/db.py` with the database functions from `app.py` (lines 71-100 approximately).

    Include:
    - `get_db()` — Connects to SQLite DB at DB_PATH
    - `init_db()` — Creates tables (rooms, swipes, matches) and runs schema migrations
    - Any DB_PATH and CLIENT_ID constants needed

    Keep the logic identical to `app.py` — this is a straight extraction, not a refactor.

    Per D-08, no imports needed within db.py since it only uses stdlib sqlite3 and os.
  </action>
  <verify>jellyswipe/db.py contains get_db() and init_db() functions</verify>
  <done>jellyswipe/db.py created with database functions</done>
</task>

<task type="auto">
  <name>Task 2: Create Flask app in jellyswipe/__init__.py</name>
  <files>jellyswipe/__init__.py</files>
  <action>
    Replace the skeleton in `jellyswipe/__init__.py` with the full Flask app implementation migrated from `app.py`.

    **Migration steps:**

    1. **Environment setup** (lines 1-66 from app.py):
       - Keep the .env loading try/except block
       - Import all required modules (Flask, werkzeug, sqlite3, os, etc.)
       - Define DB_PATH and CLIENT_ID constants
       - Define _normalized_media_provider() and MEDIA_PROVIDER
       - Run env validation (missing vars check)
       - **IMPORTANT:** Update imports at lines 67-68:
         - Change `from media_provider import get_provider` → `from .factory import get_provider`
         - Change `from media_provider.jellyfin_library import JellyfinLibraryProvider` → `from .jellyfin_library import JellyfinLibraryProvider`

    2. **Flask app instantiation** (lines 58-65 from app.py):
       - Create `app = Flask(__name__)` (per D-14, agent discretion on name parameter)
       - Configure ProxyFix middleware
       - Set secret_key
       - Define PLEX_URL, ADMIN_TOKEN, JELLYFIN_URL, TMDB_API_KEY constants

    3. **Import database functions** (add after app creation):
       - Add `from .db import get_db, init_db` (per D-08, relative import)
       - Call `init_db()` at module load time to ensure DB is initialized (matches current app.py pattern, per D-09)

    4. **Copy all Flask routes** (remaining lines from app.py):
       - Copy all @app.route decorators and their handler functions
       - Keep the SSE generator at /room/stream
       - Keep all Plex and Jellyfin-specific routes
       - **IMPORTANT:** Update any imports within route handlers to use relative imports where needed
         - `from media_provider import X` → `from .module import X` or `from .factory import X`

    5. **Export only the app object** (per D-04):
       - At the end of the file, ensure only `app` is exported
       - Do NOT add an `__all__` list — let consumers import `from jellyswipe import app`

    **Flask instantiation pattern (D-14):**
    The agent has discretion on whether to use `Flask(__name__)` or `Flask('jellyswipe')`. The current app.py uses `Flask(__name__)`. After moving templates/ and static/ in a later plan, ensure Flask can find them under jellyswipe/templates/ and jellyswipe/static/ — this may require adjusting the Flask instantiation or template_folder/static_folder parameters.

    Keep all logic identical to app.py — this is a migration, not a refactor.
  </action>
  <verify>jellyswipe/__init__.py contains Flask app with all routes from app.py</verify>
  <done>jellyswipe/__init__.py contains full Flask app implementation</done>
</task>

<task type="auto">
  <name>Task 3: Update app.py to import from jellyswipe (temporary shim)</name>
  <files>app.py</files>
  <action>
    Update `app.py` to be a thin re-export from the jellyswipe package (temporary until imports are updated in a later plan).

    Replace the entire contents of `app.py` with:

    ```python
    """
    Jelly Swipe — Flask application (legacy entry point).

    This file is a temporary shim for backward compatibility.
    The actual Flask app lives in the jellyswipe package.

    Per PKG-01, server code lives under jellyswipe/ and this file
    will be removed or become a documented thin re-export after
    all imports are updated in a later plan.
    """

    from jellyswipe import app  # noqa: F401

    __all__ = ["app"]
    ```

    This allows existing code that imports `from app import app` or uses `app:app` in Gunicorn to continue working while we update imports.
  </action>
  <verify>app.py contains only the jellyswipe import shim</verify>
  <done>app.py updated to import from jellyswipe package</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Flask app initialization | Env validation and DB init at import time |
| Database operations | SQLite queries in routes and db.py |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-03 | I | Env validation in __init__.py | accept | Existing pattern from app.py — no new attack surface |
| T-11-04 | I | DB init at import time | accept | Existing pattern from app.py — no change in security model |
| T-11-05 | T | Route handlers in jellyswipe/__init__.py | accept | Migration only — no logic changes |
</threat_model>

<verification>
1. Verify `jellyswipe/db.py` exists with get_db() and init_db()
2. Verify `jellyswipe/__init__.py` imports from .db and .factory
3. Verify `jellyswipe/__init__.py` creates Flask app and calls init_db()
4. Verify `jellyswipe/__init__.py` contains all routes from original app.py
5. Verify `app.py` is now a thin shim importing from jellyswipe
6. Smoke test: `python -c "from jellyswipe import app; print(app)"` should succeed
7. Import test: `python -c "import jellyswipe; print('app' in dir(jellyswipe))"` should print True
</verification>

<success_criteria>
1. `jellyswipe/db.py` exists with get_db() and init_db() functions
2. `jellyswipe/__init__.py` creates Flask app at module import time
3. `jellyswipe/__init__.py` contains all routes from original app.py
4. `jellyswipe/__init__.py` imports get_db, init_db from .db (relative import)
5. `jellyswipe/__init__.py` imports get_provider from .factory (relative import)
6. `app.py` is a thin shim importing from jellyswipe
7. `from jellyswipe import app` succeeds
8. DB initialization runs when jellyswipe package is imported
</success_criteria>

<output>
After completion, create `.planning/phases/11-jellyswipe-package-layout/11-02-SUMMARY.md`
</output>
