# Milestone v1.4: Clean up Unraid Template

**Status:** ✅ SHIPPED 2026-04-26
**Phases:** 18
**Total Plans:** 3

## Overview

Fix the Unraid template to use Jellyfin environment variables instead of removed Plex variables, ensuring users can successfully deploy the application. This milestone removes all legacy Plex references from the Unraid deployment template and adds CI validation to prevent future drift.

## Phases

### Phase 18: Unraid Template Cleanup

**Goal**: Update Unraid template variables and add CI validation
**Depends on**: Phase 17 (Unit Tests)
**Plans**: 3 plans

Plans:

- [x] 18-01: Update Unraid template with Jellyfin environment variables and blank placeholders
- [x] 18-02: Create CI lint workflow to validate Unraid template variables
- [x] 18-03: Document Unraid template and CI validation in README

**Details:**

Replaced PLEX_URL and PLEX_TOKEN with JELLYFIN_URL and JELLYFIN_API_KEY in both Variable and Config sections. Removed all fake placeholder values and set masked fields to empty values. Created Python lint script and GitHub Actions workflow to validate template variables against recognized app env vars. Added comprehensive README documentation for Unraid deployment.

---

## Milestone Summary

**Key Decisions:**

- Decision: Config entries with Type="Variable" are the only environment variables in Unraid templates (Rationale: Simplifies validation logic)
- Decision: Quote 'on' key in YAML to prevent boolean keyword parsing (Rationale: GitHub Actions YAML parser treats 'on' as boolean)
- Decision: Unraid template uses both <Variable> (legacy) and <Config> (modern) sections for environment variables (Rationale: Supports both old and new Unraid template formats)

**Issues Resolved:**

- Fixed broken Unraid template deployment due to removed Plex variables
- Eliminated user confusion from fake placeholder values in template
- Prevented future template drift with automated CI validation

**Issues Deferred:**

None

**Technical Debt Incurred:**

None

**Performance Metrics:**

- **Duration:** ~15 minutes
- **Plans:** 3 total
- **Tasks:** 3 total
- **Files Modified:** 3 (unraid_template/jelly-swipe.html, .github/workflows/unraid-template-lint.yml, scripts/lint-unraid-template.py, README.md)
- **Lines Changed:** 482 insertions, 0 deletions
- **Timeline:** 2026-04-26 00:00:56 → 2026-04-26 00:15:29

**Key Deliverables:**

- Unraid template updated with JELLYFIN_URL and JELLYFIN_API_KEY environment variables
- Python script (scripts/lint-unraid-template.py) for template validation
- GitHub Actions workflow (.github/workflows/unraid-template-lint.yml) for CI validation
- README.md documentation for Unraid template deployment

---

_For current project status, see .planning/ROADMAP.md_
