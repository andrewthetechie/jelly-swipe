---
phase: "06"
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/02-media-provider-abstraction/02-VERIFICATION.md
autonomous: true
requirements:
  - ARC-01
  - ARC-02
  - ARC-03
---

# Plan 02 — Phase 2 verification artifact (ARC-01..03) + Plex route-level parity checklist

<objective>
Create `.planning/phases/02-media-provider-abstraction/02-VERIFICATION.md` as the auditable verification record for **ARC-01**, **ARC-02**, and **ARC-03**, including **live Plex** route-level parity checks for **ARC-02** per `06-CONTEXT.md` D-05, plus explicit evidence for **ARC-03** module locality (dedicated Jellyfin module exists; auth/http not scattered).
</objective>

<threat_model>
| ID | Threat | Severity | Mitigation |
|----|--------|----------|------------|
| T-06-01 | Secrets pasted into verification evidence tables | high | Redact tokens; never paste full URLs containing secrets; prefer “OK/FAIL + endpoint name” |
| T-06-02 | “Parity” claimed without route proof | medium | Route checklist must include explicit HTTP paths exercised and observed outcomes |
</threat_model>

<tasks>

<task id="06-02-01" type="execute">
<read_first>
- .planning/phases/06-verification-closure-foundation-abstraction/06-CONTEXT.md
- .planning/phases/02-media-provider-abstraction/02-CONTEXT.md
- .planning/ROADMAP.md
- .planning/REQUIREMENTS.md
- app.py
- media_provider/base.py
- media_provider/factory.py
- media_provider/plex_library.py
- media_provider/jellyfin_library.py
</read_first>
<action>
1. Create `.planning/phases/02-media-provider-abstraction/02-VERIFICATION.md` with a traceability table mapping **ARC-01/02/03** to evidence rows.
2. **ARC-01 evidence**: document the provider entrypoints used by routes (name the Flask routes and the provider methods they call) based on code reading + at least one **live** call path exercised in Plex mode (record which route you hit and what JSON/shape you observed at a high level).
3. **ARC-02 evidence (route-level, live Plex)**: run the checklist in the verification file, minimally covering:
   - room create + deck load (thumbs present)
   - swipe/match sanity (at least one swipe path that exercises persisted state)
   - trailer + cast endpoints for a known movie id
   - proxy image fetch for a poster/thumb path used by cards
   - `/plex/server-info` returns `{machineIdentifier,name}` shape
   Record **PASS/FAIL/PARTIAL** per row with date and environment notes (no secrets).
4. **ARC-03 evidence**: record module boundaries showing Jellyfin HTTP/auth is concentrated in `media_provider/jellyfin_library.py` (and factory wiring), not ad hoc in unrelated modules—support with `rg` evidence commands and short excerpts in the verification file.
5. **Integration check (Phase 1 ↔ Phase 2)**: document the expected fail-fast behavior for `jellyfin` mode before Phases 3–4 completeness (per roadmap Phase 2 success criterion #3) as an explicit **live** or **intentionally observed** outcome row (do not contradict `02-CONTEXT.md` scope).
</action>
<acceptance_criteria>
- `test -f .planning/phases/02-media-provider-abstraction/02-VERIFICATION.md` exits 0.
- `grep -n "ARC-01" .planning/phases/02-media-provider-abstraction/02-VERIFICATION.md` exits 0.
- `grep -n "ARC-02" .planning/phases/02-media-provider-abstraction/02-VERIFICATION.md` exits 0.
- `grep -n "ARC-03" .planning/phases/02-media-provider-abstraction/02-VERIFICATION.md` exits 0.
- `grep -n "/plex/server-info" .planning/phases/02-media-provider-abstraction/02-VERIFICATION.md` exits 0.
- `grep -n "/get-trailer" .planning/phases/02-media-provider-abstraction/02-VERIFICATION.md` exits 0.
- `grep -n "/cast/" .planning/phases/02-media-provider-abstraction/02-VERIFICATION.md` exits 0.
- `grep -n "/proxy" .planning/phases/02-media-provider-abstraction/02-VERIFICATION.md` exits 0.
- `grep -nE "PLEX_TOKEN|X-Plex-Token|token=[A-Za-z0-9._-]{8,}" .planning/phases/02-media-provider-abstraction/02-VERIFICATION.md` exits **1** (no high-entropy token patterns in file).
</acceptance_criteria>
</task>

</tasks>

<verification>
1. Confirm ARC-02 checklist rows are all dated and have PASS/FAIL/PARTIAL states (no blank outcomes).
2. Confirm ARC-03 evidence cites `media_provider/jellyfin_library.py` and does not claim scattered auth beyond documented exceptions (watchlist/pin remain Plex-specific per Phase 2 context).
</verification>

<success_criteria>
- ARC-01/02/03 are evidenced in `02-VERIFICATION.md` with live Plex route checklist coverage for ARC-02 as required by `06-CONTEXT.md`.
</success_criteria>
