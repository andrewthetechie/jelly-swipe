---
phase: "07"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/03-jellyfin-authentication-http-client/03-VERIFICATION.md
autonomous: true
requirements:
  - JAUTH-01
  - JAUTH-02
  - JAUTH-03
---

# Plan 01 — Phase 3 verification artifact (JAUTH-01..03)

<objective>
Author `.planning/phases/03-jellyfin-authentication-http-client/03-VERIFICATION.md` with traceability and **live** runtime evidence for **JAUTH-01**, **JAUTH-02**, and **JAUTH-03**, matching Phase 6 evidence style and `07-CONTEXT.md` D-03–D-05.
</objective>

<threat_model>
| ID | Threat | Severity | Mitigation |
|----|--------|----------|------------|
| T-07-01 | Secrets pasted into verification markdown | high | Never paste `JELLYFIN_API_KEY`, access tokens, or full `MediaBrowser` headers; use `dummy` host and redacted placeholders only |
| T-07-02 | False PASS from `.env` leakage | medium | Document `env -i PATH=… HOME=…` style invocations where isolation matters |
</threat_model>

<tasks>

<task id="07-01-01" type="execute">
<read_first>
- .planning/phases/07-verification-closure-jellyfin-parity/07-CONTEXT.md
- .planning/phases/03-jellyfin-authentication-http-client/03-CONTEXT.md
- .planning/REQUIREMENTS.md
- app.py
- media_provider/jellyfin_library.py
- media_provider/factory.py
</read_first>
<action>
1. Create `03-VERIFICATION.md` with YAML frontmatter (`phase: 3`, `status: complete` when filled, `verified: YYYY-MM-DD`).
2. Add a **traceability table** for **JAUTH-01..03** with columns: Requirement | Status | Evidence summary | Date.
3. **JAUTH-01:** Document supported env contract (`JELLYFIN_URL` + API key **or** username+credential pair) from `app.py` validation and `_login_from_env` branches; cite line-level evidence via `grep` command outputs (no secrets).
4. **JAUTH-02:** Record `get_provider()` / `ensure_authenticated` behavior: successful `/Items?Limit=1` probe after login; **reset** clears token (cite `reset()` in `jellyfin_library.py`). Include at least one **live** `test_client` or CLI transcript: e.g. `MEDIA_PROVIDER=jellyfin` + unreachable `JELLYFIN_URL` shows connection failure on first provider use (expected).
5. **JAUTH-03:** Evidence that JSON errors from representative routes do **not** echo `JELLYFIN_API_KEY` or bearer tokens — use `dummy` key and show error strings are generic / transport-level only.
</action>
<acceptance_criteria>
- `test -f .planning/phases/03-jellyfin-authentication-http-client/03-VERIFICATION.md` exits 0.
- `grep -n "JAUTH-01" .planning/phases/03-jellyfin-authentication-http-client/03-VERIFICATION.md` exits 0.
- `grep -n "JAUTH-02" .planning/phases/03-jellyfin-authentication-http-client/03-VERIFICATION.md` exits 0.
- `grep -n "JAUTH-03" .planning/phases/03-jellyfin-authentication-http-client/03-VERIFICATION.md` exits 0.
- `grep -nE "JELLYFIN_API_KEY|PLEX_TOKEN|Authorization:" .planning/phases/03-jellyfin-authentication-http-client/03-VERIFICATION.md` exits 1.
</acceptance_criteria>
</task>

</tasks>

<verification>
1. Re-read `03-VERIFICATION.md` and confirm each JAUTH row has a dated status and reproducible procedure.
2. Re-run the secret-marker `grep` from acceptance (must exit 1).
</verification>

<success_criteria>
- JAUTH-01..03 are independently evidenced with runtime notes where feasible per `07-CONTEXT.md`.
</success_criteria>
