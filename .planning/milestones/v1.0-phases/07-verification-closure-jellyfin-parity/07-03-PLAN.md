---
phase: "07"
plan: "03"
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/05-user-parity-packaging/05-VERIFICATION.md
autonomous: true
requirements:
  - JUSR-01
  - JUSR-02
  - JUSR-03
  - JUSR-04
---

# Plan 03 — Phase 5 verification artifact (JUSR-01..04)

<objective>
Author `.planning/phases/05-user-parity-packaging/05-VERIFICATION.md` with traceability for **JUSR-01..04**, including route-level behavior for identity headers, `/auth/jellyfin-login`, `/watchlist/add`, and packaging checks (`requirements.txt`, CI workflow reference).
</objective>

<threat_model>
| ID | Threat | Severity | Mitigation |
|----|--------|----------|------------|
| T-07-01 | User session token pasted into docs | high | Describe token as “present” only; never paste JWT/API material |
| T-07-04 | Misleading PASS on Docker | medium | Cite `docker build` or workflow name from observation or “not re-run in this closure” with PARTIAL |
</threat_model>

<tasks>

<task id="07-03-01" type="execute">
<read_first>
- .planning/phases/07-verification-closure-jellyfin-parity/07-CONTEXT.md
- .planning/phases/05-user-parity-packaging/05-CONTEXT.md
- .planning/REQUIREMENTS.md
- app.py
- templates/index.html
- requirements.txt
- .github/workflows/docker-image.yml
</read_first>
<action>
1. Create `05-VERIFICATION.md` with frontmatter (`phase: 5`, `status`, `verified` date).
2. Traceability table for **JUSR-01..04**.
3. **JUSR-01:** Document `_provider_user_id_from_request` precedence (`X-Provider-User-Id`, `X-Jellyfin-User-Id`, `X-Emby-UserId`, `X-Plex-User-ID`, then token resolution); cite `/room/swipe` 400 when identity missing in Jellyfin mode.
4. **JUSR-02:** Document `/watchlist/add` 401 without user token; note `add_to_user_favorites` path (no live call without user token — mark PARTIAL with repro steps).
5. **JUSR-03:** Summarize `templates/index.html` Jellyfin vs Plex auth path (grep-based evidence: `jellyfin-login`, `auth_provider`, etc.).
6. **JUSR-04:** List `requirements.txt` deps added for Jellyfin path and `.github/workflows/docker-image.yml` existence; state CI status as observed on closure date or PARTIAL if not executed.
</action>
<acceptance_criteria>
- `test -f .planning/phases/05-user-parity-packaging/05-VERIFICATION.md` exits 0.
- `grep -n "JUSR-01" .planning/phases/05-user-parity-packaging/05-VERIFICATION.md` exits 0.
- `grep -n "watchlist" .planning/phases/05-user-parity-packaging/05-VERIFICATION.md` exits 0.
- `grep -n "jellyfin-login" .planning/phases/05-user-parity-packaging/05-VERIFICATION.md` exits 0.
- `grep -nE "JELLYFIN_API_KEY|PLEX_TOKEN|Authorization:" .planning/phases/05-user-parity-packaging/05-VERIFICATION.md` exits 1.
</acceptance_criteria>
</task>

</tasks>

<verification>
1. Confirm swipe/delete/undo identity requirements reference the same `plex_id` column semantics as `05-CONTEXT.md`.
</verification>

<success_criteria>
- User parity and packaging requirements have traceable evidence rows.
</success_criteria>
