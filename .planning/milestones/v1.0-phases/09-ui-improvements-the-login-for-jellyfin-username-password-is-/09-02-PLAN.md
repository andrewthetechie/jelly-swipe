---
phase: "09"
plan: "02"
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - templates/index.html
  - data/index.html
autonomous: true
requirements: []
---

# Plan 02 — Poster `object-fit` containment (full artwork visible)

<objective>
Stop cropping wide Jellyfin/TMDB poster art on the main swipe deck and mini-poster surfaces by replacing `object-fit: cover` with `contain` and neutral backing, per `09-CONTEXT` D-05–D-06 and operator note `.planning/notes/2026-04-24-aspect-ratio-displayed-posters.md`.
</objective>

<threat_model>
| ID | Threat | Severity | Mitigation |
|----|--------|----------|------------|
| T-09-10 | Layout regression breaking swipe hit targets | medium | Keep outer `.movie-card` dimensions unchanged; only change **inner** `img` fit and backing fill |
| T-09-11 | PWA `data/index.html` diverges from `templates/index.html` | medium | Edit both files in one commit with mirrored selectors |
</threat_model>

<tasks>

<task id="09-02-01" type="execute">
<read_first>
- templates/index.html
- data/index.html
- .cursor/rules/gsd-project.md
- .planning/phases/09-ui-improvements-the-login-for-jellyfin-username-password-is-/09-CONTEXT.md
</read_first>
<action>
1. In **both** `templates/index.html` and `data/index.html`, locate the rules:
   - `.movie-card img { ... object-fit: cover ... }`
   - `.mini-front img { ... object-fit: cover ... }`
   Change `object-fit` from `cover` to **`contain`**.
2. Ensure poster **containers** (`.card-front`, `.mini-front` — add if missing) use `background-color: #000` so letterboxing bars match trailer area styling (`#000` already used for `.trailer-box`).
3. Update `.match-poster-preview` rule if it still uses `object-fit: cover` on the same element — switch to **`contain`** while keeping `width: 140px; height: 210px` fixed frame (pillarboxing acceptable).
4. Do **not** change `#swipe-deck` height, `.movie-card` absolute positioning, or swipe JS math in this plan.
</action>
<acceptance_criteria>
- `grep -n "object-fit: contain" templates/index.html | wc -l` returns at least `3` (main deck, mini front, match popup).
- `grep -n "object-fit: contain" data/index.html | wc -l` returns at least `3`.
- `grep '.movie-card img' templates/index.html | grep 'object-fit: cover'` exits **1** (main deck image must not use `cover`).
- `grep '.mini-front img' templates/index.html | grep 'object-fit: cover'` exits **1**.
- `grep -n ".match-poster-preview" templates/index.html` exits 0 and that rule block includes `object-fit: contain`.
</acceptance_criteria>
</task>

</tasks>

<verification>
1. Side-by-side visual check (operator): one wide poster before/after screenshot note in SUMMARY.
2. Re-run grep pair comparing `templates` vs `data` for `.movie-card img` blocks — first differing line number reported in SUMMARY if any drift.
</verification>

<success_criteria>
- Primary deck posters no longer crop left/right edges for typical theatrical one-sheets.
- `templates/index.html` and `data/index.html` remain structurally aligned for poster CSS.
</success_criteria>
