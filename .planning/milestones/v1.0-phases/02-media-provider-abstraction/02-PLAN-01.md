---
phase: "02"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - media_provider/__init__.py
  - media_provider/base.py
  - media_provider/plex_library.py
  - media_provider/factory.py
autonomous: true
requirements:
  - ARC-01
  - ARC-02
  - ARC-03
---

# Plan 01 — `media_provider` package: ABC, Plex implementation, factory (no `app.py` migration)

<objective>
Introduce a new `media_provider/` package with an `abc.ABC` contract covering library genres, deck JSON, per-item resolution for the TMDB chain, Plex server identity, and validated upstream image fetch for card thumbs. Implement `PlexLibraryProvider` by lifting the current `get_plex` / `reset_plex` / `get_plex_genres` / `fetch_plex_movies` semantics (including instance-level genre cache and retry-after-reset on section access). Add `factory.py` with process-wide `get_provider()` and `reset()` singleton invalidation. For `MEDIA_PROVIDER=jellyfin`, `get_provider()` raises a clear `RuntimeError` aligned with today’s `get_plex()` guard—**no** Jellyfin implementation file in this wave (ARC-03).
</objective>

<threat_model>
| ID | Threat | Severity | Mitigation in this plan |
|----|--------|----------|-------------------------|
| T-01 | Open proxy via image fetch (user-controlled `path` concatenated to server URL) | high | Implement `fetch_library_image(path: str)` (or equivalent) on `PlexLibraryProvider` with the **same** allowlist as today: reject missing `path` or any `path` not starting with `/library/metadata/` by raising a dedicated exception (e.g. `PermissionError` or custom) mapped to HTTP **403** in wave 2; never pass arbitrary paths to `requests.get` |
| T-02 | Secrets (Plex token, URLs with embedded credentials) leaked via exception messages | medium | Do not embed `ADMIN_TOKEN` / full authenticated URLs in raised errors; prefer generic failure messages; when surfacing `RuntimeError` for jellyfin mode, mirror existing copy and avoid echoing env **values** |
| T-03 | Jellyfin “partial route” confusion: some code paths appear library-ready while others 503/500 inconsistently | medium | `get_provider()` is the **only** library entry for non-Plex modes in later waves; in this plan, `get_provider()` for `jellyfin` raises **immediately** with the same conceptual message as `get_plex()` today—no concrete provider instance, no `PlexLibraryProvider` construction, no split between “factory ok” and “methods fail” |
| T-04 | SSRF / blind SSRF via upstream image URL | medium | Restrict outbound `requests` URL to `{PLEX_URL}{path}` where `path` is validated as in T-01; do not follow redirects to arbitrary hosts (preserve current `requests.get` behavior document in code comment if unchanged) |
</threat_model>

<tasks>

<task id="02-01-01" type="execute">
<read_first>
- .planning/phases/02-media-provider-abstraction/02-CONTEXT.md
- .planning/phases/02-media-provider-abstraction/02-PATTERNS.md
- app.py (functions `get_plex`, `reset_plex`, `get_plex_genres`, `fetch_plex_movies`, constants `MEDIA_PROVIDER`, `PLEX_URL`, `ADMIN_TOKEN`)
</read_first>
<action>
1. Create `media_provider/base.py` defining abstract base class `LibraryMediaProvider` (name discretionary but use one consistently) subclassing `abc.ABC`.
2. Declare `@abstractmethod` APIs (exact names discretionary) that together cover CONTEXT D-02 A–E:
   - **Genres:** equivalent to `get_plex_genres()` return (`list[str]`, including Sci-Fi label normalization), with caching **on the concrete instance** (no `app.py` global).
   - **Deck:** equivalent to `fetch_plex_movies(genre_name=None)` JSON list shape (`id`, `title`, `summary`, `thumb`, `rating`, `duration`, `year`) and semantics: Recently Added, Sci-Fi ↔ Science Fiction mapping, shuffle rules, `random`/`addedAt` sorts, retry `plex.library.section('Movies')` after `reset()` on failure.
   - **Resolve by id:** method accepting provider movie id (string or int) used by TMDB routes—internally `fetchItem` with **try → `reset()` → retry** parity.
   - **Server info:** method returning `dict` with keys `machineIdentifier` and `name` (friendly name), with same retry pattern as `/plex/server-info` today.
   - **Image fetch:** method taking raw Plex thumb path (e.g. `/library/metadata/...`) performing validated upstream GET and returning data suitable for Flask `Response` (bytes/stream + `Content-Type`), raising on bad path (for 403 mapping in wave 2). When `PLEX_URL` or `ADMIN_TOKEN` is missing, raise/behavior must allow wave 2 to return **503** (document contract in docstring).
3. Add minimal module docstring referencing ARC-01 as the stability boundary for library I/O.
</action>
<acceptance_criteria>
- `test -f media_provider/base.py` exits 0 and file contains `ABC` and at least five `@abstractmethod` definitions.
- `grep -n "class LibraryMediaProvider" media_provider/base.py` returns exactly one match line.
- `python -m py_compile media_provider/base.py` exits 0.
</acceptance_criteria>
</task>

<task id="02-01-02" type="execute">
<read_first>
- media_provider/base.py (from task 02-01-01)
- app.py (`get_plex`, `reset_plex`, `_plex_instance`, `_genre_cache`, `get_plex_genres`, `fetch_plex_movies`)
- .planning/phases/02-media-provider-abstraction/02-RESEARCH.md (lazy `plexapi` import)
</read_first>
<action>
1. Create `media_provider/plex_library.py` implementing `PlexLibraryProvider(LibraryMediaProvider)`.
2. Move **logic** from `get_plex` into construction/caching on the provider: hold `_plex_server` (or equivalent) lazy singleton **per provider instance**; lazy `from plexapi.server import PlexServer` inside the code path that builds the server; constructor accepts `plex_url: str` and `admin_token: str` (no env reads inside deep helpers if avoidable).
3. Implement `reset()` on this class mirroring `reset_plex()`: null the cached `PlexServer` reference and clear `_genre_cache` (instance field) so connection identity changes do not reuse stale genres (per RESEARCH “reset clears cache”).
4. Implement genre listing mirroring `get_plex_genres` including `try/except` → return `[]` on failure.
5. Implement deck fetch mirroring `fetch_plex_movies` including outer `try` for `library.section('Movies')`, `reset()` + retry, `do_shuffle` / `search_genre` / fallback search, `random.shuffle`, and thumb URLs as `/proxy?path=...`.
6. Implement TMDB resolve method using `fetchItem(int(movie_id))` with identical **double try** pattern to `get_trailer` / `get_cast` today.
7. Implement server info method using `machineIdentifier` and `friendlyName` with same retry pattern as `get_server_info` route.
8. Implement validated image fetch mirroring `/proxy` Plex branch: validate `path` prefix `/library/metadata/`, `requests.get(f"{plex_url}{path}?X-Plex-Token={token}", stream=True)`, return body/content-type; do not proceed on failed validation.
</action>
<acceptance_criteria>
- `grep -n "PlexLibraryProvider" media_provider/plex_library.py` returns at least one class definition line.
- `grep -n "plexapi" media_provider/plex_library.py` shows imports **only** inside functions/methods (no top-level `from plexapi` at column 1).
- `grep -n "_genre_cache" media_provider/plex_library.py` shows instance cache field (not imported from `app.py`).
- `python -m py_compile media_provider/plex_library.py` exits 0.
</acceptance_criteria>
</task>

<task id="02-01-03" type="execute">
<read_first>
- media_provider/plex_library.py
- app.py (`_normalized_media_provider`, `MEDIA_PROVIDER`, `get_plex` RuntimeError text for jellyfin)
- media_provider/base.py
</read_first>
<action>
1. Create `media_provider/factory.py` with module-level `_provider_singleton: LibraryMediaProvider | None` (typing optional per project Python version) and `_media_provider_name` read via the same normalization rules as `app.py` (`plex` default, case-insensitive, invalid → `RuntimeError` with allowed values message—may import `_normalized_media_provider` from `app` **only if** no circular import; **prefer** duplicating the small normalizer in `factory.py` to keep wave 1 import-safe, or read env directly per 01-plan pattern).
2. Implement `get_provider() -> LibraryMediaProvider`: if `MEDIA_PROVIDER != "plex"`, raise `RuntimeError` with message aligned to existing `get_plex()` string: Plex library access unavailable when `MEDIA_PROVIDER=jellyfin` until later phases; if singleton is `None`, construct `PlexLibraryProvider(PLEX_URL, ADMIN_TOKEN)` using **validated** module-level constants passed from caller: read `PLEX_URL` / `PLEX_TOKEN` from `os.environ` the same way as `app.py` **or** accept injection only from `get_provider` after import—document chosen approach in file docstring.
3. Implement `reset()` to call `.reset()` on the concrete singleton if present and then set singleton to `None` (mirror `reset_plex` invalidation semantics globally).
4. Ensure **no** `jellyfin*.py` file is added and no HTTP client to Jellyfin is introduced—only the guard branch.
5. Create `media_provider/__init__.py` exporting `get_provider`, `reset`, `LibraryMediaProvider`, `PlexLibraryProvider` as needed for `from media_provider import get_provider, reset`.
</action>
<acceptance_criteria>
- `test ! -f media_provider/jellyfin.py` and `test ! -f media_provider/jellyfin_client.py` both exit 0 (no Jellyfin impl stub).
- `grep -n "def get_provider" media_provider/factory.py` and `grep -n "def reset" media_provider/factory.py` each return a line.
- `grep -n "jellyfin" media_provider/factory.py` is limited to guard/messaging (no `requests.get` to `JELLYFIN_URL`).
- `python -m py_compile media_provider/factory.py media_provider/__init__.py` exits 0.
</acceptance_criteria>
</task>

</tasks>

<verification>
1. **Import smoke (Plex env):** With dummy `MEDIA_PROVIDER=plex`, `PLEX_URL`, `PLEX_TOKEN`, `TMDB_API_KEY`, `FLASK_SECRET` set, run `python -c "from media_provider import get_provider; p=get_provider(); assert p is get_provider()"` — exits 0 (singleton).
2. **Jellyfin guard:** With `MEDIA_PROVIDER=jellyfin` and required jellyfin env from Phase 1, run `python -c "from media_provider import get_provider; get_provider()"` — must raise `RuntimeError` whose message states Plex library access is unavailable / later phases (substring match).
3. **Reset:** In same Plex dummy env REPL, call `from media_provider import reset, get_provider; p=get_provider(); reset(); q=get_provider(); assert p is not q` **or** assert new instance identity per implementation doc (document if singleton reused—**preferred:** `reset()` clears so a **new** `PlexLibraryProvider` is created on next `get_provider()`).
4. **ARC-03 structure:** Confirm via listing `media_provider/` there is only `__init__.py`, `base.py`, `plex_library.py`, `factory.py` plus `__pycache__`—no Jellyfin client module.
</verification>

<success_criteria>
- **ARC-01:** Library-facing capabilities A–E from CONTEXT are expressed on the ABC and implemented on `PlexLibraryProvider` without remaining logic trapped only in planning docs.
- **ARC-02 (wave 1 scope):** `PlexLibraryProvider` is a mechanical lift of `get_plex` / `reset_plex` / `get_plex_genres` / `fetch_plex_movies` / `/proxy` Plex path validation + upstream fetch / server info + `fetchItem` retry semantics; full HTTP route parity is verified in `02-PLAN-02.md`.
- **ARC-03:** No scattered Jellyfin HTTP; no `jellyfin` implementation file; factory exposes a single extension point for a future dedicated Jellyfin provider module in Phase 3.
</success_criteria>
