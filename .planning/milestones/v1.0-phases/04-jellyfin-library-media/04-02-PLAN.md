---
phase: "04"
plan: "02"
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - media_provider/jellyfin_library.py
  - app.py
  - README.md
autonomous: true
requirements:
  - JLIB-04
  - JLIB-05
---

# Plan 02 — TMDB resolve + server-info parity + docs alignment

<objective>
Ensure Jellyfin-backed trailer/cast resolve and server-info parity are stable and documented, then align docs/context where implementation evolved (notably accepted Jellyfin item-id formats for proxy paths) to keep downstream planning/verification accurate (JLIB-04, JLIB-05).
</objective>

<threat_model>
| ID | Threat | Severity | Mitigation |
|----|--------|----------|------------|
| T-01 | Route parity drift between docs and runtime | medium | Update docs/context to match implemented behavior exactly |
| T-02 | Secret leakage in route errors | medium | Keep current generic error style; avoid adding sensitive payloads in raised messages |
</threat_model>

<tasks>

<task id="04-02-01" type="execute">
<read_first>
- media_provider/jellyfin_library.py
- app.py
- README.md
- .planning/phases/04-jellyfin-library-media/04-CONTEXT.md
</read_first>
<action>
1. Confirm `resolve_item_for_tmdb()` uses authenticated `GET /Items/{id}` and returns `.title/.year` consumed by `/get-trailer/<movie_id>` and `/cast/<movie_id>`.
2. Confirm `/plex/server-info` route returns provider-backed `{machineIdentifier,name}` in Jellyfin mode via `server_info()` (`/System/Info` with public fallback).
3. Update `04-CONTEXT.md` Decision D-06 to reflect current strict allowlist behavior accepting both 32-hex and canonical UUID item ids (same fixed `jellyfin/{id}/Primary` shape).
4. Ensure README's Jellyfin phase notes remain accurate for Phase 4 parity claims (`/proxy` thumbs and route behavior). Make minimal wording edit only if mismatch exists.
5. Run `python -m py_compile media_provider/jellyfin_library.py app.py`.
</action>
<acceptance_criteria>
- `grep -n "resolve_item_for_tmdb" media_provider/jellyfin_library.py` exits 0.
- `grep -n "/Items/{movie_id}" media_provider/jellyfin_library.py` exits 0.
- `grep -n "def get_server_info" app.py` exits 0.
- `grep -n "machineIdentifier" media_provider/jellyfin_library.py` exits 0.
- `grep -n "{32}" .planning/phases/04-jellyfin-library-media/04-CONTEXT.md` exits 0.
- `python -m py_compile media_provider/jellyfin_library.py app.py` exits 0.
</acceptance_criteria>
</task>

</tasks>

<verification>
- `grep -qi "Phase 4+" README.md` and `grep -qi "proxy" README.md` both exit 0 (doc parity quick check).
</verification>

<success_criteria>
- JLIB-04: Trailer/cast routes work with Jellyfin `movie_id` via provider metadata resolution.
- JLIB-05: Server info parity shape is returned in Jellyfin mode and documented consistently.
</success_criteria>
