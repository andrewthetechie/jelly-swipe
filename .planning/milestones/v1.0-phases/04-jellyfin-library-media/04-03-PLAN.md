---
phase: "04"
plan: "03"
type: execute
wave: 3
depends_on:
  - "02"
gap_closure: true
files_modified:
  - media_provider/jellyfin_library.py
  - app.py
autonomous: true
requirements:
  - JLIB-04
---

# Plan 03 — Gap fix: Jellyfin trailer/cast item lookup 500

<objective>
Fix the UAT gap where Jellyfin-backed trailer/cast requests return 500 by hardening item lookup in `resolve_item_for_tmdb()` and returning controlled route errors for known lookup failures.
</objective>

<tasks>

<task id="04-03-01" type="execute">
<read_first>
- media_provider/jellyfin_library.py
- app.py
- .planning/phases/04-jellyfin-library-media/04-UAT.md
- .planning/debug/jellyfin-tmdb-item-lookup-500.md
</read_first>
<action>
1. Update `resolve_item_for_tmdb()` to try a fallback Jellyfin item endpoint when `/Items/{id}` fails for valid deck ids (prefer user-scoped lookup with current user id).
2. In `/get-trailer` and `/cast`, map known Jellyfin lookup failures to controlled responses (`404` not found or explicit lookup error) rather than opaque 500 when the item cannot be resolved.
3. Keep behavior compatible for Plex mode and successful Jellyfin lookups.
4. Run `python -m py_compile media_provider/jellyfin_library.py app.py`.
</action>
<acceptance_criteria>
- `grep -n "resolve_item_for_tmdb" media_provider/jellyfin_library.py` exits 0.
- Manual/automated check with previously failing item id no longer produces 500 for both `/cast/{id}` and `/get-trailer/{id}`.
- `python -m py_compile media_provider/jellyfin_library.py app.py` exits 0.
</acceptance_criteria>
</task>

</tasks>

<verification>
- Re-run UAT test 5 from `04-UAT.md` using the failing item id from the report and confirm non-500 behavior.
</verification>

<success_criteria>
- UAT gap test 5 closes (trailer/cast lookup no longer fails with 500 for valid Jellyfin deck ids).
</success_criteria>
