---
phase: "04"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - media_provider/jellyfin_library.py
  - app.py
autonomous: true
requirements:
  - JLIB-01
  - JLIB-02
  - JLIB-03
---

# Plan 01 — Jellyfin deck/genres/image proxy parity

<objective>
Verify and, only if needed, tighten Jellyfin library/media paths so card JSON, genre/deck behavior, and `/proxy` image validation satisfy Phase 4 parity constraints (JLIB-01, JLIB-02, JLIB-03) while preserving Plex mode behavior.
</objective>

<threat_model>
| ID | Threat | Severity | Mitigation |
|----|--------|----------|------------|
| T-01 | Open proxy abuse through `/proxy` | high | Keep strict `jellyfin/{id}/Primary` allowlist checks in both route and provider |
| T-02 | Valid posters rejected due item-id format mismatch | medium | Allow both canonical UUID and 32-hex item ids while keeping fixed path shape |
</threat_model>

<tasks>

<task id="04-01-01" type="execute">
<read_first>
- media_provider/jellyfin_library.py
- app.py
- media_provider/plex_library.py
- .planning/phases/04-jellyfin-library-media/04-CONTEXT.md
</read_first>
<action>
1. Confirm `_item_to_card()` emits exactly `id,title,summary,thumb,rating,duration,year` and `thumb` uses `/proxy?path=jellyfin/{id}/Primary`.
2. Confirm `list_genres()` and `fetch_deck()` preserve Plex-equivalent semantics: Sci-Fi normalization, `Recently Added` date-desc without shuffle, randomized behavior elsewhere with fallback handling.
3. Confirm `/proxy` jellyfin branch in `app.py` enforces provider mode + URL presence and strict path allowlist; ensure provider `fetch_library_image()` independently validates the same fixed shape before upstream fetch.
4. If route/provider validation diverges, align them to one strict regex and keep behavior documented in context/comments.
5. Run `python -m py_compile media_provider/jellyfin_library.py app.py`.
</action>
<acceptance_criteria>
- `grep -n "\"id\": mid" media_provider/jellyfin_library.py` exits 0.
- `grep -n "Science Fiction" media_provider/jellyfin_library.py` exits 0.
- `grep -n "DateCreated" media_provider/jellyfin_library.py` exits 0.
- `grep -n "jellyfin/(?:[0-9a-fA-F]{32}|[0-9a-fA-F-]{36})/Primary" app.py` exits 0.
- `grep -n "_JF_IMAGE_PATH" media_provider/jellyfin_library.py` exits 0.
- `python -m py_compile media_provider/jellyfin_library.py app.py` exits 0.
</acceptance_criteria>
</task>

</tasks>

<verification>
1. Manual smoke in Jellyfin mode: start app, create room, verify `/movies` deck has poster thumbs and no proxy 403 spam for valid item ids.
2. Negative path: request `/proxy?path=jellyfin/../../etc/passwd` (or malformed id) returns 403.
</verification>

<success_criteria>
- JLIB-01: Jellyfin card objects match expected front-end shape.
- JLIB-02: Genre list and deck behavior align with Plex semantics.
- JLIB-03: Jellyfin thumbnails are served via Flask proxy with strict path validation.
</success_criteria>
